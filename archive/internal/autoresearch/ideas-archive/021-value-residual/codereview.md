# Code-review log — 021 value-residual

## r1 — 2026-06-10 — verdict: accept

- **Mechanism is implemented faithfully to the spec.** The blend at `models/layers.py:1522-1526` is exactly what `idea.md:18-23` / `plan.md:60-70` prescribes — `V = (1.0 - self.lambda_v) * V + self.lambda_v * v_residual` (post-transpose, post-GQA `repeat_interleave`, pre-`v_norm`, pre-attention; both V and `v_residual` have shape `[B, n_heads, T, d_k]` here, invariant to `n_kv_heads`). The stash at `models/layers.py:1524` is `self._v_residual = V.detach()` — `.detach()` correctly blocks the layer-l blend gradient from flowing back into layer-0 `W_V` (the spec's "each layer's W_V trains on its own attention path"). Verified end-to-end at runtime: block-0 stash shape `[1, 4, 32, 16] = [B, n_heads, T, d_k]` with the GQA=2 trt config, matching `idea.md:24`.
- **Flag-OFF is identity-safe by construction.** `MHA.__init__` (`models/layers.py:725-728`) only creates `self.lambda_v = nn.Parameter(...)` and `self._v_residual = None` inside the `if self.use_value_residual:` guard, so the optimizer's parameter set is byte-identical when off (no new params allocated). The forward branch at `models/layers.py:1522` is fully gated. `TransformerBlock.forward` and `MinimalLLM.forward` pass `v_residual=v_residual` unconditionally, but with the default `None` chain when off — no compute, no compute path change. `test_flag_off_byte_identical_to_pre_021_build` confirms flag-off determinism AND asserts `not hasattr(block.attention, 'lambda_v')` when off.
- **Flag-ON step-0 is identity within fp32 rounding noise.** `lambda_v = nn.Parameter(torch.zeros(()))` (`models/layers.py:727`) ⇒ `(1 - 0)·V + 0·V_1 = V` mathematically; one extra fp32 multiply-add introduces sub-`1e-6` drift. `test_flag_on_step0_identity_at_lambda_zero` pins this with `assert diff < 1e-5` (passes). Runtime confirmation: all 12 `lambda_v` values are exactly `0.0` on a fresh `Tiny1M3MVResidualOnFireConfig` build.
- **Gradient path is live and correctly partitioned.** Confirmed empirically: `block[0].attention.lambda_v.grad = None` (block 0 stashes, never blends — its `lambda_v` is unused, so no gradient; correct), `block[1..11].attention.lambda_v.grad` are all non-zero finite scalars (e.g. block 1 = 23.18, block 6 = 22.95, block 11 = 38.50). The blend is a direct multiply by `self.lambda_v`, so `dL/dλ_v = ∂L/∂V_blended · (V_1 - V)`. `test_wiring_live_when_lambda_nonzero` pins this from the output side: setting `lambda_v = 0.5` on blocks > 0 perturbs the forward output by > `1e-3`.
- **No silent HP drift.** `git diff models/layers.py configs/llm_config.py models/llm.py` is empty (changes already committed in `c6bb6a3` alongside 022/023/024/025; the lever surface in those files — `use_value_residual` flag, MHA/TB kwarg, model.forward `v_residual` thread, `Tiny1M3MVResidualOnFireConfig` — is what `plan.md` promised). Only the new `use_value_residual: bool = False` field is added to `LLMConfig`; no LR / schedule / init / seed constants touched. Single boolean flag, default OFF.
- **Trt config materializes the correct A/B.** `Tiny1M3MVResidualOnFireConfig(Tiny1M3MConfig)` at `configs/llm_config.py:974-1012` sets `use_fire_pe: bool = True, use_value_residual: bool = True`. The ctrl is `Tiny1M3MConfig + use_fire_pe=True` — the actual 009 WIN config (per `closed.md:44` and `009-fire-pe/evidence.md`: trt 6.3234 vs bare-ctrl pair 6.3875/6.4050). The A/B isolates `use_value_residual` on top of FIRE, partitioning the orthogonality question. Mirrors the established `Tiny1M3MFOXOnFireConfig` (020) / `Tiny1M3MGatedAttnOnFireConfig` (024) shape — same parent (`Tiny1M3MConfig`), same flag-stack pattern. Seed 42, single tier (tiny1m3m), no sweep. `idea.md`'s text incorrectly cites `Tiny1M3MVQGainSWAHighRoPE250KConfig` as the 009 WIN parent, but the implemented config matches the actual ctrl that 009 ran against (bare `Tiny1M3MConfig + use_fire_pe=True`) — the text mis-cite is a definition-gate drift, not a code-gate issue (the code is faithful to what was actually run); the established 020 / 024 precedent uses the same pattern.
- **Layer-tying interaction is correct.** `MinimalLLM.forward` (`models/llm.py:607-623`) uses the **forward-pass index** `i == 0` to gate the stash readback (`v_residual = block.attention._v_residual`), not the unique-block index — so with `tie_layer_groups > 1` the position-0 block stashes once and subsequent re-uses of the same physical block take the blend branch (`v_residual is not None` ⇒ skip the stash branch). The default `tie_layer_groups = 1` (`Tiny1M3MConfig` inherits the default) leaves this moot for the live A/B but the general path is sound.
- **SDPA fast path is preserved off-FIRE.** The blend happens at `models/layers.py:1522` BEFORE the manual-vs-SDPA branch split — so if someone runs `use_value_residual=True` without `use_fire_pe=True`, V is blended first and then fed into the SDPA flash kernel as-is (no extra forcing of the manual path needed). The trt config forces FIRE, so the manual path is taken; either way the blend is correctly placed pre-attention.
- **Test suite covers the four invariants.** `tests/test_value_residual.py` (~140 LoC): (1) flag-off determinism + `lambda_v` absence, (2) flag-on step-0 identity ≤ 1e-5, (3) wiring-live with `λ = 0.5` perturbs output > 1e-3, (4) V_1 stash shape `[B, n_heads, T, d_k]` + `grad_fn is None` (detach verified). `pytest tests/test_value_residual.py -v` → 4/4 passed in 5.19s.
- **LoC budget respected.** Real diff (code + comments) is ~60 LoC across `models/layers.py` (~40), `models/llm.py` (~15), `configs/llm_config.py` (~50, mostly the config-class docstring), `configs/__init__.py` (~2), `tests/test_value_residual.py` (~140). Production code (excluding tests) is ~110 LoC including all the inline docstrings — under the 200 LoC ceiling and within tolerance of the plan's ~36 LoC estimate (the implementer's count omitted comment-line LoC; the spec-promised `if self.use_value_residual: ...` guards are the LoC that actually matter and they're ~12 lines total).
- **Coordination clean.** No conflict — the 022/023/024/025 patches in `c6bb6a3` interleave with 021 cleanly: softpick/SSMax additions live above the V-transpose line, gated-attn's `gate_x` kwarg sits before `v_residual` in MHA.forward signature, canon-conv reads/writes the residual stream BEFORE the attention sublayer's pre-LN while v_residual is plumbed THROUGH the attention sublayer. No push.
- **No findings.** Code is correct, faithful to the spec, identity-safe at λ=0, gradient-live for blocks > 0, and ready to run.
