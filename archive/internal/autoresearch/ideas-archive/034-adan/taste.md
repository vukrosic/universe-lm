# Taste log — 034 Adan

## r2 — 2026-06-11 — verdict: accept
- **All r1 blockers closed.** Slot named: option (c) full optimizer swap — Adan on 2D, AdamW 1D unchanged, Muon retired (the most decisive of the three r1 options, and the one that makes a result interpretable). EMA defended: β3=0.95 (~14-step half-life, shortened from 0.99's ~70) is explicitly sized to the 92-step run, and the AdEMAMix 018 failure mode is named and routed around. Crisp bet: Δ −0.01 to −0.03 vs Muon-only ctrl, Cautious-Lion 011 bracket-band.
- **Information value is symmetric.** WIN dethrones a WIN'd 015-Moonlight-Muon-RMS slot — pipeline-changing. NULL combines with 001-cautious-muon / 002-cautious-adamw / 006-schedule-free-adamw to give *three* optimizer-momentum NULLs that together close "Nesterov-style differencing on the matrix-weight path" as a direction at our tier. Either outcome is a structural conclusion, not a wash.
- **Portfolio earns it.** The 031-040 optimizer wave is crowded, but per r1's own concession Adan is the most distinct mechanism in the queue (Nesterov gradient differencing vs Adam-mini's per-block grouping, Sophia's Hessian probe, AGC's grad-norm clip, Lookahead's slow-weights). One Adan slot survives the diversity test.
- **Niche fit clean.** Mechanism (optimizer rule), scale-invariant by construction, β-tuned to fire by step ~30 of a 92-step run, transfer-risk: low (paper validated GPT-2 1.5B + ViT/MAE 100M+). Defines a sharp two-ctrl test on a WIN'd surface.
- **Out of scope for this gate (note for definition gate, not a taste objection):** the −0.01 lower bound is close to the prior two-ctrl gap (0.004-0.018 in `closed.md`) — definition gate should pin the pass bar with the right bracket and confirm the Muon-only ctrl matches the `Tiny1M3MConfig` recipe currently on the leaderboard.

## r1 — 2026-06-11 — verdict: revise
- **Slot ambiguity (blocker).** Idea says "replacement for AdamW on the matrix-weight path." In this repo Muon owns the matrix-weight path (Moonlight-Muon-RMS 015 was a WIN; cautious-muon 001, decoupled-qkv-muon 005 already mapped that surface); AdamW is on 1D/embed params. The pitch as written is undefined for our codebase. Pick one: (a) Adan replaces **Muon** on 2D weights — major bet, must justify dethroning a WIN'd optimizer; (b) Adan replaces **AdamW** on 1D/embed params only — but then ~vocab embeds dominate at 0.94M and the lever is mostly bypassed (the 003-SOAP failure mode); (c) Adan on 2D, AdamW on 1D, Muon retired — full optimizer swap. Without the slot named, a win/null isn't interpretable.
- **EMA half-life vs run length — the AdEMAMix problem.** tiny1m3m runs ~92 steps. Adan's default β3≈0.99 (second moment) has a ~70-step half-life — borderline; β2≈0.92 (grad-diff momentum) is ~12-step half-life — fine. 018-AdEMAMix was taste-rejected on exactly this axis ("slow EMA half-life ~7k steps vs ~92-step run; 99% init-weighted"). The idea must explicitly argue why Adan's defaults don't replicate that failure pattern, or propose tuned (β1,β2,β3) sized to 92 steps. As written, the risk that NULL means "moments didn't warm up" rather than "Nesterov differencing doesn't help" is uncontrolled — which kills the information value.
- **Crisp bet missing.** "tells us whether short-horizon gradient dynamics want Nesterov-style differencing" is a vibe, not a bet. State an expected Δ band (e.g. −0.01 to −0.03 like Cautious-Lion 011) and the *specific* mechanism a NULL rules out — phrased so the result is informative regardless of sign. "What does a NULL teach beyond what 001/002/006 NULL already taught?" must have a one-sentence answer.
- **Portfolio context (FYI, not a rejection axis on its own).** Optimizer family is at saturation: 7 done (5 NULL, 2 WIN), 1 running (027), and 031/032/033/035/036/037/038/039/040 all queued at needs-taste behind this. Per the prompt, "5th optimizer-momentum variant in a row" is a `revise` cue — there's room to keep one well-specified Adan, but only one. Nesterov differencing IS the most distinct mechanism in the 031-040 queue (vs Adam-mini's per-block grouping, Sophia's Hessian probe, AGC's grad-norm clip), so sharpened it earns a slot. As written it doesn't.
- **What a clean re-pitch looks like.** "Adan replaces Muon on 2D weights, AdamW unchanged on 1D/embed. β1=0.98, β2=0.92, β3=0.95 (shortened from 0.99 for ~14-step half-life, sized to our 92-step run). Bet: −0.01 to −0.03 val vs Muon-only ctrl. A NULL with these warmed moments rules out 'Nesterov differencing helps at our run length' independent of EMA warm-up." That's the version that earns the slot.
