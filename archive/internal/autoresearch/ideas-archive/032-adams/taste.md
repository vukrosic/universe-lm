# Taste log — 032 AdamS

## r2 — 2026-06-11 — verdict: accept
- **r1 findings closed cleanly:** (1) crisp bet now stated as Δval ≈ −0.005..−0.012 vs the recent ctrl-pair gap 0.0047 (top of range is clearly above noise; bottom skirts it — acceptable for a one-shot A/B); (2) explicit mechanism-axis differentiation from 031 (granularity), 033 (curvature), 034 (Nesterov-momentum), 040 (factorization), 002 (sign-mask) — each on a distinct lever; (3) honest "memory ÷ 2 is moot at 0.94M" caveat carried into Scale-evidence and Why-it's-worth-a-slot. No re-litigation needed.
- **Mechanism is real and tiny-budget-relevant:** "non-degenerate denominator from step 1 (no cold v_t warmup)" is a sharp hypothesis for *why* AdamS could win specifically at our 92-step / 3M-token budget where bias correction has the fewest steps to bite. This is the right kind of mechanism for tiny1m3m — scale-free in argument, scale-specific in payoff window.
- **Null is informative:** cleanly separates "momentum normalization is genuinely a better denominator" from "any well-scaled denominator matches AdamW." The second outcome closes the lever for our ladder (memory-bound is not our regime) without being info-free — it tells us not to mine further `v`-replacement ideas at this scale.
- **Portfolio crowding addressed:** of the 5 optimizer ideas flagged in r1, 040 has since been taste-rejected (scale-up tool) and 036-LAMB rejected (large-batch). 031 is at `needs-repitch`, 033/034 still `needs-taste` — AdamS is on a clearly distinct axis (drops `v` entirely vs 031's granularity / 033's curvature / 034's Nesterov-`m`). The "remove second-moment buffer" thesis has no in-queue or closed sibling.
- **Track-record honesty:** matrix-weight optimizer wins at tiny1m3m so far are 011-cautious-lion + 015-moonlight-muon; AdamW-family tweaks have been NULL (002, 006). AdamS is genuinely a different mechanism from cautious sign-mask and SF-AdamW iterate-averaging, so the prior null streak doesn't bind. The bet is risky but the mechanism is non-derivative — fair odds.
- **Transfer to 135M:** "no-cold-start normalization" argument is scale-free and the paper's 13B parity adds independent evidence that AdamS doesn't blow up at scale. A win at tiny1m3m would port cleanly into the 135M recipe.
- Verdict: **accept** → `needs-review`. Round resets to 1 for the definition gate's budget.

## r1 — 2026-06-10 — verdict: revise
- **Leverage gap**: the AdamS pitch ("momentum alone can stand in for Adam-style variance tracking") is a *parity* bet, not a leverage bet. The paper's selling point is "competitive with AdamW *plus* memory savings." At tiny1m3m the second-moment state is a rounding-error line item — there is no memory win to harvest, so the slot can only be earned on val loss. State the predicted `Δval` and the variance bar that distinguishes a real win from noise (cite a recent ctrl-pair gap, e.g. 0.0047–0.0175 from 015/009).
- **Portfolio saturation**: the active queue already holds **four other optimizer variants** at `needs-taste` (031 adam-mini, 033 sophia, 034 adan, 040 adafactor). Closed optimizer experiments at this scale: 002/003/005/006/001 = 5 NULL, 015/016 = 2 WIN, 018 = taste-reject (slow EMA half-life vs run length). Win rate ~25% and most "novel" tweaks are null. To pass taste here, AdamS must differentiate its mechanism from those four and argue why it would be the *first* to clear the bar.
- **Mechanism framing**: the lever is "weighted sum of squared momentum + current gradient under a square-root" replacing `sqrt(v)`. That is a normalization change *internal* to AdamW, not an architecture change. Be specific: how does the "momentum magnitude as normalizer" differ from sign-mask / cautious-AdamW (002) or blockwise LR (031)? If the only difference is "another normalization, slightly different math", the bar is higher, not lower.
- **Scale transfer**: paper shows 13B parity, so transfer-risk: low is plausible, but the "leverage" at 13B is also *parity* (the paper's headline is memory ÷ 2). Carry-claim toward 135M must be a *val-loss* claim, not a memory claim — the 135M recipe doesn't have a memory bottleneck either. Reframe as: "we expect Δval ≥ -0.01 at tiny1m3m because [momentum normalization reduces early-step noise under the 0.94M / 3M-token budget]."
- **Crisp bet missing**: the "Why it's worth a slot" paragraph is a *test description*, not a *bet*. Rewrite as: "we expect Δval = X (with Y mechanism reasoning) — a null closes the lever because [Z], a win ports to 135M because [W]."
- Verdict: **revise** → `needs-repitch`. The miner should return with: (1) a sharp one-sentence bet with predicted Δval + reasoning, (2) explicit differentiation from 031/033/034/040, (3) honest framing that memory savings are moot at 0.94M so the bet stands or falls on val loss.
