## r2 — 2026-06-09 — verdict: approve
- **All 5 r1 findings are correctly applied.** (1) `b_f^h = +10` (was +5) — math in `idea.md:42-48` is right: `sigmoid(10) ≈ 0.99995`, `log f ≈ -4.54e-5`, `D[0,2047] = exp(-0.0929) ≈ 0.91` (reviser wrote 0.90, within rounding; ~9% worst-case decay over the full context, well below the +5 init's `~1e-6`). The r1 BLOCKING on init is resolved. (2) Ctrl pinned to `Tiny1M3MVQGainSWAHighRoPE250KConfig + use_fire_pe=True` (line 79-84), with the 009 WIN signature `6.3234 vs 6.3875/6.4050` cited from `closed.md:40` as the FIRE baseline. Trt is same + `use_fox=True` (line 85). A/B now partitions the orthogonal axis (mass control on top of additive logit bias), not "does FIRE win". (3) Pass bar at `idea.md:88-96`: Win `trt < ctrl − 0.02`, Null `|trt − ctrl| < 0.02`, Fail `trt > ctrl + 0.01` — clears the 0.018 ctrl-gap spread and is tight enough to beat the ±0.01 noise floor. (4) Seed 42 only (line 98-101), explicit "never add seeds to confirm" — matches the prime directive. (5) LoC budget ≈ 47/200, broken into a/b/c/d/e pieces (line 103-115). Plan can proceed.
- **Source re-checked.** arXiv:2503.02130, "Forgetting Transformer: Softmax Attention with a Forget Gate", Lin, Wang, Yang et al., March 2025 — matches the r1 cite; mechanism description is the standard one from that paper, not a fabricated hybrid. ✓
- **Closed-list cross-check.** `closed.md` has no FoX / per-head learnable decay / multiplicative-on-probabilities entry. Closed axes are score-function (NSA/diff-attn), mask (SWA/dilated), position-encoding (RoPE/NoPE), logit-cap (softcap), and softmax-replacement (008/012 delta-net / linear). FoX is a *conservative extension* of softmax — strictly orthogonal. ✓
- **Falsifiable, distinct, ready to spec.** Mechanism is a structural, parameter-sharing, per-head per-token *learned* decay (not an LR / init-constant lever). The bet is crisp: *FIRE wins on additive logit bias; FoX would win on multiplicative mass control — orthogonal axes; null partitions "does mass control matter on top of FIRE at tiny1m3m?"* Informative either way. Ctrl-vs-trt is single-line (one boolean flag), and the 47-LoC budget includes the wiring + the step-0 identity assertion.
- **(NIT, non-blocking) Step-0 test tolerance in (e) may be too tight.** The plan pins "output within `1e-5` of `use_fox=False` at step 0" (line 55-56). With `W_f=0, b_f=+10`, `D_h[i,j] = exp((i-j)·log f)` is a smooth causal decay ranging 1.0 → 0.91 across a 2048-length row. For a freshly-initialized model with roughly-uniform `softmax(QK^T/√d)` (random Q,K), the row-renorm cancels most of the D perturbation, so the test *probably* passes — but the 1e-5 tolerance leaves no slack for init-shape edge cases. The implementer should sanity-check the actual delta and pick a tolerance that is tight-but-not-flaky (e.g. `1e-2` is the obvious ceiling; `1e-4` is more defensible). The mechanism correctness does not depend on the specific number; flag for the code-implementer to pick what survives a smoke run.

## r1 — 2026-06-09 — verdict: revise
- **Mechanism is sound, distinct, and falsifiable.** `f_h,t = sigmoid(W_f^h · x_t + b_f^h)`; `D_h[i,j] = exp(cumsum(log f)[i] − cumsum(log f)[j])`; `A_h = softmax(QK^T/√d) ⊙ D_h`, row-renorm. This is the Forgetting Transformer (FoX, Lin et al. arXiv:2503.02130, March 2025). The mechanism is a *conservative extension* of softmax attention — softmax stays, projection stays, V path is unchanged; only an elementwise multiplicative decay kernel is applied to A. Strictly orthogonal to every closed attention lever: NSA / diff-attn (score-function change), SWA / dilated (mask), RoPE / NoPE (position encoding), logit softcap (logit range), delta-net / linear-attention (replaces softmax). The 009 WIN (FIRE, additive on logits) and FoX (multiplicative on probs) are on opposite sides of softmax — orthogonal mass-control axis. Distinct from `models/retention.py` (RetNet retention kernel, **linear** attention — softmax is replaced, not preserved). The `closed.md` norm-zoo, attention-modifications, and 008/012 entries do not subsume this. ✓
- **(BLOCKING) Identity-init claim is numerically wrong at T=2048.** The idea says *"b_f^h = +5 → f = 0.993, log f ≈ −0.007, so D_h[i,j] ≈ 1 everywhere at step 0."* At `seq_len = 2048` (`configs/llm_config.py:25`), `D[0, 2047] = exp(−0.0069 · 2047) = exp(−14) ≈ 8e-7` — i.e. a token starting at position 0 *cannot* attend to the last token at all under this init. The "≈ 1 everywhere" claim only holds for `T ≲ 100`. To get true near-identity at T=2048, `b_f ≳ +10` (sigmoid(10) ≈ 0.99995, log f ≈ −5e-5, D[0,2047] = exp(−0.1) ≈ 0.90). Fix: pick `b_f = +10` (or numerically solve for `log f · 2047 = −0.1`, i.e. `b_f ≈ 10`), and either drop or replace the miner's `b_f = +5` with the correct value, OR keep `b_f = +5` and add an explicit step-1 vs step-1000 loss recovery check (mirroring the DyT init caveat at 019). The bet's framing — *"the model has to learn to forget from scratch, attention is numerically identical to baseline at init"* — only works with the corrected init. The miner's number is wrong; revise, not reject, because the mechanism is fine and the fix is a one-line change in `idea.md` / plan.
- **(BLOCKING) Ctrl must be pinned to the FIRE-equipped baseline.** Taste.md correctly flagged: *"A/B must run on 009 FIRE in the baseline (per the miner's framing) so the result partitions the orthogonal-axis question. If run against the non-FIRE ctrl, the effect will be conflated with FIRE's win and uninterpretable."* Without this pin, the result is unreadable: a "win" could be FoX, or FoX-on-top-of-FIRE, or something else. Plan must run `Tiny1M3MVQGainSWAHighRoPE250KConfig + use_fire_pe=True` as ctrl and same + `use_fox=True` as trt. Cite the 009 WIN line in `closed.md:39` (Δ −0.064/−0.082) as the FIRE baseline signature.
- **(REQUIRED) Pass bar at tiny1m3m noise floor.** Noise at this scale ≈ ±0.01 val_loss (closed.md run deltas on the FIRE ctrls span 6.3875 → 6.4050 = 0.018). The bet is "wins on top of FIRE." Pass bar should be `trt < ctrl − 0.02` to clear ctrl-gap; null zone is `|trt − ctrl| < 0.02`; fail is `trt > ctrl + 0.01`. Spell this out in `plan.md`.
- **(REQUIRED) Seed 42 only.** Plan must use a single fixed seed (42), no multi-seed sweep, no per-seed mean. The prompt's seed protocol is non-negotiable.
- **(NIT) LoC accounting.** Plan should commit a one-line LoC budget for each of: (a) the per-head gate `nn.Linear(d_model, H)` (≈ 12 LoC), (b) the cumsum path + elementwise multiply on A (≈ 8 LoC), (c) the row-renorm (≈ 4 LoC), (d) flag wiring + config plumbing (≈ 8 LoC), (e) one test asserting `use_fox=False` ≡ baseline at step 0 (≈ 15 LoC). Total ≤ 50 LoC, well under the 200 LoC ceiling.
