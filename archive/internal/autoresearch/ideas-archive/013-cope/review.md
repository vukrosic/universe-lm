## r2 — 2026-06-09 — verdict: approve
- All r1 findings resolved. PASS ≤ −0.01 (was ≤ −0.005), explicit NULL/INCONCLUSIVE band |Δ| < 0.01, τ pinned at 0 with sweep language removed, probe init pinned to `N(0, 0.02)` mirroring `models/fire_pe.py:54`, and the ctrl is now the FIRE-equipped baseline (6.3234) not the V+q+SWA+HighRoPE reference. The RoPE call-site audit names 7 real sites and is consistent with the actual grep output of `models/layers.py` and `models/llm.py` (lines 12/20/548 in layers.py; 207/217/210-213/322/346/347 in llm.py all match).
- LoC bumped from ~40 to ~50 to account for the multi-site bypass; still well under the 200 LoC cap. Identity-safety holds (probe near 0 → near-uniform positions → RoPE-like at step 0), tiny1m3m scope is explicit, mechanism is structural (content-conditional position encoding), not HP tuning. arXiv:2405.18719 (Golovneva et al., Meta, 2024) is the correct CoPE citation — distinct from the mis-cited 014-sigmoid-loss closure.
- A/B is "FIRE + CoPE" vs "FIRE alone" — a stacked lever over the current best baseline, not a duplicate of 009. No collision with `closed.md` (FIRE, RoPE sweeps, sigmoid-loss, gated-deltanet, etc.). Plan is spec-ready; handing to the code gate.
- **Round reset to 1** for the code gate per reviewer protocol.

## r1 — 2026-06-09 — verdict: revise

- **Pass/fail bar sits inside the noise floor.** Hypothesis Δ in [−0.005, −0.02], tiny1m3m run-to-run variance is ~±0.01 (per reviewer protocol — same ballpark as the 0.0175 ctrl-pair gap that has decided prior 001/002/005/004 closures). PASS ≤ −0.005 would call a sub-noise effect a win. The taste gate already flagged this — tighten PASS to ≤ −0.01 (upper half of the hypothesis range) so a real win has to clear noise, and add an explicit NULL/INCONCLUSIVE band at |Δ| < 0.01.
- **One-seed-only rule forbids the τ sweep.** Notes say "small sweep over τ in {−1, 0, +1}" as a secondary axis. Per pipeline rule (one seed only, seed 42), no sweeps. Pin τ to 0 (midpoint sigmoid) for the primary A/B. If primary lands, a follow-up idea can probe one alternative τ value as a single-experiment secondary. The plan must not promise a sweep.
- **Probe init must be concrete.** Notes say "small random" without scale. Pin to a specific init in the plan (mirror the FIRE PE init pattern in `models/fire_pe.py`, or pick `p ~ N(0, 0.02)` and justify) so the A/B is reproducible from the plan alone with no implementer judgement call.
- **Replace-RoPE call-site audit.** CoPE replaces RoPE; `models/layers.py` and `models/llm.py` both reference RoPE. The plan should name every call-site that has to be bypassed when `use_cope=True` (rotary cache, q/k application, any post-rotation scaling). If more than one site needs editing, push the LoC estimate up from ~40 and confirm it still fits under 200.
- **Drop the "FIRE result not yet in" dependency.** Plan line "if 009's result lands first, this idea should reference 009's evidence" — 009 already closed as WIN in `closed.md` (−0.064/−0.082). The CoPE plan should anchor against the FIRE-equipped ctrl (the current best baseline, not V+q+SWA+HighRoPE), so the A/B measures "CoPE vs best" not "CoPE vs last week's best." Update the ctrl reference.

Notes that survived my pass: arXiv:2405.18719 (Golovneva et al., Meta, 2024) is a real and verifiable CoPE citation (the same ID was misused by closed 014-sigmoid-loss — the citation here is correct), mechanism is a real content-conditional position encoding (not HP tuning), orthogonal to closed FIRE PE and closed RoPE sweeps, identity-safe at step 0 (probe near 0 → near-uniform positions).
