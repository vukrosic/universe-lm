## r2 — 2026-06-11 — verdict: accept
- **All 5 r1 findings closed substantively.** The 4 substantive concerns from r1 (backwards scale argument, no Δ prediction, missing param/init story, 023-canon crowding) are addressed head-on in the r2 body, and the citation got a stable URL. The pitch is now the tightest of the post-023 generation.
- **L=6 mechanism argument is honest, not handwaved.** Point 3 is the load-bearing one: at L=64 each block's routing choice is one of 64 summands (low leverage per decision); at L=6 it's one of 6 (high leverage per decision). The lever is *stronger per-decision* at L=6, even if total capacity is smaller. Combined with point 1 (per-block interpolation has 4 free parameters that can move away from identity) and point 2 (the routing primitive scales O(L), not O(L²) or with width), this is the first time the r1 "does it fire at L=6" question has been answered with mechanism rather than authority. Null hypothesis is sharp: "uniform sum is already optimum at L=6 with FIRE+Canon." That's a falsifiable, informative-null bet.
- **023-canon differentiation is now a real contrast, not vibes.** Five orthogonal axes laid out cleanly: token-axis vs depth-axis, per-block vs across-blocks, sublayer vs replacement, additive capacity, orthogonal stacking. Crucially the pitch pre-empts the "stacks redundantly" worry by naming what AttnRes *adds* beyond Canon (a free routing decision per block on the depth axis). The 023-canon WIN (Δ -0.06 isolated) and an AttnRes win (Δ -0.02 expected) would compose at the 135M recipe rather than fight for the same slot — that's exactly the portfolio-fit shape we want.
- **Identity-init story is clean enough to kill the wiring-artifact question.** b_id=10, α_identity ≈ 0.9995, ≤5 other keys sharing ~0.0005 mass, ~30 trainable floats across the entire model. Step-0 ≡ baseline within ~1e-3 (b_id=20 → <1e-9); assertion in unit test (e) mirrors 023-canon's gate-of-zeros. The pitch's "lever can only help or stay neutral, never hurt" claim is now defensible — the bet's `regression impossible` clause is not vibes, it's a property of the init.
- **Sharp Δ bet.** `Δ ∈ [−0.005, −0.02] if depth-routing non-redundant with Canon, null `|Δ| ≤ 0.005` if L=6 too shallow, regression impossible by identity-init`. The win/null/regression trichotomy matches the run-bar grammar and either outcome updates a real belief: win = structural depth-routing lever worth carrying to 135M, null = uniform-residual-sum is locally optimal at L=6 (constrains future depth-axis lever design). Both are above the taste bar of "clean null still worth logging."
- **Niche fit strong.** Mechanism (per-block routing softmax), not HP. Identity-init-able. Should fire at tiny1m3m if anywhere. Sub-50-LoC. Paper's 125M ablation gives a transfer-down argument (not transfer-up), which is the right direction for the taste gate.
- **GPU has been idle 36+ hours** (queue.md: "GPU IDLE since 2026-06-10T11:27Z"). 16 ideas currently in `needs-taste` and the pipeline needs accept verdicts to feed the runner. This is a clean accept, not a "we need to ship something" accept — the r2 pitch is genuinely tight.

**One concern to flag at definition (not taste):** `arXiv:2603.15031` (March 2026) is still novel. r2 added the stable URL, which is the right move. If the definition gate cannot resolve the Kimi AttnRes paper from the URL, the idea dies there — not taste's call. Tasted-but-not-blocking.

**ACCEPT.** Move to definition. Round reset to 1.

---

## r1 — 2026-06-11 — verdict: revise
- **Backwards scale argument.** The pitch's `transfer-risk: low` rests on "demonstrated at 48B-total / 3B-active over 1.4T tokens" — that's a *transfer-up* argument. For the taste gate the question is the inverse: does softmax-over-depth routing have anywhere to **fire** at our `tiny1m3m` (6 layers, ~92 steps)? At depth-6 the per-block routing softmax sees ≤5 keys and learnable logits over 5 candidates may be indistinguishable from the trivial identity solution. Re-pitch must explain mechanistically why 6 layers is enough for the lever to express itself, not why a 48B model benefits.
- **Crowded-family check: 023-canon-conv already won here.** Canon (depthwise causal Conv1d on the residual stream) closed `WIN w/ caveat: trt=6.2581 vs 6.3419 (Δ -0.084)` — same spiritual family ("break the uniform residual"). AttnRes is *not* a duplicate (Canon acts within-block on tokens; AttnRes acts across-blocks on depth) but the pitch must explicitly differentiate: what does softmax-over-depth test that Canon's residual-stream conv does not, and why would stacking them later be additive instead of redundant? Without this contrast the idea reads as "another residual-stream lever" and loses portfolio fit.
- **No crisp Δ prediction.** "Isolates whether uniform residual is wasting capacity" is information-value framing, not a bet. Per the taste prompt, a sharp bet has the form "we expect Δ -0.0X because Y mechanism". Name an expected sign and magnitude at tiny1m3m (e.g., `−0.005 to −0.02 if depth-routing is non-redundant with the 023-canon mechanism`, or `null with bar −0.005 if 6L is too shallow`). The miner's job at re-pitch is to make even a null verdict force a real update.
- **Param/init story missing.** With 6 blocks, AttnRes adds O(L²) routing logits per block (~30 floats total) — trivially cheap. Pitch should state this and the init recipe (the idea body says "logits initialized to favor the current residual path at step 0" — good, but call out *how* this is identity-init so step-0 model = baseline, otherwise this becomes a code-loop debate, not a taste one).
- **Citation will get checked at definition, not taste — but flag:** `arXiv:2603.15031` parses as "March 2026, paper 15031". Plausible but novel; if the next gate cannot resolve the Kimi AttnRes paper, the idea dies there. Re-pitch could pre-empt by adding a stable URL alongside the arXiv ID.

**Closing the gap (concrete re-pitch checklist):**
1. Mechanistic argument for why softmax-over-depth fires at L=6 (not why it scales to L=64).
2. One sentence contrasting AttnRes vs 023-canon-conv — what new degree of freedom does it add?
3. Sharp Δ prediction at tiny1m3m with bar.
4. Identity-init confirmation (step-0 = baseline, so a null isn't a wiring artifact).
