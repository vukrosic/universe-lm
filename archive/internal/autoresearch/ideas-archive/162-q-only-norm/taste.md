# Taste log — 162 q-only-norm

## r1 — 2026-06-14 — verdict: accept
- **Leverage**: 016-qk-norm was our strongest QK-axis WIN (Δ=-0.014 vs both ctrls, pass-bar -0.005 cleared ~3×). Q-only is the natural magnitude probe — plausibly ~half of 016's gain if Q-side dominates, null if K-side/symmetry was carrying the WIN. Either magnitude is detectable at tiny1m3m against the ±0.04 noise band.
- **Information value**: Sharp orthogonal ablation. WIN → "Q-side normalization is the binding axis" → future Q-side levers (lowrank-refine, softplus-gain on Q) are worth filing. NULL → 016's WIN came from K-side or the QK symmetry → close the Q-side direction. Both outcomes close the QK-norm-attribution axis at 0.94M and inform the rest of the QK portfolio.
- **Non-obviousness / novelty**: Real design tension (Cohere Command-R uses asymmetric QK at 35B+, Gemma 2 discusses the trade-off). Cross-checked closed axes: 152 (post-softmax per-head logit bias) and 155 (per-head temperature) are post-softmax interventions; 162 is pre-softmax Q-side only — distinct axis. 016 is the only symmetric QK WIN. Not a re-pitch.
- **Portfolio fit**: QK family is sparse — 016 is the sole WIN, 152/155 closed null. Filing Q-only now (alongside the V-mix / K-only neighbors) is exactly the orthogonal 3-axis test the portfolio needs; not crowded.
- **Niche fit**: Mechanism, identity-init-able (RMSNorm's standard init rescaling is acknowledged and accepted via the 159-emb-layernorm precedent / fp32 max-abs-diff < 1e-3 tolerance). ~6 LoC in `models/layers.py`, fits tiny1m3m. Config flag defaults off, so zero cost to non-toggled runs.
- **Crisp bet**: "Q-only RMSNorm should produce ~half of 016's gain if Q-side is dominant, or null if K-side or symmetry is the binding axis." One sharp sentence — Q controls what each token *asks for*, K controls what it *offers*; the ablation picks between them.
- **Transfer**: low (RMSNorm family production-validated at LLaMA 3 / Qwen 2.5 / Mistral 1B+, Cohere validates asymmetric QK at 35B+). No scale-only-above-our-tier concern.
- **Forward flags (non-blocking, for the definition gate)**: step-0 byte-identity relies on the `nn.RMSNorm` rescaling landing inside the fp32 tolerance — definition gate should verify this on the actual block at the chosen config class; reference to "943-softplus-gain / 938-lowrank-refine" in the pitch is forward-looking and out of scope for this round.