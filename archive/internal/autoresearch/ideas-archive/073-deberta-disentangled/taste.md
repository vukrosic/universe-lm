# Taste log — 073 DeBERTa Disentangled Attention

## r2 — 2026-06-11 — verdict: revise
- **Procedural note.** taste.md r1 below was a verdict but the flip never executed (no `log.jsonl`, `idea.md` `updated:` unchanged since initial mining). Idea content is identical to what r1 reviewed — r1 findings stand verbatim; this round only adds the one external fact that has changed since r1 and tightens the next-pitch ask.
- **072 is no longer a sibling — it's ahead.** Since r1 was written, **072-t5-rpe has cleared the taste gate and now sits at `needs-review`** (same learned-relative-distance-bias family). r1.(b) said "pick ONE of {072, 073}"; the pipeline has now effectively picked 072. The "differentiate sharply" requirement is no longer optional advice — it is the only path to survival. If 073 cannot beat 072 to a *different* mechanism question, it is a near-dup and `taste-reject` is the right close.
- **The one binding ask for the re-pitch.** Answer this in one mechanistic sentence: *what does DeBERTa's content-position cross-term measurably do that 072's content-agnostic bucketed bias cannot?* Concrete examples of what would count: (a) "head-and-content-dependent gating on which distances matter — e.g. punctuation tokens attend to distance-1 strongly, content tokens diffuse" — and a way to read that off val loss vs 072; or (b) "the position branch lets the model route attention by content-conditional decay, which a content-blind bias cannot, so 073−072 ≈ X on tokens whose POS bucket differs". A vibe like "richer interaction" is *not* an answer.
- **Other r1 gaps still owed.** The re-pitch must also (i) commit to *replace FIRE* (not stack — 013-cope showed stacking is destructive), (ii) state the cold-start story for the position Q/K projections firing in <100 steps with 3M tokens, (iii) name the param overhead (~2× attention-projection params per layer) and the repayment story at 0.94M total, (iv) state up-front what a null vs 072 teaches (e.g. "content-position cross-terms don't pay at our token budget" — and that goes in `closed.md`).
- **r3 is forced.** Round 3 will be `accept` or `reject` — no further revise. The bar at r3: a one-sentence differentiation from 072 that names a mechanism, not a vibe, and credible execution of (i)–(iv) above. Anything weaker → `taste-reject` as PE-family near-dup with 072 already advanced.

## r1 — 2026-06-11 — verdict: revise
- **Bet is not framed against the actual baseline.** The "Why it's worth a slot" says "cleanest test of explicit content-position disentangling vs a single relative bias term" — but the current pipeline best is **009-fire-pe (WIN, Δ -0.064/-0.082)**, which already *is* a learned relative-distance prior. The pitch reads like "DeBERTa vs no-PE", not "DeBERTa vs FIRE". Re-pitch with one sharp sentence: "DeBERTa beats FIRE because content-position cross-term carries [X signal] that FIRE's content-agnostic bias cannot — measured by Y." Without that, this is a baseline-shaped bet in a niche where the baseline isn't baseline.
- **Portfolio is PE-saturated.** Active PE-family bets at this gate: 061-alibi-bias, 072-t5-rpe, 073-deberta (this one) — plus 009 already shipped as the WIN and 013-cope closed as destructive PE-stack. 5th PE bet in a row is dull regardless of individual merit; either differentiate sharply from 072 (also a learned relative-distance table) or accept it's the same family — pick one of 072/073, not both.
- **No mechanism for fast firing at tiny1m3m.** Position-branch params zero-init → step 0 == baseline is fine for safety, but the run is ~92 steps. DeBERTa's reported gains assume the position projections learn enough to contribute; the idea owes a one-line argument for why a cold-init position Q/K develops useful structure in <100 steps with 3M tokens, versus FIRE's already-decay-shaped kernel which fires immediately. Missing this turns a likely null into uninformative null (can't tell "wrong mechanism" from "needed more steps").
- **Parameter budget cost is unaccounted.** Three attention terms means content-Q/K **and** position-Q/K projections — roughly 2× the attention-projection params per layer. On 0.94M, that's not a rounding error; either the position branch shares projections (then it's no longer DeBERTa-shaped) or it eats budget the lever has to repay. Name the param overhead and the repayment story.
- **Info-value framing for a clean null.** State up-front what we *learn* if disentangling loses to FIRE at tiny1m3m: "content-position cross-term needs ≫92 steps" (close the lever) vs "FIRE's smooth kernel beats bucketed disentangle at this scale" (closes a sub-family). Without this, a null reads "inconclusive" not "informative".
- **Concrete way to sharpen:** (a) Specify whether 073 *replaces* FIRE or *stacks* on FIRE (013-cope showed stacking is destructive — pick replace, and own the fight against the WIN). (b) Pick ONE of {072-t5-rpe, 073-deberta-disentangled} to advance; the other is a near-dup taste-reject. (c) Add a sentence on the cold-init position branch learning fast enough in 92 steps. (d) Add the "what a null teaches us" sentence so the result is logged-worthy either way.

## r3 — 2026-06-11 — verdict: accept
- The mechanism difference vs 072 is now explicit and content-conditional.
- FIRE replacement, cold-start story, and null interpretation are pinned.
- The slot is now a clean head-to-head against 072 rather than a PE vibe.
