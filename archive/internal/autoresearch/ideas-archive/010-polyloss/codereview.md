# Code review log — 010 polyloss

## r1 — 2026-06-09 — verdict: accept
- **Mechanism fidelity ✓.** Diff at `training/trainer.py:433-453` (AMP) and `485-503` (non-AMP) implements the spec's pinned math exactly: `L = L_CE + ε₁·(1 - p_t)` with `ε₁=1.0`. Source: `flat_logits.softmax(-1).gather(-1, safe_labels.unsqueeze(-1))` returns the model's gold-class probability; the mask-multiplied `(1 - p_t)` term is the j=1 Taylor correction. No off-by-one, no logit-softcap stub, no LabelSmooth repurposing — the term is its own thing.
- **Mask parity with CE ✓.** `flat_labels = shift_labels.view(-1)` reuses the same `shift_labels = torch.full_like(y, -100); shift_labels[:, :-1] = y[:, 1:]` construction that the existing `F.cross_entropy(..., ignore_index=-100)` consumes at `training/trainer.py:409-414 / 461-466`. The PolyLoss term multiplies by `(flat_labels != -100).float()` so the last token of every sequence contributes exactly 0 to the loss *and* 0 to the gradient — the `clamp(min=0)` keeps the gather safe (`-100 → 0` is a valid vocab index, and the mask zeros it anyway). Smoke confirmed: all-`-100` labels → `poly_loss = 0.0` exactly.
- **Train-only reporting rule ✓.** `training/evaluation.py:53` is unchanged — plain `F.cross_entropy(shift_logits, shift_labels)` with no PolyLoss / no z_loss / no conf_penalty / no label_smooth. Val_loss is directly comparable to the leaderboard baseline (≈6.4287). Matches the train-only rule the spec called out at idea.md:30-33.
- **Flag wiring ✓.** `use_poly_loss: bool = False` default, `getattr(config, "use_poly_loss", False)` read; `poly_eps1: float = 1.0` default, `getattr(config, "poly_eps1", 1.0)` read; both gate on `>0.0` so `ε₁=0` is a clean no-op. The treatment config `Tiny1M3MPolyLossConfig(Tiny1M3MConfig)` at `configs/output_head_ablations.py:65-76` flips the flag to `True` and pins `poly_eps1=1.0`, mirroring the `Tiny1M3MOutputZLossConfig` shape. Placed in the anchors block (A8) right after A7 UntieHead.
- **Bit-identity at step-0 (flag OFF) ✓.** When `use_poly_loss=False`, the `else` branch sets `poly_loss = logits.new_zeros(())` and the loss sum becomes `(ce_loss + entropy_reg_loss + z_loss + conf_penalty + 0) / gradient_accumulation_steps`. The zero scalar is the only addition; no reordering, no new ops in the default branch. Smoke confirmed sum-with-flag-off equals sum-baseline.
- **Treatment path is not dead ✓.** `use_poly_loss=True` actually exercises the new code — smoke with random logits and V=49152 produced `poly_loss ≈ 1.0` (≈ `1 - 1/V` for uniform predictions, expected for an untrained model). The pbar/console log gets a new `pl:` / `PolyLoss:` tag at trainer.py:554 / 559 for visibility.
- **LoC budget ✓.** ~21 LoC in trainer.py (3-5 LoC x 2 paths × identical math + 2 detach/log lines + 1 pbar key + 1 console key) + 13 LoC dataclass + docstring in `configs/output_head_ablations.py`. 0 LoC in evaluation.py. Total ≈ 34 LoC, well under the 200 cap.
- **Plan ↔ idea consistency ✓.** plan.md pass/fail bar (PASS ≤ ctrl − 0.005, NULL = |Δ| < 0.005, DRIFT > ctrl + 0.01) matches idea.md:23-26 exactly. Both pin ε₁=1.0. Both call out train-only rule + -100 mask.
- **One seed ✓.** plan.md is seed-42-only, single tier `tiny1m3m`. No seed sweep, no multi-tier promotion ladder.
- **No silent HP drift ✓.** Diff is mechanism-only. No LR/schedule/init/seed constants were touched. The only new config knobs are `use_poly_loss: bool = False` and `poly_eps1: float = 1.0`, both getattr'd with safe defaults — existing configs (and the leaderboard baseline) are unaffected.
- **No coordination conflict ✓.** `git status` shows trainer.py and configs/output_head_ablations.py as the only shared files modified. Diff is local to the two loss blocks (lines 433-453 and 485-503) and the new dataclass (lines 65-76); no stomp on parallel-agent work.

**Decision**: accept. The implementation is faithful to the spec, identity-safe at step-0, train-only, and has a clean config wiring. No bugs found; no fixes requested. Ready for the runner.
