# Review log — 168-av-output-carry

## r1 — 2026-06-14 — verdict: approve

- **Source / dedup.** Source is real and well-anchored: 021-value-residual (closed WIN Δ -0.034) and 164-q-carry (in pipeline, r2) are the two adjacent cross-block carry axes the idea explicitly contrasts. 116-hyper-connections (closed null) and 150-xlayer-feedback (closed reject) are cited as the structurally heavier predecessors and the *reason* the post-AV axis is the smallest possible cross-block carry (no new Q/K/V, no full cross-attention). Distinct from the closed list: the closed "V/Q/K/O embeds + combos" are *intra-block* input-side projections; 168 is *cross-block* on the *post-AV* tensor. Not a duplicate.
- **Mechanism is a lever, not an HP tune.** Per-block scalar α_l (init 0, 12 params total, +0.001% of 0.94M) mixing the previous block's post-AV pre-W_O tensor into the current block's AV product before W_O. Identity/zero-init at step 0 ⇒ baseline forward graph on the flag-off path AND on the flag-on path with α=0. Categorically cheaper than 150 (no cross-block Q/K/V, no cross-attention) and cheaper than 021 (no per-block W_V(prev_x) — only an additive carry of the already-computed av_{l-1}). ✓
- **🔴 tiny1m3m / seed 42 only.** No mention of screen20m, full ladder, or any larger tier. ✓
- **< 200 LoC.** Plumbing follows the established `v_residual` / `q_carry` pattern: MHA.__init__ at models/layers.py:760 already takes `use_value_residual: bool = False`; the forward at models/layers.py:1911 already accepts stash kwargs (`v_residual`, `q_carry`); MinimalLLM's per-layer stateful forward at models/llm.py:1373-1389 already wires per-block carries. Adding `use_av_output_carry` is a parallel ~10-15 LoC change (one flag, one α_l Parameter per block, one stash kwarg on the MHA forward, one MinimalLLM stash/loop). ✓
- **Falsifiable bar (NUMERIC GAP — pin in plan.md).** idea.md doesn't state a specific Δ vs fresh ctrl. Reviser must add: **WIN if Δ ≤ -0.01** at tiny1m3m seed 42 against a fresh ctrl (cached-mean 6.4394±0.04 baseline cache at autoresearch/baseline-cache.json); **null if |Δ| < 0.01**; **DRIFT/hostile if Δ > +0.01** (any wrong-sign > noise is a strong null since 021's WIN was in the other direction). Per ONE-TIER-ONLY rule, no scale-up retry is possible — the bar must be tight enough to *resolve* at 92-step tiny1m3m (box noise ~±0.01 val loss).
- **`prev_av` definition — pin in plan.md.** idea.md says "the *post-attention output* of the previous block, post-AV, pre-W_O." That's unambiguous in the math but the implementer needs the *exact* tensor: stash from layer l-1's `av_{l-1} = SDPA(Q_{l-1}, K_{l-1}, V_{l-1})` (or manual-attention equivalent) **before** the W_O projection. Shape is `[B, T, d_model]` (the post-merge head output, not the per-head `[B, H, T, d_k]`). Pass to every layer l ≥ 1's MHA.forward as a new `av_carry` kwarg. Layer 0 has no previous block → carry term is exactly 0 (stash is None, no add — mirror 021's `v_residual is None` pattern at models/layers.py:2352 and 164's `q_carry is None` pattern).
- **Detach policy — pin in plan.md.** Idea's formula `α_l · av_{l-1}` lets gradient flow back into av_{l-1} and therefore into every parameter that produced it. That's a "feedback gradient" structure — fine if intentional, but 021's V-residual explicitly `.detach()`-es the layer-0 stash with the documented reason "each layer's W_V trains on its own attention path." **Pin the carry to `.detach()`** so the only gradient through the carry path is `α_l`'s gradient. Mirror 021's contract exactly so the runner's `isolate_effects` audit doesn't flag it as a gradient-coupling confound.
- **Layer 0 special case — explicit in plan.md.** At layer 0 the stash is initialized but NOT consumed (carry term is a no-op). At layers l ≥ 1 the stash is read back. Matches 021's `v_residual = None → stash branch; v_residual != None → blend branch` pattern at models/layers.py:2352-2356 and 164's `q_carry is None → skip blend` pattern.
- **Step-0 fp32 max-abs-diff ≤ 1e-6 — required verification.** With α_l=0 the additive mix is multiplied by 0, so its contribution is numerically zero; but adding a new path to the forward graph can perturb numerics at the trailing bits if the path is not branch-skip-elided. Plan must specify a `step0_diff ≤ 1e-6` check between `use_av_output_carry=True, alpha_l=0` and `use_av_output_carry=False`, both at fp32, across ALL 12 blocks (mirror 021's bit-identity claim at models/layers.py:2253-2254 and 164's bit-identity check at plan.md). If this fails, the lever's identity claim is broken — same bug class as 150-xlayer-feedback's `xlayer_mem` inplace append (closed reject at r3 cap).
- **α_l placement.** Put `nn.Parameter(torch.zeros(1))` on each MHA (same slot as 021's `lambda_v` at models/layers.py:1106 and 164's `alpha_q`), one per block, init 0 → AdamW (1-D params). Model-side construction mirrors 021's wiring at models/llm.py:337 / models/llm.py:879 and 164's wiring (one flag → one parameter block per layer).
- **Composition.** Composes orthogonally with 021 (V-side cross-block carry), 164 (Q-side cross-block carry), 016 (QK norm), 155 (per-head temp), 161 (per-layer temp), 162 (Q-only norm). The carry is added BEFORE the W_O projection, so post-AV-only mix does not perturb pre-softmax normalization. Plan should NOT enable 021 or 164 simultaneously with 168 — the A/B is `use_av_output_carry=True` vs baseline, single-flag diff. A future 3-way interaction test (all three carries on) is out of scope for this run.
- **Plan.md structure expected.** (Reviser writes it.) Sections: `## Mechanism` (math formula + prev_av definition + detach policy), `## Wiring` (LLMConfig flag → MHA param → MHA forward branch → MinimalLLM stash/loop), `## Step-0 identity claim` (verification protocol with fp32 max-abs-diff ≤ 1e-6 per block), `## Pass bar` (numeric Δ thresholds), `## Risk` (transfer-risk: med rationale unchanged — structural lever but AV-output-vs-V asymmetry is not a published production result), `## Composition matrix` (which existing flags are allowed simultaneously — 168 is the *third* axis of the V/Q/AV-output attribution test and should run solo for clean attribution).

**Why approve (not revise):** idea.md frontmatter + source claims + mechanism shape are sound and the info-value is high (closes the "WHY 021 won" question — V-specific vs general cross-block carry). The findings above are plan-specification gaps the reviser folds into plan.md without changing the idea — they don't reverse the mechanism, the source, the cost case, or the small-step-over-150 argument.

**Why not reject:** Not a duplicate (021 is V-side pre-AV; 164 is Q-side pre-AV; 116 is residual stream split; 150 is full cross-block Q/K/V attention — all structurally different from a post-AV additive carry); not fabricated (all four source citations resolve); not HP-tuning (structural lever, identity-init-able, 12 scalars); not over-budget (~15 LoC, +12 scalars, +0.001% param overhead); not off-tier (tiny1m3m seed 42 only); transfer-risk: med is justified (021 + ResNet/Highway/mHC at 100M+ scale, AV-output-specific axis less directly validated).

**Verdict: approve** → `needs-plan`, round reset to 1 for the code gate.
