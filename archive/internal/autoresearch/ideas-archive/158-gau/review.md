# Review log — 158 gau

## r1 — 2026-06-14 — verdict: approve
- **Source real and current.** Hua et al. "Transformer Quality in Linear Time" (Google, 2022), arXiv:2202.10447 verified live (HTTP 200, citation_author list = Hua/Dai/Liu/Le, citation_date 2022/02/21). Mechanism is GAU, not FLASH-as-a-whole — the citation is precise.
- **Mechanism is structural, not HP.** Replaces `TransformerBlock` with `GAUBlock` (fused Q/K/V/U_g/V_o linear + element-wise V_o gating + U_o output proj), with `U_g` and `V_o` zero-initialized slices so the block is identity at step 0 (`models/layers.py:4361-4362`). Step-0 identity is verifiable: `U_g=0 ⇒ y=x_norm`, `V_o=0 ⇒ z·V_o y=0 ⇒ U_o(0)=0 ⇒ block(x)=x`. Baseline off-path stays bit-identical.
- **tiny1m3m only, seed 42.** Plan explicitly fixed to the single tier (`configs/llm_config.py:5241-5303`, `Tiny1M3MGAUConfig(Tiny1M3MConfig)`, default `use_gau=True`); runner script follows the standard `--config_class --seed 42` A/B convention.
- **Not a duplicate of any closed lever.** The closed FFN-side set (117-soft-moe, 118-MoD, 145-expert-choice, 146-sparse-ffn, 157-conv-ffn) and parallel-attention-experts (156-moa) all close at 0.94M; none of them tests *whether the FFN should exist at all*. GAU ELIMINATES the FFN entirely (param count: 949,056 → 640,320, -32.5%) and folds the FFN's gating role into the attention block's `(U_g, V_o)` pair. Distinct axis.
- **<200 LoC.** `GAUBlock` body is ~182 LoC (`models/layers.py:4305-4446`); the rest of the wiring is dispatch (`models/llm.py:761-792, 1365-1454`) and one `Tiny1M3MGAUConfig` dataclass (~63 LoC docstring + 1 dataclass field). `training/trainer.py` change is +10/-1 (the `or ()` guard around `m.transformer_blocks` in `_collect_entropy_reg`). Total well under the 200-LoC cap.
- **Falsifiable pass/fail bar.** Final val loss at step 700 vs `autoresearch/baseline-cache.json` baseline cache (`baseline.sh verdict`), same `|Δ|≤0.01 NULL / >+0.01 DRIFT / ≤-0.01 PASS` band conventions used by every other idea. Headline prediction: most likely DRIFT/null at 0.94M (the 32% param cut means the GAU model is undertrained relative to baseline at the same token budget, and the savings can't be re-spent on attention dim in this 1-idea A/B). PASS would be a real signal that the Attention/FFN separation is the binding bottleneck.
- **Transfer-risk tag justified.** `transfer-risk: low` matches `## Scale evidence`: Hua et al. tested at T5 (250M-13B) with multiple production-grade ablations in Google's research codebase; the bit-budget-free FFN savings is qualitatively different from the closed capacity-injection levers. Tag holds.
- **Mutual-exclusions asserted.** `use_yoco`, `use_hyper_connections` are excluded at construction (`models/llm.py:769-786, 1405-1409`); `use_value_residual` lives on `block.attention` (which GAUBlock lacks) so it's structurally incompatible. None of these are on at the tiny1m3m baseline, so the default config is compatible.
- **One residual concern (non-blocking).** The auto-implementer bounced `needs-recode → needs-review` after 3 failed GPU runs all hitting `TypeError: 'NoneType' object is not iterable in _collect_entropy_reg` on the box. The local fix is in place (`trainer.py:1453` uses `m.transformer_blocks or ()`), but the rc=3 needs-recode never landed because a reset-button auto-requeued the running state mid-cycle, then a fresh runner hit the same error on the box (box trainer.py stale). The definition gate's job ends here — the implementer/runner side will re-verify on the next `needs-recode` pass — but the implementer should sanity-check that the box trainer.py carries the same `or ()` guard before re-running, so the next attempt isn't a repeat round-trip.

Round reset to 1 so the code gate gets a fresh budget per §3 ("On `approve`, reset `round` to 1").