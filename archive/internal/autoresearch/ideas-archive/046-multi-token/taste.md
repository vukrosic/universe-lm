## r2 — 2026-06-11 — verdict: accept

- **All five r1 findings addressed concretely.** (1) Stack-on-Canon declared: ctrl is the 023 WIN recipe (`Tiny1M3MVQGainSWAHighRoPE250KConfig + use_fire_pe=True + use_canon_conv=True`), trt adds `use_mta_qk=True` — so the A/B *directly* asks "is score-side local mixing a lever beyond what residual-side Canon already captured?", which is the right question. (2) Heads-axis conv explicitly de-scoped with the right reason (n_heads≈6–8 → depthwise heads-axis conv ≈ identity at tier; LoC without a math change). (3) Surface picked: Q and K, time-axis, k=3, depthwise, delta-init via scalar gates `g_q`, `g_k` init 0 → step-0 ≡ vanilla attention; left-padded with 2 zeros (causality, matches 023). (4) One-sentence bet is sharp: *n-gram-conditioned score is a new degree of freedom that single-token QK dot products cannot express even after Canon mixes the residual stream*. (5) WIN/NULL/FAIL all carry information at tier — the NULL is genuinely informative because it answers the orthogonality question for the cluster ("Canon already absorbs local-mixing at this scale → don't carry MTA-on-QK to 135M"), not just "didn't help".
- **Portfolio orthogonality argument is the right one.** 020–030 cluster is *post-projection inside-attention* rewrites (forgetting, softpick, gated, ssmax, qk-norm, v-norm, fire×qknorm, moonlight×qknorm) — all operate on or after the per-head Q·K dot product. MTA-on-QK is **pre-score** (DWConv on projected Q/K *before* the head reshape), genuinely outside that family; its true cousin is 023-Canon (residual-side local mixing, now the cluster's WIN). Filing MTA-on-QK as the score-side counterpart to the cluster's WIN is taste-defensible, not a 9th-in-a-row.
- **Niche fit clean.** Mechanism not HP; identity-init via zero scalar gates; ~40 LoC ≪ 200 ceiling; param cost `2·d_model·3 + 2` per block is rounding error at 0.94M; runs at tiny1m3m unchanged.
- **Transfer-risk: med, with a real mechanistic carry argument.** The Q/K time-axis half rests on "single-token QK ignores bigram-level position structure", which does not vanish with scale — if anything it gets *more* useful at 135M where long-range disambiguation matters more. Heads-axis de-scoping (the part that *would* be scale-conditional) is named in `## Scale evidence`. Good enough for the screen; the 135M decision is the runner's later business.
- **Accept → definition gate.** Round resets to 1 for the definition loop's own 3-round budget. Definition gate will hammer the specifics (exact left-pad-2 vs `padding=2` causality bug as in 023, scalar-gate vs LayerScale, position of the DWConv relative to RoPE, eval/log conventions); those are not taste's lane.

## r1 — 2026-06-11 — verdict: revise

- **Bet is fuzzy on the dimension that decides info-value: relation to 023-canon-conv (already a WIN, Δ≈-0.06).** Canon is a depthwise causal Conv1d on the *residual stream* before attention, delta-init. MTA is a depthwise conv on the *projected Q/K streams* (and, in the paper, the heads axis). The two are mechanistically different (score-side vs value-side local mixing) but neighbouring enough that at tiny1m3m one likely subsumes the other. The miner has to declare: **stack on top of 023 (Canon + MTA-on-QK), or head-to-head replace (vanilla + MTA-on-QK vs vanilla + Canon)?** Without that, the null is unreadable ("MTA didn't help" tells us nothing about whether the *score-side* lever is dead or just redundant with what Canon already absorbed).
- **Surface and axis under-specified — and at tiny1m3m the paper's heads-axis conv is a non-starter.** With n_heads ≈ 6–8, a depthwise conv over the heads dim is mostly identity. Pick **Q-only, K-only, or both Q and K** along the *time* axis only (the score-conditioning half of the paper). Drop the heads-axis half; call it out in `## Scale evidence` as deliberate de-scoping for the tier.
- **No crisp single-sentence "we expect X because Y".** Current line ("tests whether tiny1m3m needs a little token-group context inside the score computation") is a vibe, not a bet. Tighten to e.g.: *"score-side depthwise k=3 conv on Q and K (delta init) lowers val loss because dot-product scores miss bigram-level position context that single-token Q·K can't express, even after Canon's residual conv."*
- **Frame the null so it still earns the slot.** Two informative outcomes: WIN → score-side local mixing is a *separate* lever from residual-stream mixing (justifies carrying both to 135M); NULL given Canon is present → Canon's residual conv already captured the local-mixing degree of freedom at this scale (closes the question for the cluster). State that explicitly in `## Why it's worth a slot`.
- **Portfolio note (not blocking, but flag):** 020-030 is dense with attention-score variants (forgetting, softpick, gated, ssmax, qk-norm, v-norm, fire×qknorm, moonlight×qknorm). One more is acceptable *only* because the mechanism is genuinely orthogonal to the residual-conv winner — and the re-pitch must lean on that orthogonality.
