# 027 — taste log

## r1 — 2026-06-10 — verdict: accept

- **Leverage.** Both parents are proven WINs (015: −0.0138/−0.0185; 016: −0.0138/−0.0185 — tied exactly in the same A/B session ctrl→015→016→017→ctrl2). Even if the stack is fully additive the upper-bound Δ is ~−0.028; if the optimizer-scale lever (015) and the runtime logit-bounding lever (016) end up chasing the same downstream stability property, the combined Δ will be smaller than additive. Either way, the magnitude is well above the in-session ctrl gap (0.0047), so the answer — win, additive-win, or subadditive-win — is informative, not a rounding-error A/B.
- **Information value (the real bet).** Sharpest "we expect X because Y" the stack can ask right now: are 015 and 016 *orthogonal* (different mechanisms, additive ~−0.028) or *substitutes* (both stabilising the same attention-logit headroom, subadditive)? The subadditive answer is non-obvious and would force a recipe decision: carry only one of the two into the 10M→135M ladder. The additive answer licenses carrying both. A clean null is the *interesting* outcome here, not a failure mode. The idea is, in effect, an orthogonality test with two well-instrumented WIN parents — exactly the kind of high-info A/B this gate is supposed to admit.
- **Non-obviousness / dup check.** No dupe in `closed.md` or on the active board. `026-fire-x-qknorm` is the parallel composition of 009×016; 027 is 015×016 — distinct parents, distinct test (optimizer × MHA vs attention-positional × MHA). The two compositions are complementary, not redundant.
- **Portfolio fit.** 015 and 016 already shipped in the same A/B session and tied exactly. A separate A/B that *stacks* them is the natural next step; the GPU-famine concern cuts toward running it, not skipping it. With the queue light on `needs-run` and a known-WIN pair available for a two-flag run, this is one of the cheapest non-trivial A/Bs on the board.
- **Niche fit.** Both flags (`use_moonlight_muon`, `use_qk_norm`) are already wired in their respective code paths (`optimizers/muon.py` and `models/layers.py` MHA forward); the composition is a two-flag enable. No shared state, no init hazard (both step-0 identity — Moonlight NS5(0)=0, QK-Norm γ=1/β=0). Easily under the LoC budget. Scale-evidence `transfer-risk: low` is well-grounded for both parents (Moonlight at billion-param, QK-Norm at 22B+) and stacking them does not introduce a new scale risk.
- **Verdict.** Accept. The hypothesis is concrete (orthogonality vs substitute, with an expected Δ range that is well above noise), the implementation is trivial, and either outcome moves the 135M recipe decision. Move to `needs-review` for definition.
