## r2 — 2026-06-09 — verdict: approve

- All four r1 findings landed. Lion scope now carried by the idea (`optimizers/lion.py`, `training/trainer.py:_setup_optimizers` branch, 2-D→Lion / 1-D+embed→AdamW routing mirroring Muon at `trainer.py:92-156`). Bare-Lion prerequisite ctrl is explicit — two leaderboard rows, both logged. PASS tightened to ≤ −0.015 vs bare-Lion ctrl with |Δ| < 0.01 NULL band. `mask.mean().clamp(min=0.1)` rescale floor pinned in mechanism, `lion_lr=0.0003` pinned in Notes — no HPs deferred to the implementer.
- Cite (arXiv:2411.16085, Liang et al. 2024) verified r1. Mechanism is structural sign-mask on the sign-update direction, not HP tuning. Implementable < 200 LoC (Lion ~50 + trainer branch ~20 + flags ~5). Tiny1m3m only, seed 42. Not a closed lever — 001/002 are the variance-mask family on momentum optimizers; Lion's pre-signed update makes the mask a direction-disagreement filter, mechanistically distinct enough for one slot. A third null in the Cautious family would itself be informative ("Cautious doesn't fire at tiny1m3m, across update geometries"); a win would be a free routing-table lever.
- Plan-level notes for the code-implementer (not blocking approval): (a) `use_lion` and `use_muon` must be mutually exclusive — Lion replaces Muon on the 2-D path, so set one of them to False when the other is True (assertion at trainer.py setup). (b) Two-run protocol (bare-Lion ctrl, then Cautious-Lion vs bare-Lion) deviates from the usual one-A/B-per-idea — plan must spell out the run order and which Δ goes against which ctrl on the leaderboard. (c) `lion_lr=0.0003` is Chen et al.'s default at much larger scale; if bare-Lion ctrl ends up far worse than Muon-AdamW the A/B still measures the cautious-mask effect against Lion, which is the right ctrl for the hypothesis — note this in the plan so the runner doesn't flag the bare-Lion gap as drift.

## r1 — 2026-06-09 — verdict: revise

- **Lion is not in the routing table.** `optimizers/` has `cautious_adamw.py`, `muon.py`, `soap.py`, `schedule_free_adamw.py` — no `lion.py`. `training/trainer.py:14-17` only imports `Muon`, `CautiousAdamW`, `SOAP`, `ScheduleFreeAdamW`. The "ctrl Lion" baseline cited in the pass/fail bar does not exist in `LEADERBOARD.md`. The Wiring section understates the change: it is not just a flag and a new 30-LoC file — it also requires (a) adding `optimizers/lion.py` (or a thin wrapper), (b) wiring Lion into `training/trainer.py:_setup_optimizers` alongside Muon/AdamW, (c) routing logic for the 2-D vs 1-D vs vocab split that Lion replaces, and (d) a Lion-only baseline run that establishes the ctrl number.
- **A/B conflates two levers without the baseline.** With no Lion ctrl, the Cautious-Lion run measures `(Lion − current baseline) + (Cautious − Lion)` in one number, not the Cautious effect alone. Either (i) split this into two ideas — "Add Lion as a routing option" first (Lion vs current Muon-AdamW, no cautious), then "Cautious-Lion" as a follow-up referencing the Lion ctrl; or (ii) commit the plan to running a Lion-only baseline ctrl as a prerequisite step in the same idea, with both numbers in the leaderboard entry. The reviser must pick one and make it explicit in the Plan section.
- **Pass/fail bar is on the noise floor.** PASS ≤ −0.005 is well inside the ~0.0175 ctrl-pair gap and the ~±0.01 run-to-run variance. Both prior Cautious variants (001-muon, 002-adamw) closed as null. Tighten PASS to ≤ −0.015 and explicitly call out the |Δ| < 0.01 NULL band so a sub-noise "win" cannot be called a win. If the hypothesis range was honest, this still gives Cautious-Lion room to land a real effect (upper bound of the hypothesis is −0.03).
- **Mask-mean rescale detail.** The mechanism says "rescale the update by `1 / mask.mean().clamp(min)` to keep the effective LR constant" — pin the clamp floor to a concrete value (e.g. `clamp(min=0.1)`) in the plan, not deferred. Otherwise the plan leaves an HP to the implementer.

Notes that survived my pass: mechanism is real (Liang et al. 2024, arXiv:2411.16085 is a verifiable cite and the cautious-on-Lion generalization is explicit in the paper), no duplication with closed levers, identity-safe at the mask level, no compute cost.
