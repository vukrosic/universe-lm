## r2 — 2026-06-11 — verdict: accept
- **All r1 gaps closed.** 077-fnet verified nonexistent (`grep -l "fnet|fourier|fft" autoresearch/ideas/*/idea.md` returns only 078) — portfolio-overlap concern moot. Bet upgraded from "tests feasibility" to concrete **Δ ≤ −0.01** vs FIRE ctrl with a SSMax-style mechanism argument (late-position attention flattening at seq_len=2048). Construction named (lower-triangular causal DFT, not vanilla FFT + tril). Short-seq critique was based on a wrong premise on my part — tiny1m3m runs at `max_seq_len=2048` (verified in `configs/llm_config.py:25`), so the global-mixing argument actually holds.
- **Sharp, niche-fit bet.** Zero-init gated additive residual (`x + tanh(g)·FNetAR(LN(x))`, `g=0` ⇒ step-0 identity). One scalar param/block, < 80 LoC, no infra needed. Same identity-init / bounded-null discipline as 023/025/030. Causal correctness explicit (per-position prefix DFT, no future leakage).
- **Orthogonal axis.** Frequency-domain global mixing is distinct from 023-canon-conv (local time-domain) and 025-SSMax (length-dependent temperature) — none of the in-flight family-mates touch this lever. A null still teaches us the global-mixing slot is already filled by attention at 1M scale, which directly informs whether Mamba-style SSM ideas (also content-independent global) are worth filing. Informative either way.
- **Transfer argument is mechanistic, not paper-cited.** The additive-gated form is a strictly weaker use than the paper's attention-substitution — higher chance of clearing at our scale, and the same identity-init / scalar-gate mechanism transferred for the U-Net skip work. Medium transfer-risk is honest and acceptable.

## r1 — 2026-06-11 — verdict: revise
- **Portfolio overlap with 077-fnet (same `needs-taste` queue).** Two adjacent Fourier-mixer slots is the "5th optimizer variant" pattern the taste bar warns about. Either kill 077 and make 078 the only Fourier mixer (we train causal LM — non-causal FFT in 077 leaks future tokens, so the AR variant is the right form anyway), or differentiate 078 with a sharp claim of what it teaches *beyond* 077. As written, if 077 lands first, 078 is redundant; if 077 fails, 078 inherits the same null.
- **Leverage claim is missing.** "Tests whether Fourier mixing can survive autoregressive training" is a feasibility check, not a win prediction. Add one sentence of "we expect val-loss Δ X because Y mechanism at tiny1m3m". Without it, the only positive outcome is "it didn't diverge", which is info-poor.
- **Mechanism doesn't name what makes FNetAR distinct.** The paper's AR Fourier construction (sliding-window causal DFT or whatever they used) is not the same as naively causal-masking 077's vanilla FFT. The idea.md says "causal Fourier token-mixing variant from the paper" without naming it. Spell out the construction in one line so the reviewer can tell this isn't just `077 + tril mask`.
- **Tiny1m3m short-seq leverage isn't argued.** FFT mixers' headline win is global mixing at long context where attention's O(n²) bites. At tiny1m3m's short seq (~256), the global-mixing advantage is muted and we already have full attention. Argue why a *gated additive* causal FFT residual would still move val loss at this scale — e.g. cheap frequency-domain inductive bias the model can't easily learn from attention alone — or concede this only fires at longer context (which → reject as off-niche).
