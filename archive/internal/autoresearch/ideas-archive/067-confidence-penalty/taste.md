# 067 — confidence-penalty — taste log

## r1 — 2026-06-11 — verdict: revise

- **Sibling-null risk is the dominant taste gap.** `010-polyloss` just closed null at tiny1m3m (trt 6.5938 vs ctrls 6.5991/6.6050, Δ −0.0053 inside ctrl-gap 0.0059, closed.md:40). PolyLoss's `(1 − p_t)` and confidence-penalty's `−λ·H(p)` push the same direction (softer gold-token spike); the entropy form only differs in that the pressure lands on all V logits rather than gold-only. The re-pitch must explain mechanistically why gold-vs-uniform escapes the ~0.005 noise floor that already swallowed PolyLoss at this scale — otherwise the most likely outcome is another null inside ctrl-gap, which is **uninformative** given the sibling result.
- **Crowded family — info value collapses if we run all three in isolation.** 067-conf-penalty, 068-unlikelihood, 069-focal-loss are three consecutive "reshape the output distribution" ideas in needs-taste; 010-polyloss already ran null. Running 067 in isolation is fine; running it as the 3rd soft-output knob with no differentiation argument is portfolio-crowded. Either (a) name the *one* sub-question only the entropy form answers (e.g. "does symmetric-across-vocab pressure beat gold-only pressure?"), or (b) propose a head-to-head against 010 with both flags pinned, so the slot adjudicates the family instead of duplicating PolyLoss.
- **Lever is already pre-wired — claim the cheapness, don't bury it.** `configs/output_head_ablations.py:36` ships `Tiny1M3MConfPenaltyConfig(β=0.01)` as the OH3 anchor; `training/trainer.py:474-485, 526-537` already reads `conf_penalty_beta` and applies `−β·H(softmax(logits))` train-side with eval staying plain CE. closed.md and LEADERBOARD don't show a prior A/B for OH3, so it's not formally closed — but the slot is ≈0 LoC. That cheapness is itself a leverage argument the pitch should lead with, not ignore.
- **Crisp-bet sentence is missing.** The "Why it's worth a slot" line ("sharper sibling of label smoothing") is a vibe, not a bet. The re-pitch must add one sentence of the form "we expect Δ ≈ X at tiny1m3m because Y" with an explicit β choice and the mechanism for why that β fires inside ~92 train steps (PolyLoss's default pin didn't, and the entropy form's per-step gradient scales with V so the magnitude argument needs to be made explicit). Without that, the idea fails the crisp-bet bar (idea-taste.md:67).
- **Concrete way to close the gap.** Re-pitch as: "ConfPenalty β=0.01 vs 010-PolyLoss-style gold-only soften — bet is that symmetric-across-vocab entropy pressure (`∇` hits all V logits) survives the tiny-vocab regime where gold-only pressure (`∇` hits one logit) collapsed into the ~0.005 noise floor; expect Δ ≈ −0.01 to −0.02 because per-step gradient magnitude is ~V× larger, so the signal survives the 92-step tiny1m3m run. Lever already wired (configs/output_head_ablations.py:36, trainer.py:474-485, 526-537); ≈0 LoC. Null still tells us soft-output knobs are dead at tiny1m3m regardless of which logit they touch — closes the family." That pitch is acceptable; the current one is not.

## r2 — 2026-06-11 — verdict: accept
- The repitch now makes the entropy-vs-gold-only distinction mechanistic and falsifiable.
- The beta, expected delta, and family-level null interpretation are pinned.
- This is ready for definition review.
