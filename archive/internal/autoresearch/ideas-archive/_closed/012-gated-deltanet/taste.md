## r1 — 2026-06-09 — verdict: reject
- **Re-pitch of 008-gated-deltanet**, which is already in `_closed/` with the same mechanism (gated delta-rule linear attention, same Yang et al. paper). The miner's own Notes admit "this filing subsumes 008" — but superseding requires beating the prior rejection, not just re-filing.
- **Same off-niche reason**: 008 was taste-rejected because it proposed `screen20m+`; pipeline runs `tiny1m3m` only. 012's hypothesis itself says "If null at tiny scale, that's a clean negative — the mechanism is correct but doesn't fire at this scale" — i.e., the miner *agrees* the bet is small at our tier, and proposes to spend the slot on a "clean negative." That's not a clean negative — that's a slot spent confirming what the tier is too small to test. The pipeline's tier is fixed and small; "doesn't fire here" was already decided when 008 was closed.
- **Pass-bar asymmetry**: 012's pass bar is −0.01, but the idea concedes linear attention often *hurts* at small scale (DRIFT > +0.02). The expected-value calculation is: a known-tier-mismatch architectural rewrite that pays off rarely, ships a chunkwise kernel we don't need, and competes for a slot with niche-fitting tweaks. Not worth it.
- **Information value is real but already collected**: the 004-retnet-retention null at tiny1m3m is the linear-attention-at-our-scale datum. We have it. We don't need a second, more expensive one.
- Reject. Folder moves to `_closed/`.
