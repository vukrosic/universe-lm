## r1 — 2026-06-11 — verdict: reject

- **Family-overlap with 022 Softpick (running r3).** Softpick is rectified-softmax attention (`relu(exp(x)−1) / Σ|exp(x)−1|`) — same bet category as ReLUFormer (`A = ReLU(scores)` + row scale): both are "let attention mass go to zero, drop the softmax normalizer". 022 is the better-spec'd member: mask interaction worked out, `ε` pinned, step-0 smoke + grad asserts wired, FIRE-equipped ctrl pinned, LoC ≤ 50. Running 053 in parallel can only either confirm 022 (no new info) or contradict it (a coin-toss between two redundant levers, useless without a tie-breaker). The "softmax geometry" question is already owned by 022 — burning a slot to re-ask it in a coarser form is not worth it.
- **Source evidence is sequence-length-driven, not LM-pretraining-driven.** Paper grounds the win in "many value slots" and document-level translation; the idea's own `transfer-risk: high` admits this. Our tier is tiny1m3m × seq 1024 with SWA-512 in the WIN ctrl — exactly the regime where a long-seq lever doesn't fire. No mechanistic argument is offered for why rectified-attention would help at SWA-512 specifically, only the generic "tiny models with few heads suffer most from sink-waste" — which is 022's pitch, not ReLUFormer's.
- **Spec is hand-wavy where 022 is sharp.** "The paper's row scaling" is referenced but not written down; mask interaction is not addressed (softpick taught us this is correctness-critical and a silent-bug class); step-0 behaviour (`ReLU(0) = 0` → zero attn output → zero Q/K/V grads) is not analyzed. These are gate-2 issues, but they signal the bet hasn't been thought through enough to make 053 *additive* to 022.
- **Portfolio is already crowded with attention-activation/normalizer levers.** 016 QK-Norm (done), 024 gated-attention (done w/ caveat), 025 SSMax (done w/ caveat), 022 softpick (running r3), plus 029 V-Norm (running), 026 FIRE×QK-Norm, 027 Moonlight×QK-Norm in flight. Adding a 6th attention-normalizer variant in the same family — and a redundant one — is the "5th optimizer-momentum variant in a row" anti-pattern the prompt calls out.
- **Information value low.** A null result here is "softpick is the canonical rectified-attention test; ReLUFormer is the coarser sibling" — we already knew that going in. A win without 022 winning would be confusing (mechanism unclear); a win after 022 already won would be redundant. Either way, info value < the slot cost.
- **Reason summary:** redundant with 022 Softpick (running, sharper spec, same bet); long-seq evidence does not transfer to tiny1m3m × SWA-512; queue is already saturated with attention-activation variants.
