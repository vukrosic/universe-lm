## r1 — 2026-06-09 — verdict: reject
- **Re-pitch of 007-sigmoid-loss**, already in `_closed/` (not "deleted" as the miner's Notes claim — git status shows 007 in `_closed/`, and closed.md has it under "Closed by the loop"). The miner's premise is wrong, and the prior rejection still stands.
- **Same mechanism as 007**: sigmoid loss with z-loss regularizer, same `γ` and `z_coef` knobs, same `L = -log σ(γ·z_t) - Σ log σ(−γ·z_j)` form. The "fuller spec" framing is a longer mechanism paragraph, not a new mechanism.
- **Citation problem not fixed — in fact repeated.** 014's Source still leans on "arXiv:2405.18719-adjacent literature," but `2405.18719` is the *CoPE* paper (arXiv:2405.18719, Golovneva et al. 2024) — same author neighborhood and the same wrong-arXiv-ID pattern that got 007 killed after 3 rounds. Mining the prior's flaw instead of fixing it is exactly what the taste gate is supposed to catch.
- **Why the prior kill was correct**: the original 007 lost the source. If the source can't be pinned to a real paper with a real result we can reproduce, the lever has no anchor — we're A/B'ing against vibes. A sigmoid-loss plan with a real source (Xi et al. 2023, "Efficient Training of Language Models with Sigmoid Loss for Large Language Models" — arXiv:2503.01403 or similar) would be a fresh start; 014 is not that filing.
- **Differentiator claim vs 010 (PolyLoss) is fine** but irrelevant when the source is broken. The right move is a fresh filing with a citable source, not a re-pitch of a closed one.
- Reject. Folder moves to `_closed/`. If a real sigmoid-loss idea resurfaces with a real citation, file it as a new slug.
