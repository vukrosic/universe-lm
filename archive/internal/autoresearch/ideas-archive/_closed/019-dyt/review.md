## r1 — 2026-06-09 — verdict: reject
- **Duplicate of closed `squash` lever.** `models/layers.py:52-61` defines `SquashNorm` as `weight * tanh(alpha * x)` with no cross-feature reduction — operationally identical to the miner's `DyT(x) = γ ⊙ tanh(α·x) + β` (the bias term is a no-op affine the existing `weight` already covers when split into γ/β; the `+ β` is an LR/HP re-parameterization, not a structural change). `closed.md:24` lists `squash` in the closed norm-zoo. Prior empirical result: **val_loss 7.6278, diverged** (`docs/tutorials/normalization/ablations.md:25`, `docs/tutorials/normalization/findings.md:18,60,165`). Findings doc states explicitly: *"bounded/DyT residual (already falsified by squash)."*
- **The "categorically distinct" claim is false.** The miner's bet hinges on "DyT drops the reduction entirely — the closed zoo all kept reduce-then-normalize." `SquashNorm` is the *one* closed lever that already drops the reduction entirely (it has no mean/variance step). The closed null on `squash` IS the categorical null on "is the reduce needed at all?" — the miner's "partition" question is already answered.
- **A scalar-α tweak is not a mechanism.** The only paper-vs-code-shape difference is α shape: `SquashNorm` uses per-dim `alpha` init 1.0, paper's DyT uses scalar `alpha` init 0.5. This is an init/parameterization lever on the same diverged operation. Even the repo's `dyntanh` lever (N15, `models/layers.py:289-290`) — which is closer to the paper's scalar-α form, init 1.0 — is listed as "own control, pending" in `docs/research/rmsnorm/rmsnorm/plan.md:85`. No prior result supports a "diverged→matches-LN" flip on init-shape alone.
- **Verdict**: reject. Closed-zoo duplicate with a *diverged* prior result. Re-pitching a falsified lever on init-shape grounds is exactly the "mathematical duplicate" the prompt says is `reject`. If the miner's read of the paper is actually a *new* mechanism (e.g. learned global α in (0,1) gating how much tanh squashes, vs the current learnable unbounded α), that needs a *new* idea — not a re-pitch with different init.
