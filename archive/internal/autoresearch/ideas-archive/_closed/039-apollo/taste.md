## r1 — 2026-06-11 — verdict: reject

- **Tier mismatch: the lever has nothing to lever.** APOLLO's payoff is memory-state savings at *AdamW-level* loss (paper's own claim, not "beats AdamW"). At tiny1m3m (0.94M params), AdamW state is ~7.5 MB — never the bottleneck. The mechanism literally cannot fire here; the best-case outcome is matching AdamW, which is the definition of an info-free A/B.
- **The miner's framing is tautological, not informative.** "A null would tell us low-rank structure is only useful once optimizer state is the bottleneck" — we already know that from the paper's setup (LLaMA-7B/13B); running tiny1m3m to confirm it teaches us nothing we couldn't read off the abstract. This is the "meh, inconclusive" outcome the taste prompt explicitly flags as failure.
- **Precedent is already in closed.md.** Two direct analogues both failed: (a) `018-ademamix` taste-rejected for tier mismatch (slow-EMA doesn't fire in ~92 steps); (b) `003-soap` ran NULL because "vocab params on AdamW fallback so SOAP mostly bypassed at tiny1m3m" — APOLLO has the same matrix-path-only wiring (1D params fall through to AdamW), so the A/B mostly measures the AdamW fallback, not APOLLO.
- **Portfolio is saturated with optimizer variants.** Closed: 001-cautious-muon (null), 002-cautious-adamw (null), 003-soap (null/bypassed), 005-decoupled-qkv-muon (null), 006-schedule-free-adamw (clear negative), 018-ademamix (taste-reject). Active wins are 011-cautious-lion and 015-moonlight-muon-rms — both *modifications to existing-state* optimizers, not *low-rank approximations*. Adding another low-leverage optimizer slot at this tier is the portfolio drift the taste gate exists to block.
- **`transfer-risk: low` tag is misapplied.** Paper evidence is LLaMA-7B/13B, but the *mechanism* (low-rank state) only activates when state size matters. Transfer risk for a memory-efficiency lever from 7B→1M is "the lever literally does not engage" — that's the highest risk class, not the lowest. The tag is a tell that the miner did not stress-test the at-our-tier story.
- **No re-pitch is rescuable.** The paper's contribution is memory, not loss; reframing it as a loss lever at 1M would require fabricating a mechanism the authors do not claim. Reject, do not revise.
