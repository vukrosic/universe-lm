# Taste log — 200 rope-phase-offset-per-layer

## r2 — 2026-06-15 — verdict: accept

The r2 fix lands. The mechanism is now mathematically non-trivial, the prior engagement is direct (each r1 finding closed with a real argument, not hand-waving), and the lever sits in a genuinely open cell. Worth a slot.

- **The math is now correct.** r1's default sketch (1 φ per layer, applied to both Q and K) was a guaranteed no-op — orthogonal R on both sides preserves QK^T exactly. r2 fixes this two ways at once: (a) applies the rotation to **K only**, which breaks QK^T symmetry (`<R_l K, Q> ≠ <K, Q>` in general), giving the lever a real axis to bind on; (b) makes the rotation **per-pair** (different φ_{l,i} for each of the 8 planes in d_k=16), which breaks the cross-pair orthogonal symmetry that a uniform per-layer R would have. The Q-side sees standard RoPE; the K-side sees R_l @ standard_RoPE(K). At init φ_{l,i}=0, R_l = I, K unchanged, QK^T unchanged, step-0 byte-identical ✓. The r1 no-op concern is closed.

- **Structural argument vs W_O absorption is sound.** The r1 cluster (161-dyt-temp, 142-layerscale, 130-rezero, 111-drop-path, 116-hyper-connections) was all **scalar** per-layer parameters — single number per layer that scales a path magnitude. Scalar per-layer params are **absorbable by W_O** (W_O is per-channel; a uniform scalar gain in front of W_O's input is degenerate with W_O's row-scaling, and at 0.94M W_O has 64×16 = 1024 params to absorb a 12-scalar drift trivially). 200 is **rotational per-layer × per-pair** — 8 angles per layer that rotate K by an *orthogonal block-diagonal matrix* with *different* angles per pair. W_O is per-channel (along d_model → d_k mapping); it has no operation equivalent to "rotate pair i by angle α and pair j by angle β". The per-pair structure breaks cross-pair symmetry in a way W_O's per-channel projection has no reach into. The 175-alibi-slopes WIN is the direct precedent: per-head bias is pre-softmax, post-Q/K-projection, **W_O-non-absorbable** (W_O is post-AV; bias lives on attention scores, pre-softmax). 200 is the per-layer × per-pair rotational analogue on K — same category. The r1 review's "scalar cluster is damning" worry is closed by the structural distinction, not by vibes.

- **185 engagement is empirically correct.** r1 said "if 185 with 32 scalars per block couldn't bind" — that's a misreading. Per `ideas/185-static-per-head-k-rotation/log.jsonl`, 185 hit `MAX_RECODE_ROUNDS=3` (auto-closed by `flip.sh`) because the box's `git pull` couldn't see the locally-uncommitted `Tiny1M3MStaticKRotationConfig` class (commit `fa1ed31` confirmed SMOKE_OK, +384 params, step-0 `max_abs_diff = 0.0`). **185 never reached the GPU** — no `evidence.md`, no val measured. The miner's correction is right: 185's closure is procedural, not empirical. The static-K-rotation lever family is empirically open at 0.94M.

- **200 and 185 live in different cells.** 185 = per-head × per-plane (H × d_k/2 = 4 × 8 = **32 angles per block**, 384 total). 200 = per-layer × per-plane (L × d_k/2 = 12 × 8 = **96 angles total, shared across heads**). At 12L × 4H = 48 (block, head) cells, 185 varies the rotation *across heads within a layer*; 200 varies it *across layers* (depth axis). The two are orthogonal axes of the same lever family. If 200 binds, the depth × pair axis is real (per-layer × per-pair specialization is what the optimizer finds useful); if 200 nulls, the family is closed at this tier regardless of which axis we test.

- **172 engagement is correct.** 172 = per-head *frequency* (multiplicative scale on RoPE's position-dependent angle θ_i(m)). 200 = per-layer *phase* (constant offset on K, position-independent). Different tensor (Q/K angles pre-rotation vs K post-RoPE), different operation (frequency scaling vs K rotation), different axis (per-head vs per-layer). The 172 null doesn't close 200's mechanism.

- **Leverage.** If it binds at 0.94M, unlocks a per-layer × per-pair lever for Phase-2 ≥135M where the (depth × head × pair) grid is 24L × 12H × 8P = 2,304 cells (vs 384 at tiny1m3m) and the optimizer has richer structure to find. 154-rebased-attn already validated the orthogonal-rebase axis at 0.94M (WIN Δ=−3.48, record break); 200 is the learned per-layer × per-pair *learned* generalization of 154's *fixed random shared* rebase. The axis exists; the question is whether the optimizer can find a useful per-layer × per-pair basis at this tier.

- **Information value — high in all 3 outcomes.** WIN: depth × pair rotational axis binds → lift to 135M Phase-2. NULL: bounds the per-layer × per-pair static-K-rotation axis at this tier, informs Phase-2 to look at *non-RoPE* position-encoding extensions. DRIFT: closes the static-K-rotation family until ≥135M. No "meh" outcome.

- **Crisp bet (one sentence).** "At 0.94M the depth × pair rotational axis is empirically open (185 never ran; 172 tested a different lever; depth-axis cluster tested scalars); 200 tests whether the optimizer can find a per-layer × per-pair static K-rotation that lowers val loss, with 96 params and a step-0 byte-identity guarantee." Sharp, falsifiable, scope-bounded. ✓

- **Niche fit.** Mechanism (not HP) ✓; identity-init (θ=0 ⇒ R_l = I ⇒ step-0 byte-identical) ✓; tiny1m3m runnable ✓; +96 params (+0.001%) negligible ✓; ~50 LoC across `models/layers.py`, `configs/llm_config.py`, `models/llm.py` ✓. Transfer-risk: med (tagged) — lever form is novel at ≥100M (no published paper tests per-layer static K-rotation), but the underlying orthogonal-rebase mechanism is well-validated by 154 (WIN at 0.94M) and RoPE family at 7B+. The novel-lever-form at scale is a real transfer risk, but the mechanistically-grounded W_O-non-absorbable argument gives a path to evaluate it.

- **Portfolio fit.** RoPE/position-encoding axis has been well-probed at 0.94M (175-alibi-WIN, 172-null, 174-xpos-null, 166-t5-rpe-null, 152/155/160-null, 009-FIRE-WIN), but **per-layer × per-pair static K-rotation is novel** — no in-repo test, no in-paper test. The static-K-rotation lever family has zero empirical data points at this tier after 185's procedural closure; 200 supplies the first. The 6th depth-axis null in the cluster would be a problem, but the rotational-vs-scalar distinction is real and the W_O-non-absorbable argument holds.

- **One seed only.** Plan specifies seed 42 only, with the two-ctrl rule from the baseline cache box `5b8a7fea8963` (RTX 3060, val_mean = 6.3988, noise_band = 0.04, n_measurements = 3). WIN bar Δ ≤ −0.005 (modest but real for a 96-param lever), NULL bar |Δ| < 0.01, DRIFT bar Δ > +0.01. Sub-noise is inconclusive, not real; no multi-seed proposal. ✓

- **Verdict: accept** — sharp, high-leverage, fits the niche, informative in every outcome, novel in the queue, mechanism math now provably non-trivial. Proceeding to definition gate (round reset to 1).

## r1 — 2026-06-15 — verdict: revise

The mechanism has a clean shape on paper (init 0 = byte-identical, +12 to +48 params, identity-init-able) but the math plus the recent closed list together kill the bet as written. Two critical findings, one of them fatal to the default sketch.

- **CRITICAL — default sketch (1 φ per layer) is a mathematical no-op.** The mechanism writes `θ_i(m, l) = m · base^(-2i/d_k) + φ_l`, applied to **both** Q and K. Since 2D rotation matrices compose, `R(θ_standard + φ_l) = R(φ_l) · R(θ_standard)`, so the lever is: apply standard RoPE, then apply a fixed per-layer 2D-rotation block-diagonal `R(φ_l)` (all 8 pairs share the same angle) to both Q and K. The same fixed orthogonal `R(φ_l)` on both Q and K preserves QK^T exactly (`<Rq, Rk> = <q, k>` for orthogonal R), so attention scores are unchanged, softmax is unchanged, AV is unchanged. The downstream W_O can absorb `R(φ_l)` into a new equivalent W_O' (orthogonal R does not change W_O's effective spectral norm). Net: the 12-scalar version of 200 changes the forward graph but not the loss, and the optimizer has zero gradient on φ_l. The design sketch's first option is a no-op A/B, which would burn a slot for a guaranteed null. The miner must either (a) explain why the no-op claim fails (some non-obvious cross-pair or W_O interaction I'm missing), or (b) commit to the per-pair sketch (φ_l_i for each of the 4 pairs in d_k=16), where per-pair φ breaks the orthogonal symmetry across pairs and CAN have an effect.

- **CRITICAL — per-pair sketch is structurally a less-expressive 185.** 185-static-per-head-k-rotation is exactly "static orthogonal rotation on K, parameterized by learnable angles, identity-init". 185 had 4 heads × 8 planes = 32 angles per block × 12 blocks = 384 params (H × d_k/2). With per-pair φ (4 planes × 12 blocks = 48 angles), 200 is the same lever family, just with: (i) per-pair only, not per-head (4× less expressive on the per-head axis), (ii) applied to both Q and K, not K only (more constrained, since QK^T sees the same rotation on both sides — net rotation that affects AV must propagate through QK^T asymmetry between pairs, which is a thinner axis than 185's per-head K-only). 185 exhausted 3 recode rounds and the axis was abandoned. The miner pitches 200 as "different from 185 (RoPE-modulating vs post-RoPE)", but the math is the same lever family. If 185 with 32 scalars per block couldn't bind across 3 recode attempts, 200 with 4 scalars per block on a more constrained (QK-symmetric) axis is the wrong direction. Either the miner engages with 185's outcome directly — was the failure a recode plumbing issue (ImportError on the box per the `idea.md` re-code note) or a real null — and argues why the QK-symmetric per-pair version is qualitatively different, or the lever is dead.

- **Prior evidence: 172 closes the position-axis at 0.94M for the per-head-frequency variant.** 172-per-head-rope-base (per-head frequency, 48 scalars) closed null at tiny1m3m (Δ=+0.0109 wrong-sign tiny). 200 is on the same position-encoding axis (it modifies the RoPE angle), at strictly less expressiveness (per-layer 12 scalars vs per-head 48 scalars in 172). The 172 prior says: scalar modulations of RoPE angles don't bind at this tier. The miner dismisses 172 as "different axis (frequency vs phase)", but at 0.94M with 92 update steps, "different axis" reads as a distinction that the optimizer can't see — the binding constraint is total scalar count, not which RoPE parameter they touch.

- **Prior evidence: depth-axis null cluster is damning.** Recent per-layer scalars at 0.94M have all closed null: 161-dyt-temp (per-layer τ — the `idea.md` literally said "at 0.94M/12L the model has no scale-depth for the lever to find a useful different-per-layer schedule"), 142-layerscale, 130-rezero, 111-drop-path, 116-hyper-connections. The pattern: 12-layer depth at 0.94M has no useful per-layer specialization axis. 200's per-layer φ_l falls into this cluster. The 175-alibi WIN is the *counter-example* that the miner will cite, but 175 is a *uniform* per-head bias, not a *per-layer* variation — different axis, different finding.

- **Niche fit otherwise OK in principle**: mechanism (yes), identity-init (yes for the no-op case trivially, yes for the per-pair case at θ=0), tiny1m3m runnable, +0.001% params, RoPE angle structure is a real lever family at 7B+ (LLaMA 1/2/3, Mistral, Qwen, etc.), so the transfer argument would be strong *if* the lever could bind at 0.94M. The transfer story doesn't rescue a no-op or a near-superset of an abandoned lever — both fail before transfer matters.

- **Crisp bet is missing the strongest 2 priors.** The "Why it's worth a slot" paragraph cites 172-null and 175-WIN but doesn't engage with 185's recode-cap (closed 2026-06-15) or the 1-scalar-per-layer no-op. Without engaging these, the bet is built on priors the miner hasn't read.

- **What the reviser must do to make this sharp.** (1) Disambiguate: commit to the per-pair sketch (48 scalars, breaks the orthogonal symmetry), and drop the 12-scalar default as broken. (2) Read 185's `taste.md`, `idea.md`, and `review.md` (it was accepted at the taste gate, so the bet that "rebase axis is real" passed taste — engage with where 185 actually died). (3) Argue in one sentence why per-pair QK-symmetric rotation would bind where 185's per-head K-only rotation did not, given 185 is strictly more expressive on the per-head axis and less constrained on the QK-symmetry axis. (4) Acknowledge the depth-axis null cluster and explain why a *rotational* per-layer parameter is qualitatively different from a *scalar* per-layer parameter — the rotational lever breaks QK^T orthogonality across pairs, which scalar per-layer levers (161/142/130) do not. A WIN here is informative; a NULL is also informative (it bounds the RoPE-modulation axis). The bet only earns a slot if the math is right and the prior-engagement is real.

- **Verdict: revise** — the default sketch is mathematically a no-op, the per-pair sketch overlaps 185 (just abandoned), and the bet doesn't engage the strongest priors. With the math fix + 185 engagement + depth-axis argument, this could re-pitch as a sharp per-pair lever; without those, the next round is a `reject`.

Routing: `needs-repitch` for the miner to fix the math and engage the priors.
