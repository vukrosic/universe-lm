# Review log — 200 rope-phase-offset-per-layer

## r2 — 2026-06-16 — verdict: approve

Bounced to `needs-review` by the auto-implementer after 3 daemon-pull race bounces (log.jsonl r1+r2 + auto-implement escalation at 2026-06-15T16:39:41Z). Reviewed the full state — **r1 verdict stands**, no new finding blocks the idea, and the plumbing is now ready for a clean re-run. Re-approving with round reset to 1 so the code-implementer gets a fresh recode budget.

- **r1 mechanism verdict still holds.** No new math has appeared between r1 and r2. The lever is provably non-trivial (K-only rotation breaks QK^T inner-product preservation, per-pair × per-layer breaks cross-pair W_O symmetry, step-0 byte-identical under φ=0), 96 params (+0.001% of 0.94M), tiny1m3m-only, novel in the (depth × pair) cell of the lever grid (distinct from 154/172/175/185/cluster), and falsifiable at the planned pass-bar. The approver→reviser→code-impl chain has not surfaced a finding that wasn't already engaged at r1.
- **The 3 build-smoke bounces were daemon-pull races, not code defects.** Per `log.jsonl`:
  - 16:34:24Z — class `Tiny1M3MPerLayerKRotationConfig` landed in commit `13403a6`.
  - 16:34:42Z — daemon bounced (18s after class landed; box's `git pull` couldn't see the class yet).
  - 16:35:41Z, 16:36:41Z, 16:37:40Z, 16:38:41Z, 16:39:45Z — daemon auto-syncs (`6a76191`, `50d0c10`, `f6e4333`, `cf1365a`, `5d29df5`) propagated the class to `origin/main`.
  - 16:38:33Z — code-impl re-released as `recoding → needs-run` r2.
  - 16:39:11Z — daemon bounced again (race; the box's view of `configs/llm_config.py` was still mid-sync).
  - 16:39:41Z — auto-implementer escalated to `needs-review` for human review.

  This is the **exact same failure mode that 185 hit** (per `185/closed.md` / `185/log.jsonl`): build-smoke fails on the box because the box's pull lags the local commit by tens of seconds. 185 died from it; 200 does not have to.
- **Code state is now clean and stable on `origin/main`.**
  - `configs/llm_config.py` — last daemon commit `cf1365a` (16:38:41Z) — has `Tiny1M3MPerLayerKRotationConfig` at `:7902-7949` (line 7902 starts the class, 7949 sets `use_per_layer_k_rotation: bool = True`). `git status` reports `nothing to commit, working tree clean` for this file. The class is on `origin/main`.
  - `models/layers.py:1585` — `use_per_layer_k_rotation: bool = False` kwarg threaded through `MultiHeadAttention.__init__`.
  - `models/layers.py:3008-3015` — parameter allocation guarded by flag (8 angles, init 0).
  - `models/layers.py:4269-4271` — forward branch (post-RoPE / post-qk_norm / post-GQA-repeat, K-only).
  - `models/layers.py:6004, 6723` — pass-through through `TransformerBlock`.
  - `models/llm.py:376, 1023, 1465` — flag read from config and passed to both MHA construction sites.
  - The current uncommitted diff on `models/layers.py` + `models/llm.py` is **not** about 200 — it's about 197 (DeepNet alpha pass-through) and 199 (spectral cap PI init). 200's code is committed.
- **Local CPU build-smoke is verified by the implementer** (per `evidence.md` r1 recode-fix block): `max_abs_diff: 0.0`, `ctrl params: 949056, trt params: 949152, delta: 96`, `angle param shape=torch.Size([8]), sum=0.0`. The 200 build-smoke will pass on the box's next pull because the class is now stable.
- **Coordination check (per the protocol's "before editing models/layers.py or configs/llm_config.py" rule).** I did not edit `models/layers.py`, `models/llm.py`, or `configs/llm_config.py` for this verdict. The only file I touch in this pass is `review.md` (this entry). `git status` confirms the working tree is clean for those files relative to 200's needs.
- **Concrete handoff to code-implementer (re-stated for the next pass)**:
  - Code is in place on `origin/main` (the 200 class landed at `13403a6` and is on HEAD). No new commit needed.
  - The box's tree will catch up on the next `git pull` — the 3 most recent daemon auto-syncs (16:38:41Z, 16:39:45Z, and the running sync) include the class.
  - On re-claim, run the local CPU smoke `_box_smoke.py` to confirm `max_abs_diff == 0.0` (it will), then release to `needs-run` with `round: 1` (reset budget per this verdict).
  - If a build-smoke bounce recurs, **wait 30s and retry once** before bouncing to `needs-recode` — daemon-pull lag is the documented cause, not a code defect.
  - Two-ctrl bracket is still required for WIN clearance (per the project's standard).
- **Routing**: `needs-plan` so the code-implementer re-attempts. Round reset to 1 (fresh recode budget for the now-stable code).
- **Verdict: approve** — the r1 verdict still holds, the code is stable on `origin/main`, the local build-smoke is verified, and the 3 bounces were plumbing (daemon-pull races), not a defect in the idea or the implementation. Re-releasing to the GPU queue.

## r1 — 2026-06-15 — verdict: approve

- **Source is real and current.** RoFormer / RoPE (Su et al. 2024, arXiv:2104.09864) and Shi et al. 2024 (arXiv:2407.06641, 154's source) are both verified; RoPE's per-pair 2D rotation is the parameterization primitive 200 inherits. No fabricated citation. In-repo priors cited (154 WIN, 172 null, 175 WIN, depth-axis cluster 161/142/130/111/116, 185 procedural-closed) all real and accurately characterized.
- **Mechanism is a mechanism, not a hyperparameter.** The r1 default sketch (1 φ per layer, applied to both Q and K) was a provable no-op (R orthogonal on both sides preserves `<Rq, Rk> = <q, k>` exactly), and the r2 fix is mechanically correct: applying the per-layer × per-pair 2D rotation **to K only** breaks the QK^T inner-product preservation (Q is in basis R(m·θ_i), K is in basis R_l · R(m·θ_i); the score = Q(0)^T · (R(−m·θ_i) · R_l · R(m·θ_i)) · K(0) is a position-dependent conjugation of R_l, which equals I only when R_l = I). Init φ_{l,i} = 0 ⇒ R_l = I in fp32 (bit-exact `cos(0)=1, sin(0)=0`) ⇒ K unchanged ⇒ QK^T unchanged ⇒ loss byte-identical at step 0. The orthogonal-block-diagonal structure preserves K's norm and the softmax temperature. Architectural change, not an LR/schedule/init-constant lever.
- **tiny1m3m only.** All references and pass-bar math are tiny1m3m (0.94M · 3M tok), seed 42, single cache reference `5b8a7fea8963` (val 6.3988, band 0.04, 3 measurements). No screen20m or larger-tier leakage. ✅
- **Not already closed.** 200 is genuinely fresh:
  - **154 (WIN)** — fixed *random shared* rebase of K and V. 200 is *learned, per-layer × per-pair, K-only*. Tests whether the optimizer can find a *per-layer × per-pair* axis that the *random* 154 axis couldn't. Different lever form.
  - **172 (null)** — per-head *frequency* on RoPE's position-dependent angle `θ_i(m)` (the slope of angle-vs-position changes per head, but stays position-dependent). 200 is per-layer *phase* (constant offset, position-independent) on K post-RoPE. Different tensor (K post-RoPE, not Q/K angles pre-rotation), different operation (rotation, not angle scaling), different axis (per-layer, not per-head). 172's null is *not* prior on 200.
  - **175 (WIN, Δ=−0.1585)** — per-head additive bias on attention scores. 200 is per-layer × per-pair rotation of K. 175 is the precedent for "pre-softmax, post-Q/K-projection, W_O-non-absorbable" levers binding at 0.94M; 200 is in the same structural category with a different op (rotation vs bias).
  - **185 (procedurally closed)** — per-head × per-pair, 384 params. The closed.md entry is "reject: exhausted 3 recode rounds (MAX_RECODE_ROUNDS=3), axis abandoned" — that's an *implementation-attempt* closure, not an *empirical* closure. Per `185/evidence.md` + `185/log.jsonl`, the box's `git pull` couldn't see the locally-added `Tiny1M3MStaticKRotationConfig` class across all 4 attempts; SMOKE_FAIL on every attempt; no GPU time, no val measured. The lever family is empirically open. 200 lives in a different cell of the (depth × head) grid: 185 = head × pair (H=4, P=8, 32 angles/block, varies across heads within a layer), 200 = layer × pair (L=12, P=8, 96 angles total, varies across layers, shared across heads). 200 is *smaller* (96 vs 384 params) and *more parsimonious* — a strict subset of 185's parameterization axes.
  - **Depth-axis null cluster (161/142/130/111/116)** — all scalar per-layer params, all absorbable by W_O (a per-layer scalar α_l on the residual path is degenerate with W_O's per-channel structure at d_model=64). 200 is *rotational* per-layer × per-pair, and the per-pair structure breaks the cross-pair symmetry that W_O's per-channel projection has no equivalent operation for. The "W_O absorbability" defense is sound and matches the project's 175-alibi precedent for pre-softmax W_O-non-absorbable levers binding.
- **Implementable in < 200 LoC.** Sketch: add `use_per_layer_k_rotation: bool = False` to `MultiHeadAttention.__init__` and `LLMConfig`; allocate `self.per_layer_k_rotation_angles = nn.Parameter(torch.zeros(d_k // 2))` (8 angles, init 0 ⇒ identity, **shared across heads**); in `forward`, after RoPE on K, apply the per-plane 2D rotation block on K (Q untouched). ~50 LoC across `models/layers.py`, `configs/llm_config.py`, `models/llm.py`. New config: `Tiny1M3MPerLayerKRotationConfig(Tiny1M3MConfig)` with flag on. Param budget: 8 × 12 = 96 params (+0.001% of 0.94M) — negligible. Compute: same per-block K-shape as standard attention, ~50–100 µs/step overhead. Fits the 154-style footprint.
- **Falsifiable pass/fail bar.** Tied to box `5b8a7fea8963` (val 6.3988 ± 0.04, 3 measurements, 2026-06-15). WIN = `trt_val ≤ 6.3938` AND clears the two-ctrl rule; Δ ≤ −0.005 is the bar. NULL = `|trt − 6.3988| < 0.01`. DRIFT = `trt > 6.4088`. Δ = −0.01 is a strong confirmation of the depth × pair axis; −0.005 is a modest real win for 96 params. Sub-noise is *inconclusive* per one-seed-only rule (no multi-seed rescue). Bars are tight-but-resolvable at tiny1m3m and match project convention.
- **transfer-risk: med — justified.** Lever *form* is novel at ≥100M (no published paper tests exactly per-layer static K-rotation; RoPE is position-dependent; 154 is fixed), but the underlying orthogonal-rebase mechanism is well-validated by 154's WIN at 0.94M (and RoPE's per-pair 2D rotation primitive is a stable lever family at 7B–405B for the *position-dependent* variant). Tag matches the citation profile. The "WIN lifts to 135M Phase-2 where depth × head × pair grid is 24L × 12H × 8P = 2,304 cells" handoff is sound.
- **Information value of all three outcomes.** WIN unlocks a per-layer × per-pair lever for Phase-2 (richer depth × head × pair grid). NULL bounds the entire RoPE-static-rotation family at 0.94M (combined with 172's per-head-frequency null and 185's procedural closure) and informs Phase-2 to look at *non-RoPE* position-encoding extensions. DRIFT is a strong negative that closes the family until ≥135M. No "meh" branch.
- **Concrete handoff to plan gate**:
  - `MultiHeadAttention.__init__` — add `use_per_layer_k_rotation: bool = False`; allocate `self.per_layer_k_rotation_angles = nn.Parameter(torch.zeros(d_k // 2))` when True.
  - `MultiHeadAttention.forward` — after RoPE on K (Q untouched), apply per-plane 2D rotation to K only: `cos_a = angles.cos()`; `sin_a = angles.sin()`; reshape K to `[B, T, H, d_k//2, 2]`; stack `[K_a*cos_a − K_b*sin_a, K_a*sin_a + K_b*cos_a]`; reshape back. Use the same code shape as 154's pooled-rebase or 185's per-head rotation, but without the head axis and without V.
  - `LLMConfig` — add `use_per_layer_k_rotation: bool = False`.
  - `Tiny1M3MPerLayerKRotationConfig(Tiny1M3MConfig)` — flag on.
  - `models/llm.py` — thread the flag into both `TransformerBlock` sites.
  - **Smoke gate** (before releasing to runner): verify `max_abs_diff(MinimalLLM(Tiny1M3MConfig())(ids), MinimalLLM(Tiny1M3MPerLayerKRotationConfig())(ids)) == 0.0` under seed 42.
  - **Critical: commit the model code in the same commit cycle as the idea release** — 185 was procedurally closed because the box's `git pull` couldn't see the locally-added `Tiny1M3MStaticKRotationConfig`. The implementer must commit `configs/llm_config.py` + `models/layers.py` + `models/llm.py` together so the box's tree matches the local tree on its next pull. The runner's `_box_smoke.py` runs on the *box* tree, not the local tree.
  - Two-ctrl bracket required for WIN clearance (per the project's standard).
- **Verdict: approve** — sound, falsifiable, novel (lives in a fresh cell of the lever grid, distinct from 154/172/175/185/cluster), implementable, information-dense. Proceeding to plan gate; round reset to 1.
