## r1 — 2026-06-15 — verdict: accept

- **Leverage**: 154-rebased-attn established the orthogonal-rebase axis as a record-breaker at 0.94M (Δ=−3.48, fixed random shared rebase of K and V). 185 is the natural learned generalization — per-head, K-only — and the *learned* axis is fresh at this tier. If it binds, it unlocks a per-head static-rotation lever for Phase-2 ≥135M where the per-head gradient signal is much richer than at 0.94M.
- **Information value (high in all 3 outcomes)**: WIN confirms learned per-head improves on fixed random → per-head basis specialization is real; NULL tells us 154's binding mechanism was the random rebase itself, not learnability; DRIFT (learned worse than none) is a strong negative that closes the axis. No "meh" outcome — every branch is informative.
- **Non-obviousness**: structurally novel — 154 was fixed-shared-on-both, 172 was per-head but position-dependent, 180 was logit smoothing, 176 was V-norm. Static (position-independent) per-head learned rotation on K only is not a re-run of any closed axis. Per-pair rotation is the same shape as RoPE, but without the position input — a sensible, named lever.
- **Niche fit**: mechanism (not HP), step-0 byte-identical via θ=0 init (orthogonal constraint preserves norms and dot products exactly), tiny1m3m-runnable, +384 params (+0.041%) negligible. transfer-risk: med is appropriately flagged — the lever form is novel at ≥100M but the underlying orthogonal-rebase mechanism is validated by 154's WIN at 0.94M.
- **Portfolio fit**: active queue (163/154/182/183/184) has no other rebase/orthogonal-rotation entries; this is the *next* natural probe of the rebase axis that 154 opened, not a redundant retread.
- **Crisp bet (one sentence)**: 154's fixed random orthogonal rebase gave Δ=−3.48 by breaking unfavorable QK-basis alignments; 185 tests whether the optimizer can find a *better* per-head basis than random at 0.94M — if it can, the per-head axis is real and lifts to 135M; if not, 154's binding was the random rebase itself.
- **Verdict: accept** — sharp, high-leverage, novel lever, fits the niche, informative in every outcome. Proceeding to definition gate (round reset to 1).
