# Taste log — 199 spectral-attn-output

## r2 — 2026-06-15 — verdict: accept
- **All four r1 findings closed cleanly.** Per-token-output branch dropped (single W_O branch). Per-block γ_l via `exp(γ_l)`, init 0 ⇒ byte-identical baseline, optimizer can loosen (γ_l>0) or tighten (γ_l<0). +12 params (+0.001% overhead). Sharp Δval window [-0.005, -0.012] with mechanism: σ_max(W_O) drifts 1.3-1.5× over the 92-step run, optimal γ_l ≈ -log(1.3..1.5) ≈ -0.26..-0.40, cap binds in late second half. The 160 distinction table is now load-bearing: intra-W_O projection Lipschitz constant (regularization axis) vs post-AV magnitude gain — different placement, granularity, gradient signal, inductive prior.
- **Information-value floor is concrete.** A clean null at 0.94M (|Δ|<0.005) closes the post-attention-shape null family at 5 deep (160/142/181/176/199). The family has now burned 4 slots for nulls inside the noise band; a 5th lets us retire the axis as "post-AV / per-head-attention-shape exhausted at 0.94M" with high confidence. Wrong-sign risk explicitly modeled (σ_max could shrink under AdamW damping at this scale → cap never binds → lever mechanically null but informative: closes the axis).
- **Distinction from 128-spectral-decoupling holds.** 128 modulates the update *direction* (gradient orthogonalization in optimizer space, null Δ=+0.10 wrong-sign, ~20× band); 199 modulates the forward *Lipschitz constant* of a specific projection (intra-W_O regularization axis, signal only fires when cap binds). Different mechanism, different placement, different backprop path — not a re-pitch of 128.
- **Niche fit is tight.** Mechanism (not HP), identity/zero-init-able (γ_l=0 ⇒ byte-identical), tiny1m3m-eligible (12 params, ~12K FLOPs/step power-iteration overhead), single-seed (42). The screen feeds the 135M recipe: W_O Lipschitz control plausibly opens an axis at larger scale even if 199 nulls here. transfer-risk: med is honest and supported.
- **Costs are bounded and the win/null asymmetry is favorable.** Worst case: 5-deep null closure, retires a family, ~3-5 minutes of GPU. Best case: Δval -0.005 to -0.012 on a structurally-distinct lever. The win window is bounded by the null band's upper edge (the 016-qk-norm WIN was -0.012 inside attention; 199 is a *softer* constraint on a *larger* projection, so its leverage should be at most equal).

**Verdict rationale.** r1's four concrete revision paths were all hit; r2 added bonus material (wrong-sign risk model, 5-deep family closure framing, 128 distinction). Pitch is now sharp, niche-fitting, and information-rich on either outcome. Worth the slot.

## r1 — 2026-06-15 — verdict: revise
- **Crowded family, walked into a closed axis.** The post-AV / per-head-attention-shape null family is already 4 deep at tiny1m3m: 160-rms-gain-per-head (Δ=-0.0023 inside band), 142-layerscale (Δ=-0.0012, 5th depth-conditional null), 181-cross-head-rmsnorm (Δ=+0.1722 wrong-sign), 176-v-pre-av-norm (Δ=+0.0303 inside band). The 128-spectral-decoupling null is the gradient-space twin of 199's W_O Lipschitz framing (Δ=+0.10 wrong-sign, ~20× noise band). The "spectral cap on attn output" branch of 199 is mechanically a sibling of 160 (per-head scalar init 1) — the miner even concedes "essentially 160-rms-gain-per-head closed null". To be worth a slot, 199 must be **structurally distinct** from 160 in a load-bearing way, not "Lipschitz interpretation" of the same lever. The miner needs to spell out what axis 199 opens that 160 didn't — e.g. constraint on the projection (W_O) rather than gain on its output is a real difference, but the current pitch doesn't cash that out into a prediction.
- **Asymmetric `min(1, cap/σ_max)` is structurally a damage-only lever.** The proposed 0-param formulation `W_O_eff = W_O · min(1, wo_spectral_cap / σ_max(W_O))` can only ever scale W_O down (or be a no-op). It has no learnable axis, so the optimizer cannot "discover" the right tightness. Power-iteration overhead runs every step to compute σ_max for a cap that almost certainly won't bind at 0.94M/12L (per 160: "gradient signal is too weak at 0.94M/3M tokens to push the gains away from 1"). Re-formulate with a learnable cap: e.g. `wo_spectral_cap = σ_max(W_O_init) · exp(γ_h_global)` with γ_h_global init 0 ⇒ byte-identical baseline, optimizer can loosen or tighten. Asymmetric is fine if the asymmetry is the bet, but the bet must be stated.
- **No sharp prediction.** The pitch says "a 199 WIN would mean Lipschitz control on W_O binds at 0.94M" but offers no magnitude. The closed sibling nulls all sit inside |Δ|<0.005; a realistic bet is also inside that band, which means the A/B is structurally low-leverage. Either: (a) name a concrete Δval window with mechanism (e.g. "σ_max(W_O) grows ~1.5× over the 92-step run, clipping to init should preserve attn-output gradient signal → Δval in [-0.005, -0.010]"), or (b) accept that the bet is sub-noise and add the information-value argument: a clean null at 0.94M would close the post-attention-shape null family at 5 deep. Either is acceptable; the current "vibes + walkthrough" is neither.
- **Branch to drop.** The early "per-token spectral norm on attention output" branch (out / (||out||_2 + ε)) is explicitly not byte-identical at step 0, which the miner already flags. Drop it; keep only the W_O spectral-norm-regularization branch, properly re-formulated with a learnable axis.

**Concrete revision path for round 2:**
1. Pick W_O spectral-norm regularization as the sole mechanism. Drop the per-token-output branch.
2. Re-formulate as learnable: `wo_spectral_cap_l = σ_max(W_O_init) · exp(γ_l)` per block, γ_l init 0 ⇒ byte-identical baseline; min(1, cap/σ) only engages if optimizer pulls γ_l < 0. Add 12 params (one γ_l per block, +0.001% overhead).
3. State a sharp bet: predicted Δval window + the *specific* mechanism that opens that window at 0.94M. Distinguish from 160: 160 modifies the post-AV *output* gain (magnitude axis on attention output, 48 params); 199 modifies the W_O *projection*'s Lipschitz constant (12 params, regularization axis on the projection itself). Different placements, different gradients — make this load-bearing.
4. Address the 4-deep null family head-on: is 199 a 5th null, or does Lipschitz control on W_O open something the post-AV gains didn't? If the answer is "5th null, but it closes the family", say so and price the bet accordingly.
