# Taste review — 195-qk-clamp-min-max

## r2 — 2026-06-15 — verdict: accept
- **r1's blocker is fully addressed.** The c=8 choice made the lever bit-identical to the closed tanh softcap (inactive at step 0; smooth tanh and hard min/max agree outside the clamp range). The r2 repitch moves to **c=2.0**, where the QK^T entries are O(1) Gaussian at Kaiming init and the 2-sigma tail (≈5% of logits) actually hits the clamp on step 0. The lever is now **not** bit-identical to baseline and **not** bit-identical to the closed softcap. The discontinuous-derivative property is genuinely *exercised* on the 5% of clipped logits, not theoretical.
- **Crisp bet, verified.** One sharp sentence holds: a *tight* hard clamp at c=2.0 is active at step 0 with a discontinuous gradient, testing whether the combined "active + discontinuous" regularizer effect of hard QK clamping lowers val loss — a different bet from the closed softcap (inactive + smooth). The null closes the *hard-clamp sub-axis* of the logit-bounding family; a mild win is a real mechanism. Both outcomes earn the slot.
- **Niche form fit:** 0 new params, identity-able (init passes through with a tight clipping window), mechanism not HP. Runs at tiny1m3m seed 42 with two clear pass bars (WIN: Δ ≤ -0.005, NULL: |Δ| < 0.01, DRIFT: Δ > +0.01). A DRIFT is informative too — it would mean the discontinuous gradient at c=2.0 is too aggressive for our scale, closing the *tight-clip* sub-axis with a clear mechanistic reason.
- **Portfolio fit:** the softcap closed the *smooth* sub-axis earlier this week; this is the *hard, active* sub-axis. One new sub-axis per family in a week is fine. No crowding.
- **Transfer risk: low** is correct. PaLM validates the *concept* of QK logit bounding at 8B-540B (tanh at c=50, a much looser clamp); the hard-clip implementation is a well-known variant. The 0.94M result is informative for the 135M recipe — logit bounding either generalizes or it doesn't, and the smooth-vs-hard distinction is a useful axis to know the answer to.
- **Defensive distinctness list is solid** — distinguishes from 184-logit-scale (LM head), 152-attn-logit-bias (smooth additive), 155-per-head-temp (scalar), 188-qk-rms-scaling (soft multiplicative). 195 is the only hard bound on pre-softmax QK^T in the queue.

**Action:** accept → `needs-review` (round reset to 1 for the definition gate's budget).
