## r1 — 2026-06-15 — verdict: accept

- **Mechanism is sharp.** Per-head scalar gate `γ_h = sigmoid(γ_raw_h)` with init `γ_raw_h = -10` ⇒ step-0 byte-identical to causal baseline. The arithmetic on the bidir-vs-causal mask blend (in the Mechanism block) is correct; only +48 params for H=4, L=12 (≈0.005% of 0.94M). The "head can grow into bidirectional" optimizer path is a real lever the optimizer can pull, not just a parameter that sits at zero.
- **Fresh axis vs. closed batch.** The 2026-06-04 batch (multiscale heads / parallel block / attn sink) closed at the *layer* level — some layers causal, some bidirectional within the same forward. 179 is genuinely different: mix at the *head* level within a single layer. The relevant prior is 152-attn-logit-bias (also per-head, also byte-identical) — but 152 was a per-head additive QK bias, which collapses into Q/K updates at 0.94M/12L/4H. 179's lever is multiplicative on the *mask*, which Q/K can't absorb; it's a structurally new axis, not a re-packaged bias.
- **High information value either way.** A clean null at tiny1m3m closes the per-head causal-mix axis (and corroborates 152's lesson that per-head axes don't differentiate at this scale, since head specialization hasn't crystallized yet). A win unlocks the hybrid-head family for Phase-2 ≥135M where per-head gradient signal is larger. The byte-identical step-0 makes the A/B variance-clean. Both outcomes are log-worthy.
- **Crisp bet holds up.** "Per-head learnable bidirectional access during training is a fresh axis vs. the closed layer-level multiscale sweep, and could give the model richer structural features without committing the whole layer to bidirectional." Single sentence, named axes, named contrast with the closed batch. Passes the vibe bar.
- **Train/inference γ flip is a known concern but not fatal here.** Yes, heads trained bidirectional will have to switch to causal at inference — this is a real distribution shift, and the closest analog (XLNet permutation LM) only works because the permutation is *fixed* across train and eval. 179 doesn't have that. But the byte-identical step-0 + tiny param count + the closed-batch contrast is what makes the *null* itself informative: if the optimizer can't even find a useful γ_h to grow despite a 50× weaker no-future signal at step 0, that's evidence the train-time leakage isn't translating to causal features at this tier. The test is cheap and the null result is interpretable.
- **Transfer-risk is med, with mechanism that's scale-free.** Mask shape doesn't change with scale. Phase-2 ≥135M is where per-head gradient signal grows; if 179 wins even weakly at 0.94M, carry it forward. If it nulls here, the axis closes for the project.
- **Portfolio check passes.** Recent queue (174-182) is attention-shape heavy but not per-head *mask*-mix — neighbors are RoPE decay, ALiBi slopes, talking-heads mixing, MQA gates, per-head window. None of them touch the causal-mask axis. 179 is orthogonal.

Verdict: **accept**. Mechanistic step-0 baseline is clean, the lever is genuinely fresh vs. the closed layer-level batch, and the test is information-dense at minimal cost. The train/inference γ flip is a concern but not a taste-bar issue — that's the definition gate's job to either parameterize the eval-time override explicitly or document the inference-time γ_h = 0 schedule as part of the run.
