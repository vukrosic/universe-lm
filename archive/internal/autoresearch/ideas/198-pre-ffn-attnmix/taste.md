## r1 — 2026-06-15 — verdict: accept
- **Novel axis (within-block, pre-FFN)**. Closed cross-block carries closed the *cross-block* axis (164-Q null Δ=+0.0360 wrong-sign, 168-AV null Δ=−0.0227 inside band), 021-V was the lone cross-block WIN. 198 is the *fourth* axis in the family but the only one that is *intra-block* and operates on the FFN-input side rather than the attention-input/output side. 021-WIN does NOT close this: V was carried across blocks, not into the FFN within a block. The queue's other "carry / mixing" items (191 token-gain, 196 block-EMA, 197 tied-W_O, 199 spectral-cap) are all post-attention or block-level — none plumb attention into the FFN. 198 is the only pre-FFN candidate. Net: portfolio crowding is shallow (mechanistic place is empty, not the 5th duplicate).
- **Crisp two-sided bet**. "WIN ⇒ intra-block attention-FFN information flow is binding at 0.94M/12L; NULL ⇒ the FFN's residual-stream input is sufficient, and the binding lever is not here." Both outcomes close real axis: WIN opens pre-FFN cross-stream conditioning as a lever family (FiLM-style); NULL retires it cleanly without leaving a 5th re-pitch on the queue. Sharp enough — the "vibe" failure mode (no expectation) is dodged.
- **Clean lever design**. 12 learnable scalars (γ_raw → sigmoid), init at -10 ⇒ sigmoid ≈ 4.5e-5, .detach() on the raw attention output (one-way conditioning, cleaner attribution, no early-step gradient interaction with the attention path), bit-identical step-0 by construction. The detach is a thoughtful choice — without it, γ would have to compete with the attention path's own gradient signal at init, which is the classic "γ collapses to 0 and stays" failure. Detached-side-channel γ has a clean separable gradient on FFN-side loss only, so it can actually learn to use the side channel.
- **Niche fit, no scale-axis worries**. Mechanism is structural (FiLM-style conditioning, Perez et al. 2018) — not tied to tiny-vocab/3M-token regime/embedding dominance. Doesn't exploit d_model=64 or 12L shallowness. The 021 result (cross-block V carry works at 0.94M) is the closest analog and it transferred through the family's general residual-mixing thesis; 198 is narrower but in the same family. `transfer-risk: med` is right — not "low" (no published LLM validation at this exact form) but not "high" (no scale-bound gimmick).
- **Param cost trivial** (12 scalars, +0.001% of 0.94M), pass-bar the only signal we need. A 021-style magnitude (-0.02 to -0.04) would comfortably clear the |Δ|>0.01 pass-bar; a NULL inside band is a clean retire.

Accept. Route to definition gate at tiny1m3m, seed 42.
