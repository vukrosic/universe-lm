## r1 — 2026-06-15 — verdict: approve

- **Sources verified.** 016-qk-norm WIN line in `closed.md` (val 6.3906, Δ −0.0138/−0.0185 ≫ 0.0047 gap, 2026-06-09); 161-dyt-temp DRIFT line in `closed.md` (trt=6.5150 vs fresh ctrl mean 6.4320±0.04, Δ=+0.0830, 2026-06-14, "closes per-layer-temp axis at 0.94M"); μP/μTransfer (Yang 2022, arXiv:2203.03466) and Pythia (Biderman 2023, arXiv:2304.01373) are real, current, and the mechanism claims (per-layer logit-temperature, exp-parameterized, init=1) match the published recipes. DeepNet α = 1/sqrt(2L) (Wang 2022) is the fixed-rule depth-conditional analog. No fabrication.
- **Mechanism is a mechanism, not HP.** Per-block learnable scalar `s_l = exp(s_param_l)` on the post-016-QK-norm scores. Init `s_param_l = 0` ⇒ `s_l = exp(0) = 1.0` exactly in IEEE 754 ⇒ `scores * 1.0 = scores` byte-identical to 016-alone at step 0. Structural in where the parameter lives and what signal it sees (post-norm QK^T, not raw Q/K magnitude). Exp-parameterization guarantees positivity — a negative scalar would invert the softmax. Not a learning-rate / init-constant lever.
- **Distinct from 161 (the closed DRIFT analog).** 161 sits on the plain baseline where `|Q|` and `|K|` are un-normalized — `s_l` fights the canonical `1/sqrt(d_k)` prior AND the Q/K gradient-magnitude noise. 188 sits on 016-norm where Q/K are RMS-normalized per head with per-head gain — `|QK^T|` is bounded and centered around 1 per dim, so `s_l` operates on a *post-norm signal* (re-sharpening/flattening whatever the norm left behind), not on a magnitude-noise prior. Different gradient landscape; different falsifiable bet.
- **Not a duplicate of any closed lever.** Cross-checked `closed.md`: 016 (QK norm, WIN), 152 (per-head additive bias, null), 155 (per-head temp, null), 161 (per-block temp on plain, DRIFT), 162 (Q-only RMSNorm, null), 165 (K-only RMSNorm, null), 169 (QK norm depth placement, null), 184 (LM-head output logit scale, distinct axis), 180 (logit depthwise conv, distinct). 188 = per-block scalar STACKED on the 016 WIN — not a math duplicate. It is the only lever in the queue that combines a closed WIN normalization with a learnable temperature.
- **tiny1m3m only.** All numeric references are 0.94M / 3M tok / seed 42. No screen20m, no ladder, no multi-tier promotion. Two-ctrl rule acknowledged in the WIN bar (`AND clears two-ctrl rule`).
- **Implementable in < 200 LoC.** ~15-20 LoC total: one new `use_qk_rms_scaling: bool = False` flag on `LLMConfig` (next to `use_per_layer_temp` at `configs/llm_config.py:157`); one kwarg on `MultiHeadAttention.__init__` + ~3 lines for `self.qk_rms_param = nn.Parameter(torch.zeros(1))`; one `scores = scores * self.qk_rms_param.exp()` line in `forward`; one `or self.use_qk_rms_scaling` addition to the manual-path forcing list; one `Tiny1M3MQKRMSConfig(Tiny1M3MQKNormConfig)` subclass mirroring the 016→169-depth pattern at `configs/llm_config.py:6454`. Plumbing is symmetric to existing 016/169 implementations.
- **Falsifiable bar with a real control.** ctrl = `Tiny1M3MQKNormConfig` (016 WIN, val 6.3906). WIN: `trt_val ≤ 6.3851` AND clears two-ctrl. NULL: `|Δ| < 0.01`. DRIFT: `trt_val > 6.4006`. Δ ≤ −0.005 is realistic — 016 itself was Δ=−0.0185 vs the prior baseline, so the WIN threshold is conservative for a stacked WIN-amplification bet.
- **Transfer-risk: low is justified by the `## Scale evidence` section.** Per-block learned scalar is a recognized depth-conditional lever with μTransfer (10M–13B, Yang 2022) and Pythia (70M–12B, Biderman 2023) validations; stacking on a normalization layer is a published μP recipe (init=1, exp-parameterized). DeepNet α = 1/sqrt(2L) (200–1000L, Wang 2022) is the fixed-rule analog — the *learned, per-block* version is the natural follow-up. Largest demonstrated scale (10M–13B μTransfer) is well above the 0.94M test tier. Tag matches the citation pattern (per-block + stacked-on-norm, both ≥10M validated).
- **Null is informative and distinct from 161.** A NULL (`|Δ| < 0.01`) or DRIFT (`trt_val > ctrl_val + 0.01`) at 0.94M would close the *post-norm* per-block-temp axis — and the diagnosis is "reason (b) of 161's close is binding (no scale-depth at 0.94M/12L for a per-block lever to exploit), regardless of normalization." That is a *different* axis closure than 161's "per-block temp fights the magnitude-noise prior on the plain baseline." A WIN amplifies the 016 WIN.
- **Bit-identity and SDPA path handled correctly.** `nn.Parameter(torch.zeros(1))` is the standard re-zero pattern; the `or self.use_qk_rms_scaling` addition to the manual-path forcing list is the same control 161 uses to keep step-0 numerics stable. exp(0) is 1.0 in IEEE 754, so `scores * 1.0 = scores` is bit-exact. Baseline path stays off-by-default and bit-identical.
- **Niche fit and portfolio fit.** Mechanism-shaped, identity-able, fits tiny1m3m, fits the 135M recipe *if* it binds. Per the taste log: 192-topk-attn, 193-mup-init, 197-deepnet-α are also in needs-taste but are different families (structural sparsity, init family, init-time only). 188 is the only one combining a closed WIN with a learnable scalar — not crowded.
- **No findings blocking. Verdict: approve → needs-plan, round reset to 1 for the code gate's own budget.**
