# Review log — 193 blockwise-attn-temp-schedule

## r3 — 2026-06-16 — verdict: approve

- **The `auto-implement` agent violated protocol by auto-`reject`ing this idea.** PIPELINE.md is explicit: "Doers never close — the reviser and code-implementer bounce blocked ideas back to a `needs-*` queue, not to `rejected`." `rejected` is a terminal state set by the taste-reviewer or the definition reviewer. The `auto-implement` (a doer-adjacent agent) flipped the idea from `needs-review` to `rejected` at 00:41:00Z with the note "auto-rejected: blocked 3x with no path forward (see log)" — but a blocked doer should have bounced to `needs-recode` (so the code-impl could fix) or stayed in `needs-review` (so the reviewer could arbitrate). Closing an idea on the doer's authority, on infrastructure grounds, is outside the agent's mandate. The reviewer is overriding that flip and routing the idea back to `needs-plan` for the daemon's next clean pull.
- **The idea is NOT in a "no path forward" state — the code is correct, in place, and the failures are all infrastructure.** Local HEAD is verified end-to-end: (a) `Tiny1M3MBlockTempConfig` is a proper `@dataclass` subclass at `configs/llm_config.py:7793` with `use_block_temp_schedule: bool = True` and `block_temp_alpha: float = -0.3`; (b) `MultiHeadAttention.__init__` (layers.py:831) accepts `tau_b: float = 1.0` and registers a non-Parameter `Buffer` of shape `[1]` when the flag is on (layers.py:2698-2700); (c) the manual-path forcing list at layers.py:5234 includes `or self.use_block_temp_schedule` so SDPA flash doesn't perturb step-0 numerics; (d) the pre-softmax `scores = scores / self.tau_b.view(1, 1, 1, 1)` is at layers.py:5298-5299; (e) `MinimalLLM`'s `TransformerBlock` call site at llm.py:1876-1879 plumbs the kwargs through with `getattr(config, ..., default)` fallbacks for back-compat. Build-smoke signature is correct (949,056 params + 12 τ_b Buffers per the r2 schedule `[0.7, 0.71, 0.75, 0.80, 0.88, 0.96, 1.04, 1.12, 1.20, 1.25, 1.29, 1.30]` for blocks 0..11) and the code-impl's CPU build verified this on 2026-06-15T16:47Z and again on 2026-06-16T00:36Z.
- **All four `auto-implement`-counted "blocks" are infrastructure, not idea.** Walking them in order: (1) 2026-06-15T16:41:18Z daemon `ImportError: Tiny1M3MBlockTempConfig` — first-attempt code-impl's edit hadn't been pulled to the box yet (stale-box import race). The code-impl re-shipped at 16:47:15Z and re-verified locally. (2) 2026-06-15T16:50:16Z daemon `no smoke output (scp/connection failed)` — scp-side network failure on the box, has nothing to do with the idea. (3) 2026-06-15T16:56:30Z daemon `no smoke output (scp/connection failed)` — same scp-side failure mode. (4) 2026-06-16T00:39:26Z daemon `ImportError: Tiny1M3MBlockTempConfig` — second-attempt code-impl shipped at 00:36:32Z, the daemon's auto-sync commit landed at 00:39:05Z, the build-smoke failed 21 seconds later. That's a daemon→box pull race (the smoke ran before the box saw the new commit), not a code defect. Two of four blocks are pure network; the other two are sync-timing artifacts that resolve themselves on the next clean pull. The 0/4 code-correctness failures is the strong signal here.
- **The "no path forward" framing is wrong because the path forward is "let the daemon retry on its next clean pull."** The `needs-recode` path is for the code-impl to fix code; the code is correct, so the next step is the daemon's natural re-claim cycle. The 188-conditional is also already resolved cleanly: 188-qk-rms-scaling is `rejected` per `closed.md` (exhausted 3 recode rounds, 2026-06-15), so the *learned* per-block scale axis is empirically closed at 0.94M, and 193 is the cleanest possible *fixed-shape* follow-up — the conditional-null result still closes the entire per-block scale axis (learned + fixed both fail) at 0.94M. No design-blocker, no implementation-blocker, just an infra-race window.
- **3-round cap note.** Frontmatter `round: 1` (preserved from the r2 approval's reset). The cap is at `round: 3` and forces a decision — we're at 1, so I have a full budget. Verdict: `approve` again, since the spec is sound, the code is verified, and the doer's auto-reject was the protocol violation, not the idea. The `auto-implement` agent's behavior is itself a finding (worth surfacing to whoever owns the runner harness), but the right place to record that is a runner-harness ticket, not a `closed.md` line on this idea.
- **Verdict: approve** → `needs-plan`, round reset to 1. Sending back to `needs-plan` re-queues the (verified) `_arq_193-blockwise-attn-temp-schedule.py` stub and the in-HEAD `Tiny1M3MBlockTempConfig` for the daemon's next clean pull. If the daemon hits the same `ImportError` on its next attempt, that's a box-pull race to fix in the daemon's auto-sync flow (the `3743ef5` auto-sync commit landed 21s before the smoke — the daemon should wait for a clean box-side `git log` to show the commit before kicking off the smoke), not a re-code. The runner-harness owner should look at the daemon's `scp/connection` failure pattern from this run as a separate ticket.

## r2 — 2026-06-16 — verdict: approve

- **Spec is unchanged from r1 approval.** `idea.md` was not edited between r1 (2026-06-15T12:21) and the current bounce (2026-06-15T16:56). The mechanism, sign convention, α=−0.3 commitment, 188-conditional framing, bit-identity-at-α=0 argument, scale evidence, and pass/fail bar all hold.
- **All r1 planning findings addressed in `plan.md`.** Walking through the five r1 findings (a–e): (a) ctrl reference ambiguity — `plan.md` pins ctrl to `Tiny1M3MConfig` (val 6.3988 ± 0.04 from `autoresearch/baseline-cache.json`, n=3, 2026-06-15) and notes the daemon owns the baseline cache; the test is "does the schedule help over the no-schedule ctrl", not "does it beat a particular historical champion." (b) Schedule buffer placement — implemented as a non-Parameter `Buffer` of shape `[1]` per MHA in `MultiHeadAttention.__init__`, transferred with `state_dict()`; the per-block formula is computed inline in the `nn.ModuleList` comprehension in `models/llm.py:1238ish`. (c) Manual-path forcing — `or self.use_block_temp_schedule` is added to the existing forcing list (per plan, mirrors the 195 `or self.use_qk_clamp` placement); α=0 short-circuit keeps the division in the path so step-0 is provably bit-identical. (d) 188-conditional at the runner — `plan.md` explicitly states the runner's commit-handover should consult `closed.md` / `baseline-cache.json` for 188's status before queueing 193 (and 188 is currently `rejected` per `autoresearch/closed.md` — closed by exhausted recode rounds, 2026-06-15 — so 193's conditional resolves to "the learned axis already closed, the fixed-shape axis is still informative as a control"). (e) α=−0.3 only — single value committed, no HP sweep. ✓ All five resolved.
- **Code is verified locally per the r2 recode log entry.** The code-impl's `2026-06-15T16:47:19Z` recoding note reports: `Tiny1M3MBlockTempConfig(α=-0.3)` constructs (949056 params + 12 τ_b buffers), schedule matches r2 formula `[0.7000, 0.7122, 0.7476, 0.8035, 0.8754, 0.9573, 1.0427, 1.1246, 1.1965, 1.2524, 1.2878, 1.3000]`; ctrl path bit-identical (0 overhead, no buffers). The stub `_arq_193-blockwise-attn-temp-schedule.py` is at the repo root with the top-level `C` driving `train_llm`. `run.json` is in place. This is a real code drop, not a plan/code drift.
- **The build/run failures were infrastructure, not spec.** The r1 runner `ImportError: Tiny1M3MBlockTempConfig` (16:41) was a stale-box HEAD issue that the code-impl fixed in the r2 recode. The two subsequent `no smoke output (scp/connection failed)` failures (16:50, 16:56) are runner-side network problems (scp to the box, no smoke output captured) — they have nothing to do with the idea's mechanism, sign convention, plan, or code. The box is the bottleneck, not the idea.
- **Re-approve → `needs-plan` with round reset to 1.** The spec is sound and the plan is complete. Sending back to `needs-plan` lets the code-implementer re-queue the (verified) `_arq_193-blockwise-attn-temp-schedule.py` stub for the daemon once the box's `scp` path is healthy. The 188-conditional resolves cleanly because 188 is closed (exhausted 3 recode rounds per `autoresearch/closed.md`), so 193's "fixed-shape vs 188's-learned-shape" control axis is fully informative — the learnable per-block axis is empirically closed at 0.94M, and a fixed-shape cosine schedule is the cleanest possible prior against the empty optimization-schedule. The conditional-null result still closes the entire per-block scale axis (learned + fixed both fail) at 0.94M, exactly as r1 framed it.
- **Not a duplicate.** The `{depth-varying × multiplicative × fixed-shape}` cell of the design plane is still unoccupied. The 188 sibling (per-block learnable scalar, rejected via recode cap) is the closest closed neighbor; 193 is its fixed-shape control. 175 (depth-uniform *additive* WIN), 155 (per-head learnable multiplicative NULL), 161 (per-layer learnable multiplicative DRIFT), 169 (placement NULL), 130-ReZero (depth-conditional *residual* init NULL) are all orthogonal cells. The bit-identical-at-α=0 baseline (the ctrl ctrl) is still the active champion at 6.3988 ± 0.04, so a clean A/B at α=0 vs α=−0.3 will resolve the axis in a single seed-42 run per the protocol.
- **Verdict: approve** → `needs-plan`, round reset to 1. The code-implementer should re-queue the existing plan/code on the daemon's next clean pull — no plan or code changes needed. If the daemon's `scp/connection` failures persist on the next two attempts, treat it as a box issue and route to whoever owns the runner harness, not the idea.

## r1 — 2026-06-15 — verdict: approve

- **All three r1 taste findings closed.** Sign convention resolved to the standard `τ<1 ⇒ sharper, τ>1 ⇒ softer`; α = −0.3 committed with a mechanistic argument (consistent with 175-alibi WIN's locality-rewarding additive bias, now on the multiplicative depth-varying side); 188-conditional framing is explicit ("if 188 wins ≥−0.005, redirect 193 — depth-conditional scale axis already captured; if 188 nulls, 193 isolates the fixed-shape axis"). The `## Source`, `## Mechanism`, `## Design sketch`, `## Scale evidence`, `## Why it's worth a slot`, and `## Pass/fail bar` sections are all present and aligned with the protocol's required structure.
- **Source is real and current.** Press et al. (ALiBi, arXiv:2108.12409), Su et al. (RoPE, arXiv:2104.09864), Roy et al. (blockwise-local attention, 2019/2020) all check out. The in-repo references (175-alibi WIN, 188-qk-rms-scaling planning, 155-per-head-temp NULL, 161-dyt-temp NULL) are all real and correctly cited. The "cascaded / curriculum attention" literature pointer is honestly flagged as "no canonical paper found" — that's the right epistemic posture.
- **Mechanism is a mechanism, not a hyperparameter.** The lever is a structural cosine-depth schedule on the attention score scale. α is committed (single value), and step-0 at α=0 is bit-identical to baseline (`τ_b = 1` for all b, so `scores / (1·√d_k) = scores / √d_k`). This is a real architectural axis, not an LR/schedule/init-constant knob.
- **Tiny1m3m only, seed 42.** ✓ No `screen20m` or multi-tier promotion referenced. The conditional stop logic ("if 188 reports WIN ≥−0.005 before 193 runs, redirect 193 to a different axis") is correctly framed as a single-tier concern.
- **Not already closed.** The {depth-varying × multiplicative × fixed-shape} cell on the design grid is unoccupied. 155-per-head-temp NULL (per-head, learned, multiplicative), 161-dyt-temp NULL (per-layer, learned, multiplicative), 175-alibi-slopes WIN (depth-uniform, additive, fixed), 188-qk-rms-scaling (per-block, learned, multiplicative — needs-run), 189-cos-attn (linear-attention family — different lever), 190-per-layer-qk-norm (different axis, needs-plan), 169-qk-norm-depth NULL (placement axis). 193 is a clean cell.
- **Implementable in <200 LoC.** Plumbing precedent exists (`use_qk_rms_scaling` in models/llm.py:651, propagated to MHA at lines 1015/1394). The new code path is: (1) two flags on `LLMConfig` (`use_block_temp_schedule: bool`, `block_temp_alpha: float`), (2) a `Tiny1M3MBlockTempConfig` subclass that sets both flags, (3) one Buffer of shape `[L]` cached on the model holding `τ_b` (or computed in-line in attention forward), (4) division of `scores` by `τ_b · √d_k` in the manual attention path, (5) add `self.use_block_temp_schedule` to the manual-path forcing list so SDPA flash doesn't perturb α=0 step-0 numerics. ~30–50 LoC; fits well under budget.
- **Falsifiable pass/fail bar.** WIN / NULL / DRIFT taxonomy is clean: `WIN ⇒ trt_val ≤ ctrl_val − 0.01 AND clears two-ctrl rule`; `NULL ⇒ |trt_val − ctrl_val| < 0.01`; `DRIFT ⇒ trt_val > ctrl_val + 0.01`. Predicted band Δval ∈ [−0.01, −0.05] is realistic and well-anchored on 175's −0.1585 (depth-uniform additive reference) × multiplicative-depth-varying's smaller lever-amplitude argument. Conditional null closes the depth-conditional scale axis decisively (the right single-tier null result).
- **Transfer-risk: low, justified.** Lever is fixed-function: `τ_b = 1 + α·cos(π·b/L)` is a function of (block index, n_layers, scalar α) — no model-size dependence. ALiBi validated 0.4B–6.7B (Press et al. 2022); the multiplicative depth-varying analog is novel in-repo but literature-supported (cascaded-attention / annealed-attention ablations). The low tag is correctly assigned and the `## Scale evidence` section is present.
- **Plan-implementer findings (none blocking approve, all to be addressed at planning):**
  - **(a) Ctrl reference ambiguity.** The pass-bar text lists three different ctrl val numbers — `6.4216` (early baseline), `6.4044 / 6.4091` (bracket), `6.3988 ± 0.04` (current 175-armed champion per `autoresearch/baseline-cache.json` val_mean=6.3988, noise_band=0.04, n=3, measured 2026-06-15T12:20Z). The code-implementer must pin ONE. **Recommended**: pin to the in-session `Tiny1M3MConfig` (val = whatever this run's two-ctrl bracket measures) — the test is α=0 vs α=−0.3 on the *same* model, so the bar is "does the schedule help over the no-schedule ctrl", not "does it beat a particular historical champion." The two-ctrl rule then validates the box.
  - **(b) Schedule buffer placement.** Compute `τ_b` once and register as a `Buffer` of shape `[L]` on `MinimalLLM` (or on the MHA) rather than per-step cosines. One allocation, transferred with model state, zero per-step compute. The α=0 short-circuit (return `τ_b = 1` everywhere, skip the division entirely) keeps the step-0 numerics truly bit-identical without even touching the scores.
  - **(c) Manual-path forcing.** SDPA flash has no per-block temperature knob. With α≠0, the manual path is mandatory. With α=0, SDPA flash would skip the division; bit-identity holds iff scores are still divided by 1·√d_k — i.e. the manual path must run unconditionally whenever `use_block_temp_schedule=True`, *and* the α=0 short-circuit above must still go through the division (otherwise SDPA flash's scores/√d_k vs the manual path's scores/(1·√d_k) could differ in fp32). Recommendation: manual-path force `or self.use_block_temp_schedule`, AND keep the `τ_b=1` division in the path so step-0 is provably identical.
  - **(d) 188 conditional at the runner, not the planner.** 188-qk-rms-scaling is at `needs-run` (val_pending). The plan should NOT block 193 on 188's outcome (the idea is conditionally informative, not gated). The runner's commit-handover should consult the closed.md / records.jsonl entries — if 188 has reported a WIN ≥−0.005 by the time 193 is committed to the queue, redirect 193 to a different axis per the conditional; otherwise run as planned. State this explicitly in `plan.md` so the runner isn't surprised.
  - **(e) α = −0.3 is the only committed scalar.** Don't introduce a hyperparameter sweep (`α ∈ {-0.1, -0.2, -0.3, -0.5}`) — the one-slot budget is a single A/B. If the single α nulls, the next move is a sharper `plan.md` for a follow-up idea, not multiple trt runs in this slot.

- **Verdict: approve** → `needs-plan`, round reset to 1 (preserved; the idea already arrived at round 1 from the taste gate).