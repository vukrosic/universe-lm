# Taste log — 190 per-layer-qk-norm

## r1 — 2026-06-15 — verdict: accept
- **Sharp attribution bet.** The closed.md lineage (162-Q-only NULL, 165-K-only NULL, 169-depth NULL, 016-QK per-head WIN Δ=−0.0138) leaves an open question: was 016's WIN driven by the *block-level* symmetric QK RMS scale or by *per-head* γ specialization? 190 directly tests block-level vs head-level granularity by sharing γ across heads within a block (12 scalars vs 48). Both branches are informative: 190-WIN confirms the binding axis is block-level (and 016's 48 gains were over-parameterized); 190-NULL confirms the per-head axis carries the binding signal and shared-γ throws it away.
- **Clean step-0 identity, niche fit is exact.** γ_b=1.0 ⇒ `RMSNorm(Q)·1 = RMSNorm(Q)`, bit-identical to 016's γ_h=1.0 init at step 0. Mechanism-shaped (not HP), identity-able, param delta is 4× fewer (12 vs 48, +0.013% of 0.94M). Lever is strictly coarser than 016 — no risk of the "needs more depth/heads" tier-mismatch pattern that closed 162/165/169 with a finer axis.
- **Transfer to 135M is low-risk.** Sharing γ across heads within a block is a *more conservative* parameterization than 016, not a niche one — if the block-level axis binds at 0.94M/12L/4H, it should bind at 135M/24+L/12+H (more heads per block, sharing gets *more* useful, not less). The argument 016 WIN is block-level → block-level-γ carries to 135M is cleaner than 016 WIN is head-level → per-head-γ carries to 135M. Either 190 result is forward-useful for the 135M recipe.
- **Not portfolio-crowded.** The 4 closed QK-norm variants (016/162/165/169) all sit on the *Q/K-side* or *per-depth* axes. 190 is the *grain* axis (per-head vs per-block) — a new orthogonal dimension. Queue is dominated by QK-attn-shape variants (189, 191, 192) but those test different mechanisms (cosformer linear, token-attn gain, pre-RoPE rotation). 190 is the parameter-golf question on 016, not a 5th-same-family HP tweak.
- **No taste-reject signal.** Transfer-risk: low (correctly tagged), scale evidence present (016 at tiny1m3m + QK-norm validated at LLaMA/Gemma-2/Qwen-2.5 ≥7B), no closed duplicates by spirit, bet is one sharp sentence ("shared γ matches 016's WIN magnitude ⇒ block-level axis binds; shared γ nulls ⇒ per-head axis binds"). Pass.
- **What to flag for the definition gate.** (1) Pass bar should target matching 016's WIN magnitude (Δ ≤ −0.01 vs the 016-equipped ctrl, not vs the no-016 baseline) — otherwise 190 passing only "tied 016" is uninformative on the attribution question. (2) Use the 016-equipped ctrl as the comparison, not a fresh no-016 baseline, so the A/B isolates the *granularity* axis. (3) Confirm step-0 byte-identity to the 016 path with γ=1 per head; the bit-equivalence claim is the lever's only safety net.
