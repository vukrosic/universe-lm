# Review log — 150 Cross-Layer Feedback Attention

## r3 — 2026-06-14 — verdict: reject
- **Round cap binds.** Frontmatter `round: 3` ⇒ per `prompts/idea-reviewer.md` §3 the only legal verdicts are `approve` or `reject`. The idea reached this gate via the `auto-implement` escape after the code loop gave up — runner logged `"3 failed attempts in a row (r1=11.39, r2=9.77, r3=11.89) — implementer must call: cap reached"` (log.jsonl line 14).
- **Mechanism is structurally destabilizing at our only tier.** Three distinct stabilization fixes have been tried and all diverged at tiny1m3m (d_model=64, 12L), seed 42:
  - r1 raw cross-attn + zero-init gate → trt val 11.39 (Δ +4.96, ~125× DRIFT band; step-0 bit-identity itself failed)
  - r2 `mem.detach()` cut the cross-block gradient cascade → trt val 9.77 (Δ +3.33, ~83× DRIFT; step-0 OK, mid-training explosion at step ~100)
  - r3 `tanh(xlayer_gate)` bound on the gate → trt val 11.89 (Δ +5.45, ~136× DRIFT; step-0 OK, diverges at step ~200)
  The trajectory is consistent: the cross-attn path always opens, and at d_model=64 its output dominates the residual stream regardless of whether the gradient cascade or the gate magnitude is bounded. There is no 4th fix in the spec's design space that hasn't already been tried.
- **The proposed escape route is out of scope.** The round-3 evidence note (`evidence.md:81-83`) concludes "the d_model=64 / 12L regime is hostile … if a future 135M tier exists, retry 150 there as a fresh idea." Per `PIPELINE.md` Hard Rules: **🔴 ONE TIER ONLY — tiny1m3m**. There is no 135M tier; an idea whose payoff only appears at larger scale is a `reject` at the taste gate, and a thrice-failed mechanism arriving at the definition gate after a recode-cap escape inherits the same answer.
- **Approving would not be productive.** `approve` resets `round=1` and sends to `needs-plan` for a fresh code spec, but the implementer has already exhausted the spec's stabilization levers (gate init, gate bound, gradient detach, init-RNG cleanup). The next code-implementer pass would have no new mechanism to encode beyond ones already tried; it would re-bounce on round-1 of the recode budget. Cost without yield.
- **Closest neighbor was already null.** `closed.md` records `116-hyper-connections` (same family — cross-layer information flow at d_model=64) null at this tier; the round-1 review note in idea.md (line 30) cited this as the precedent to beat, but the empirical answer is that *both* linear-mixing (mHC) and attention-weighted-selection (this idea) fail to amortize at d_model=64. The cross-layer-info-flow axis is now closed at tiny1m3m by two independent mechanisms.
- **Close action:** move `autoresearch/ideas/150-xlayer-feedback/` to `autoresearch/ideas/_closed/`, append the closed.md line per prompt §3, leave the dirty `models/xlayer_attn.py` + `models/layers.py` + `models/llm.py` + `configs/llm_config.py` working-tree changes for the user (per `feedback-dont-push-without-approval` — no auto-push, no auto-revert; user decides whether to keep or roll back the unstaged xlayer code).
