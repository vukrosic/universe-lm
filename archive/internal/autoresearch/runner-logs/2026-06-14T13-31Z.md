# Runner pass — 2026-06-14T13:31Z

## Status
- box: 1.208.108.242:52674 (RTX 3060, sm_86, CUDA 13.0, PyTorch 2.12.0+cu130)
- session: arq tmux = LIVE, queue detached (`run_queue.sh` from 13:29Z)
- baseline: fresh MEASURE (3 ctrls queued) → cache `runs_since_measure=0` after this batch

## Sync work this pass
- Box was at commit 42ed363 (behind local HEAD d8f5c50 by 7 orchestrate snapshots).
- Box's working tree had a parallel-agent `use_q_only_norm` duplicate that crashed the smoke.
- Streamed to box via `cat local | ssh box 'cat > ...'`: configs/llm_config.py, models/llm.py,
  models/layers.py, models/components.py, models/xlayer_attn.py, _arq_159/160/161.py, _box_smoke.py.
- Build-smoke verified all 4 treatments (158/159/160/161) report SMOKE_OK on the box.
- (rsync reported transfer but didn't actually overwrite — the macOS-vast network setup
  silently no-ops scp/rsync transfers; the `cat | ssh cat >` path is what works.)

## Per-idea status (at this tick)
- 158-gau — running (in queue, ctrl done at 05:31:56Z, ctrl2 started 05:31:56Z)
- 159-emb-layernorm — needs-run (reclaimed: daemon's smoke batch dropped this; ssh throttling)
- 160-rms-gain-per-head — needs-run (reclaimed: same)
- 161-dyt-temp — needs-run (reclaimed: same)
- 162-q-only-norm — needs-run (reclaimed: same — also a parallel-agent idea, code landed but not claimed in this pass)

## Why only 158-gau made it into the queue
The daemon's `sync_and_smoke` returned only 1 idea after ssh connection throttling truncated
several smoke commands. The other 4 ideas were claimed → `running`, but the smoke check
failed silently (no SMOKE_OK in tail -3, but the bounce-to-needs-recode flip did not run —
likely the daemon hit ssh "Connection closed by remote host" mid-batch and the else branch
was skipped). The 4 ideas were manually flipped back to `needs-run` so the next tick picks
them up cleanly.

## Files written this pass
- none (queue just started; first ctrl completed 05:31:56Z, will be finalized on next tick)

## Box health
- fresh MEASURE queue in flight: ctrl + ctrl2 + ctrl3 + 158-gau
- cache reset (runs_since_measure=0 after this batch completes)
- 4 ideas remain needs-run for next tick; keep ≥3 at needs-run/running

## Next tick
- poll STATUS; finalize ctrl2/ctrl3/158-gau as they finish (MEASURE queue → write cache first)
- claim 159/160/161/162 (all back at needs-run); smoke them, launch treatment-only batch on the now-cached baseline
- keep ≥3 ideas at needs-run/running
