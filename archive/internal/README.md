# archive/internal

Maintainer machinery and lab history, moved out of the repo root so the top level
shows only what a contributor needs (clone → train → eval → compare). Nothing here
is required to run experiments; nothing was deleted.

- `autoresearch/` — autonomous-experiment daemon state, idea backlog, research notes
- `token2science/`, `llm-manual/`, `litreview/`, `posts/` — writing/curriculum side projects
- `queue/`, `runner/`, `experiments/`, `remote-results/`, `kaggle_job/` — old run infrastructure
- `releases/`, `release.sh`, `release-pipeline.md` — maintainer release pipeline
- `tools/`, `review/` — dashboards and review server
- `run_*.py`, `mech_*.patch`, `first_experiment_colab.ipynb` — one-off scripts and stale entry points
