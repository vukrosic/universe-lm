# T302 - treatment arm (good_lever)

**Goal:** G003-lever-ablation-demo

Run `python experiment.py --config config.json` (mock-backed, no GPU) and submit
the `val_loss`. Compared against the control arm (`T301`).
