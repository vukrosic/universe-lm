Read autoresearch/prompts/runner.md (and autoresearch/PIPELINE.md) and execute ONE full runner pass exactly as specified. You are in /Users/vukrosic/my-life/llm-research-kit-scaling.

Box (live, rented): read `autoresearch/remote-box.json` for the current ssh host/port/user — the instance rotates and the JSON is the single source of truth (NEVER hardcode the box string from this prompt). Repo on box: `<remote_repo>` from that JSON (already cloned). Python: `<remote_venv>/bin/python` (export PATH=<remote_venv>/bin:$PATH). Dataset ready at `<remote_repo>/processed_data/pretrain_1B` (~15G). A setup agent may still be finishing CPU smoke checks on the box — ignore it, it won't touch the GPU. **CRITICAL — connection reuse:** Vast.ai auth-throttles a burst of fresh handshakes ("too many ssh connections in quick succession" → "Connection closed by remote host") and that wedges the loop with ideas stuck in `running`. Define the multiplexed `SSHOPTS`/`BOX`/`CP_TO`/`CP_FROM` helpers from `runner.md` §0 at the start of your pass and route **every** ssh/scp through them (one warm TCP master per box); never call bare `ssh` or `scp`.

Remember the runner prompt's non-negotiables: detached remote tmux queue named arq, ctrl first and ctrl2 last, _arq_<NNN>.py config-subclass pattern for idea flags (NOT CLI flags), MinimalLLM build-smoke for ctrl + every treatment before training, fail-isolated run() wrapper, no orphan runs. If a queue is already live on the box (tmux session arq exists), do NOT relaunch — poll STATUS, pull finished logs, finalize evidence.md + flips, then exit. This prompt is re-invoked every ~10 minutes by cron; one pass per invocation, no waiting loops longer than a few minutes.
