## r1 — 2026-06-14 — verdict: approve

- **Source / dedup.** Source is real. 021-value-residual (closed WIN, Δ -0.034) is the closest analog and is distinct from 164 (021 carries *V*, 164 carries *Q*). 150-xlayer-feedback (closed reject) was full cross-block attention — structurally heavier than 164's single-projection carry. 116-hyper-connections (closed null) was residual-stream *splitting*. None of the closed axes ("V/Q/K/O embeds + combos, q_gain / k_gain (screen20m rows 0-17") are intra-block Q-side *projections* of the *previous block's* residual stream — the closed list is input-side intra-block; 164 is cross-block. Not a duplicate.
- **Mechanism is a lever, not an HP tune.** Per-block scalar α_l (init 0, 12 params total, +0.001% of 0.94M) gating one extra matmul `W_Q(prev_x)` per block. Identity/zero-init at step 0 ⇒ baseline forward graph on the flag-off path. ✓
- **🔴 tiny1m3m / seed 42 only.** No mention of screen20m or larger tiers. ✓
- **< 200 LoC.** Plumbing follows the established `v_residual` pattern (MHA.forward at models/layers.py:1847 already takes a stash kwarg; MinimalLLM._run_post_embed at models/llm.py:1373-1389 already wires per-layer stateful forwards; block construction at models/llm.py:879 already passes `use_value_residual`). Adding `use_q_carry` is a parallel ~10-15 LoC change. ✓
- **Falsifiable bar (NUMERIC GAP — fix in plan.md).** idea.md doesn't state a specific Δ vs fresh ctrl. Reviser must add: **WIN if Δ ≤ -0.01** at tiny1m3m seed 42 against a fresh ctrl (cached-mean 6.4394±0.04 baseline cache at autoresearch/baseline-cache.json); **null if |Δ| < 0.01**; **DRIFT/hostile if Δ > +0.01** (any wrong-sign > noise is a strong null since 021's WIN was in the other direction). Per ONE-TIER-ONLY rule, no scale-up retry is possible — the bar must be tight enough to *resolve* at 92-step tiny1m3m.
- **`prev_x` definition is ambiguous — clarify in plan.md.** idea.md says "the *input* to block l, i.e. what block l sees *after* the residual sum" — that's the residual stream entering block l-1, i.e. x_{l-1}. The MHA sublayer input is `LN(x_{l-1})` post-pre-LN. **Pin it to `LN(x_{l-1})` (the actual sublayer input the previous block's MHA saw)**, stash from layer 0's pre-MHA residual, and pass it to every layer l ≥ 1's MHA as a new `q_carry` kwarg. Layer 0 has no previous block → carry term is exactly 0 (not "α_0 · prev_x" with prev_x=None — just skip the add).
- **Detach policy — pin in plan.md.** Idea's formula `α_l · W_Q(x_{l-1})` lets gradient flow back into x_{l-1} and therefore into every parameter that touches layer l-1's residual output. That's a "feedback gradient" structure — fine if intentional, but 021's V-residual explicitly `.detach()`-es the layer-0 stash with the documented reason "each layer's W_V trains on its own attention path." **Pin the carry to `.detach()`** so the only gradient through the carry path is `α_l`'s gradient through layer l's W_Q. Mirror 021's contract exactly — the reviser's plan.md should call this out so the runner's `isolate_effects` audit doesn't flag it as a gradient-coupling confound.
- **Layer 0 special case — explicit in plan.md.** State in the plan: at layer 0 the stash is initialized but NOT consumed (carry term is a no-op); at layers l ≥ 1 the stash is read back. This matches 021's `v_residual = None → stash branch; v_residual != None → blend branch` pattern at models/layers.py:2260-2264.
- **Step-0 fp32 max-abs-diff ≤ 1e-6 — required verification.** With α_l=0 the extra matmul is multiplied by 0, so its contribution is numerically zero; but an extra fp32 multiply-add in the forward graph can perturb numerics at the trailing bits if the path is not branch-skip-elided. Plan must specify a `step0_diff ≤ 1e-6` check between `use_q_carry=True, alpha_l=0` and `use_q_carry=False`, both at fp32, across all 12 blocks (mirror 021's bit-identity claim at models/layers.py:2253-2254). If this fails, the lever's identity claim is broken — same bug class as 150-xlayer-feedback's `xlayer_mem` inplace append.
- **α_l placement.** Put `nn.Parameter(torch.zeros(1))` on each MHA (same slot as 021's `lambda_v`), one per block, init 0 → AdamW (1-D params). Model-side construction mirrors 021's wiring at models/llm.py:337 / models/llm.py:879 (one flag → one parameter block per layer).
- **Composition.** Composes orthogonally with 021 (V carry), 016 (QK norm), 155 (per-head temp), 161 (per-layer temp), 162 (Q-only norm). The carry is added BEFORE q_norm/q_only_norm on the W_Q path, so 162's RMSNorm still rescales Q + α·Q_carry consistently (and at step 0 both are unchanged). Plan should NOT enable any of these simultaneously — the A/B is `use_q_carry=True` vs baseline, single-flag diff.
- **Plan.md structure expected.** (Reviser writes it.) Sections: `## Mechanism` (math formula + prev_x definition + detach policy), `## Wiring` (LLMConfig flag → MHA param → MHA forward branch → MinimalLLM stash/loop), `## Step-0 identity claim` (verification protocol), `## Pass bar` (numeric Δ thresholds), `## Risk` (transfer-risk: med rationale unchanged), `## Composition matrix` (which existing flags are allowed simultaneously).

**Why approve (not revise):** idea.md frontmatter + source claims + mechanism shape are sound and the info-value is high (closes the "WHY 021 won" question). The findings above are plan-specification gaps that the reviser folds into plan.md without changing the idea — they don't reverse the mechanism, the source, or the cost case. Approving hands the reviser a clear checklist.

**Why not reject:** Not a duplicate (021 is V-side, not Q-side); not fabricated; not HP-tuning (structural lever, identity-init-able); not over-budget (<200 LoC, ~12 extra scalars, +0.001% param overhead); not off-tier (tiny1m3m seed 42 only).

**Verdict: approve** → `needs-plan`, round reset to 1 for the code gate.
