# Taste log — 164 Q-Carry (Cross-Block Q Residual)

## r1 — 2026-06-14 — verdict: accept

- **Leverage.** 021-value-residual WIN (Δ -0.034 at tiny1m3m) leaves open "what was binding — V specifically, or cross-block residual-stream mixing generally?" 164 is the dual axis (Q carry vs V carry). Either a Q-carry win at similar magnitude (→ opens the door to richer cross-block mixing at deeper tiers, mHC-flavored) or a clean null (→ V is special, residual mixing is not the axis) closes that ambiguity. Per the "clean null result must still be worth logging" rule, both outcomes are informative; this is *not* the rounding-error shape.
- **Info value.** High. The "WHY 021 won" question is genuinely open — 021's WIN w/ caveat closed the V-axis-as-lever but not the mechanism. 164 directly tests the mechanism (Q is the dual of V; if 021 is mixing-general, 164 should also win).
- **Non-obviousness / novelty.** Q-vs-V asymmetry on a cross-block carry is unmined. mHC papers (Zhu et al. 2024) discuss cross-block residual mixing at 100M+ but the narrow QK-residual subset is not a published production lever. 150-xlayer-feedback tried *full* cross-block attention (closed hostile at d_model=64/12L — cross-attn output dominates the residual stream). 116-hyper-connections was null (residual-stream *splitting* — structurally heavier than 164). 164 is the *lightest* cross-block intervention: 12 per-block scalars, init 0, applied to a single projection.
- **Portfolio fit.** Active queue (158–163) does not crowd the cross-block axis. 162-q-only-norm and 160-rms-gain-per-head are *intra-block* Q axes; 164 is cross-block, distinct family. Diversifies the active mix instead of duplicating.
- **Niche fit.** Mechanism (not HP), identity/zero-init-able (`α_l=0` ⇒ step-0 byte-identical, no graph mutation like 150's inplace `xlayer_mem.append`), tiny1m3m-able (~10 LoC, 12 extra scalars, +0.001% of 0.94M). Plan sketches are concrete: `use_q_carry` flag on `LLMConfig`, plumbing `prev_x` into `MultiHeadAttention.forward`, per-block `nn.Parameter(torch.zeros(1))` gate.
- **Crisp bet.** "If 021's WIN was residual-mixing-general, Q carry wins similarly; if V-specific, Q carry is null at tiny1m3m." That sentence is sharp enough to accept.
- **Transfer risk: med (fair).** Cross-block residual mixing is structurally validated broadly (ResNets, HighwayNets at 100M+) so the lever is plausible at scale. The narrow Q-specific subset is unvalidated at 135M, hence med. A tiny1m3m null doesn't bound 135M behavior, but the lever is cheap enough that the A/B is worth running even if the headline payoff is at deeper tiers.

**Concerns addressed:**
- 150-xlayer-feedback hostility at 12L/d_model=64: 150 attempted full cross-block attention (K, V from prev block, softmax over B·K·T), which dominates the residual stream at any nonzero effective gate. 164 is 12 scalars times one projection — structurally much lighter; the α_l=0 init keeps the residual stream dominated by the standard `W_Q(x_l)` path at step 0 (no graph mutation, no inplace append).
- 116-mHC null: residual-stream *splitting* is a broader intervention. 164 doesn't split the residual; it adds a single scalar-weighted Q projection. Different magnitude of change.
- Recent Q-axis crowding (162, 160): 162/160 are intra-block per-head scalar knobs on Q-normalization; 164 is cross-block. Distinct.

**Verdict: accept** → `needs-review`, reset round to 1 for the definition loop.