# Taste review — 168-av-output-carry

## r1 — 2026-06-14 — verdict: accept

- **Sharp bet, high info value.** Crisp sentence: "is the post-AV axis the binding cross-block carry, or is 021's WIN V-specific?" Null is equally informative — closes AV-output as the binding axis. Completes the 3-way attribution test (V from 021 / Q from 164 / AV-output from 168). Even a null is a *real* result that bounds the family.
- **Mechanism is zero-init clean.** `α_l = nn.Parameter(torch.zeros(1))` per block, init 0 ⇒ `α_l · av_{l-1} = 0` at step 0 ⇒ byte-identical to baseline. The 168-wiring is structurally less aggressive than 150 (rejected): no new Q/K/V from prev block, no full cross-attention — only a scalar-mixed additive residual on the AV product, then the standard W_O. d_model=64 budget concerns from 150 are mitigated, though the cross-block `prev_av` plumbing is still a wiring risk and the implementer must verify step-0 across all 12 blocks.
- **Leverage is real, not vibe.** Post-AV mix has 12 extra scalar params (+0.001% of 0.94M) and is a per-block learnable scalar — fits the niche bar (mechanism, identity-able, no HP grid). If 168 wins, the carry axis is *post-AV*, not V specifically; that opens richer post-attention cross-block mixing at deeper tiers. If it nulls, the family is closed at this tier.
- **Family crowding acknowledged, but not fatal.** 3rd cross-block carry in the queue (021 WIN, 164 in pipeline, 168 candidate). This is the 3-axis attribution test, not the 5th-momentum-variant pattern — the 3-axis logic is what makes the family worth running, and one round at the same tier closes the question.
- **Caveat for definition gate:** 150 was rejected because the bit-identity claim FAILED empirically (the implementer couldn't keep the cross-block mutation source clean). 168 must demonstrate the same step-0 byte-equality in the implementer's self-check before the run, with a max-abs-diff printed per block. The α=0 init makes this *easy to verify*, unlike 150's `xlayer_mem` plumbing — but the definition reviewer should require the per-block diff in the plan, not just the headline forward.
- **Caveat for run:** 021's WIN was in a buggy fire-equipped shared ctrl (subclass override dropped use_fire_pe=False); the within-session delta was joint V-res+FIRE vs no-FIRE. 168 will be tested against the current best baseline, so the carry attribution will be clean and the result will be defensible on its own.

### Verdict: accept → `needs-review`, round reset to 1
