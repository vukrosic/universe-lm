# Review log — 195-qk-clamp-min-max

## r1 — 2026-06-15 — verdict: approve
- **Source is real and current.** PaLM (Chowdhery et al. 2022, arXiv:2204.02311) for the logit-bounding concept at 8B-540B; closed `logit softcap` (closed axes list, screen20m rows 0-17) for the prior tanh form at 0.94M. Both arXiv ids and authorship check out. No fabrication.
- **Mechanism is a mechanism, not an HP.** `torch.clamp(scores, -c, +c)` after `Q@K^T/sqrt(d_k)` is a structural transform on the attention logit surface, not an LR/schedule/init-constant lever. `c` enters as a fixed config constant, not a trainable parameter. 0 new params (explicitly stated; verified against config flag pattern in `configs/llm_config.py`).
- **Tiny1m3m only, seed 42.** Explicit. No `screen20m`, no ladder, no multi-tier. PASS.
- **Distinct from closed axes — verified.** Cross-checked `autoresearch/closed.md`:
  - "logit softcap" (closed, screen20m rows 0-17) — *smooth* `tanh(qk/c)*c` at c=8 (inactive at step 0). 195 is *hard* `clamp` at c=2.0 (active at step 0). The c=8 form would be bit-identical to closed softcap; the c=2.0 form is genuinely distinct because ~5% of init logits hit the boundary (2-sigma Gaussian tail), the lever is not bit-identical to baseline, and the gradient is *discontinuous* (zero outside the clamp range) — not just smooth-bounded.
  - 184-logit-scale (LM head, not pre-softmax QK). 152-attn-logit-bias (smooth additive bias, init 0 ⇒ bit-identical at step 0). 155-per-head-temp (scalar on QK^T, smooth). 188-qk-rms-scaling (soft multiplicative per-block scalar). None of these are hard bounds; 195 is the *only* hard bound on pre-softmax QK^T in the queue.
- **< 200 LoC, verified against the repo.** `models/layers.py` — one line in attention forward (`scores = torch.clamp(scores, -c, +c)`). `configs/llm_config.py` — `use_qk_clamp: bool = False` and `qk_clamp_c: float = 2.0` fields on `LLMConfig` (matches the existing `use_*` flag pattern at lines 43/50/63/87/106/138/157/175/214/234/239/242), plus a `Tiny1M3MQKClampConfig(Tiny1M3MConfig)` subclass with both flags flipped. Total: ~3 lines in layers.py + ~5 lines in llm_config.py. Implementation cost is trivial.
- **Falsifiable pass/fail bar with a real control.** Three regimes are well-separated:
  - WIN: `trt_val ≤ ctrl_val − 0.005` (clears 2-ctrl rule) — champion 6.24 / cache baseline 6.40 as anchors, so 0.005 is real money on this tier.
  - NULL: `|Δ| < 0.01` — exactly the box-noise band (`±0.01 val loss` from PIPELINE.md). A null closes the *hard-clamp sub-axis* of the logit-bounding family; the soft sub-axis is already closed. Both family endpoints would then be nulled at this tier, which is genuinely informative.
  - DRIFT: `trt_val > ctrl_val + 0.01` — would mean the discontinuous gradient at c=2.0 is too aggressive for 0.94M/12L/4H. Also a clean close with a mechanistic reason.
- **Transfer-risk: low — justified.** PaLM validates the *concept* (QK logit bounding via tanh at c=50) at 8B-540B; 195 is a *tight hard* form, which is a well-known alternative implementation. The lever either generalizes (low-risk call) or it doesn't, and the smooth-vs-hard distinction is a useful axis to know the answer to. The `low` tag matches the citation; no finding.
- **Step-0 byte-identity caveat is acknowledged and justified.** At c=2.0 the lever is *not* bit-identical to baseline at step 0 (~5% of logits hit the boundary). The idea explicitly owns this departure from r0 and frames it as the bet (the *regularizer effect* is what's being tested, not a null activation). The taste reviewer already accepted this framing in r2 — and at the reviewer's lens, the framing is sound: a null at the tight hard form is informative because the soft form is already closed; a win is a real mechanism; both outcomes earn the slot.

**Action:** approve → `needs-plan` (round reset to 1 for the code gate).