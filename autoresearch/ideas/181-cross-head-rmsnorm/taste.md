## r1 — 2026-06-15 — verdict: accept

- **Novel axis vs. the closed per-head-attention-shape family.** 152 (logit bias), 155 (per-head temp), 160 (per-head post-AV gain), 162/165 (QK pre-softmax), 166 (T5-RPE) all closed null at tiny1m3m. 181 tests a **cross-head coupling** lever — RMS-normalize across H heads within each d_k slice — which is qualitatively different from per-head independent rescaling and is *not* what 160 closed. The closed 160 null specifically argued "post-AV magnitude axis is plausibly redundant given W_O"; 181 is the natural follow-up that asks "but what about *joint* post-AV magnitude across heads — is that absorbable too?"
- **Crisp bet, identity-init-able.** Tanh-gate parameterization gives byte-identity at step 0 (the sketch correctly catches the RMS-divides-out error and fixes it with `γ_h[k] = 1 + tanh(γ_raw_h[k])`, init 0 ⇒ γ=1). +768 params (+0.081%) — within budget. The leverage sentence: "post-AV cross-head coupling binds even when per-head gain doesn't, because cross-head normalization produces a value distribution that W_O cannot replicate by per-channel rescaling."
- **Informative-null is real.** If the cross-head coupling axis also closes null at 0.94M alongside 160, that **closes the entire post-AV-magnitude family** at this tier (per-head gain AND cross-head coupling) and re-routes Phase-2 attention-shape bets to the pre-softmax axis (where 016-qk-norm won). That is high-value information for the 135M recipe, not a waste.
- **Niche fit clean.** Mechanism (not HP), identity-init-able, runs at tiny1m3m, step-0 byte-identical. Transfer-risk med is acceptable given the closed family provides strong prior that the per-head axis at 0.94M is exhausted — 181 is the orthogonal probe that completes the closure map.
- **Portfolio fit OK.** 176-v-pre-av-norm (V-side pre-AV normalization) is in needs-run, but that tests a different position (V-side, pre-AV) and is orthogonal to 181 (post-AV, cross-head). Two post/around-AV normalization probes is not crowding; both have informative nulls.
- **Non-obviousness.** Cross-head H-axis normalization is not the d_model-axis normalization that Qwen-2 / Gemma-2 / LLaMA-3 use. The H-axis variant is novel at this tier and worth one slot to resolve whether the closed per-head null pattern generalizes to coupling.

Reset round to 1 for the definition loop.
