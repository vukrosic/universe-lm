## r1 — 2026-06-15 — verdict: accept
- **Leverage is real, not redundant.** Per-layer γ_l is orthogonal to 172 (per-head learnable base, 48 scalars, frequency-scale lever) and to 175 (alibi slopes, additive logit-bias lever). 174 is the *magnitude-decay* lever — `g_t · cos(t·θ_i)`, `g_t · sin(t·θ_i)` — biasing attention toward recent positions via K-magnitude shrinkage, distinct from 009 FIRE (continuous integrable PE additive term) and 154 (rebased K/V before softmax). Three locality-prior WINs at this tier (009, 154, 021 V-res w/ caveat) and 174 is the 4th fresh axis on the same family.
- **Bit-identical step-0 is properly argued.** Init γ_l=0 ⇒ exp(0·t)=1 ⇒ rotation magnitudes unchanged ⇒ Q/K rotation unchanged ⇒ output bit-identical to the 500k-base RoPE baseline. No stray subclass override risk that bit 021/023/024/025 (must trust the reviewer's read of the wire site at `models/layers.py:2031-2034`; concurrent git diff in `models/layers.py` is for unrelated work, do not rebase).
- **Crisp bet, honest band.** Miner states Δval ∈ [-0.003, -0.020] — the lower end is inside the ±0.04 noise band and the upper end is a clean signal. The honest framing is good taste; a null at the low end still carries information ("exponential decay redundant with FIRE/RoPE-base at this tier") and a win unlocks the per-layer γ_l specialization at 135M Phase-2 where layer-depth specialization has room to differentiate. This is a learn-from-either-side A/B.
- **Transfer risk low is justified.** xPos validated at GLM-130B and adopted in ChatGLM-2/3 (Sun et al. 2022); mechanism is scale-free (single scalar per layer); no tier-mismatch concern like 129-YOCO (KV-cache savings invisible at 0.94M) or 143-ShortConv (depthwise Conv1d benefits from 30-50× more update steps). Pass.
- **Portfolio check (not over-crowded).** Three locality-prior entries in flight/closed: 172 (per-head base, in `needs-run` after this gate), 175 (alibi slopes, also `needs-taste` — separate gate), and 174 (this). Each is on a distinct lever axis (per-head frequency scale / additive logit bias / magnitude decay). Queue not crowded enough to `revise` for diversification; if 172 returns a null the next gate will likely `revise` 175 to fold into the closed family, but 174 stands on its own.
- **LoC and niche fit fine.** ~25 LoC, single parameter (12 scalars), mechanism not HP, identity-init-able. Matches the niche. No `niche fit` concern.
- **Decision.** `accept` — sharp, orthogonal to in-flight work, low-risk transfer, informative either way. Reset round to 1 for the definition gate's own 3-round budget.