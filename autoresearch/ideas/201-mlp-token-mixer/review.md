## r3 — 2026-06-16 — verdict: approve
- **r1 and r2 findings are settled — the idea definition is unchanged and remains sound.** Per the prompt's "do not re-litigate settled findings" rule, the r1 / r2 approvals stand. Mechanism (gMLP-style per-channel gate broadcast on attn output pre-W_O, +1.74% via 4/12 stochastic blocks at stride 3, raw `nn.Parameter` for RNG alignment, α = sigmoid(α_raw) init −10 ⇒ bit-identical baseline at step 0), source citations (Liu et al. 2021 arXiv:2105.08050; 163 as the local-conv sibling), and conditional-on-163 schedule are all unchanged.
- **Why the idea came back to `needs-review` at r3 — context for this verdict.** The log.jsonl shows the same build-smoke `ImportError: cannot import name 'Tiny1M3MGMLPSGUConfig' from 'configs.llm_config'` (path points at `/root/universe-lm/configs/llm_config.py`, the box mirror) on the daemon's smoke check. The code-impl claims to have added the class locally, but the on-box `configs/llm_config.py` doesn't see it. The auto-implement agent then routed the idea `needs-recode → needs-review → rejected` (anomaly: per PIPELINE.md "doers never close" — auto-implement is a doer; it should be bouncing to `needs-recode` and letting the `MAX_RECODE_ROUNDS=3` recode cap in `flip.sh` close the idea on the 4th attempt, not auto-rejecting out of band). I claimed the slot at the `rejected` status — the reviewer's authority is the definition, and I'm exercising it.
- **What I checked at the definition gate for r3.** (a) `idea.md` frontmatter and body are unchanged from r1's approved state. (b) `plan.md` matches r1's approved intent: same flag naming (`use_gmlp_sgu` / `gmlp_sgu_block_stride` / `gmlp_sgu_alpha_init` / `block_idx`), same construction (raw `nn.Parameter` + inline `.data.normal_` for RNG alignment, not `nn.Linear`), same forward branch (mean-pool + gelu + matmul + broadcast + `α·z` add) inserted in the correct `post-v_mix_conv / pre-W_O` slot, same 4-of-12 stochastic-block pattern, same `Tiny1M3MGMLPSGUConfig` derived config, same seed-42 tiny1m3m scope. (c) `closed.md` — no new closes that would duplicate 201's slot. (d) `git diff` — `configs/llm_config.py` is clean (good, no parallel-AI conflict for 201's edit); `models/layers.py` and `models/llm.py` are dirty with **197 (DeepNet α) and 194 (W_V low-rank) changes** from a parallel agent, **not 201's** — 201's prior code edits appear to have been rolled back (the `Tiny1M3MGMLPSGUConfig` import is missing because the class is missing). This is consistent with the build-smoke failure: the local 201 edits were never pushed/synced, or were reverted. **The implementer must add `Tiny1M3MGMLPSGUConfig` to `configs/llm_config.py` and verify the on-box mirror at `/root/universe-lm/configs/llm_config.py` exports the class before the next smoke run.**
- **Verdict: approve, reset round to 1.** The definition stands. The code-side `ImportError` is a code-gate failure, not a definition finding — the next code-impl pass needs to resolve it. The next code-impl pass should: (i) re-add `Tiny1M3MGMLPSGUConfig(Tiny1M3MConfig)` to `configs/llm_config.py` with `use_gmlp_sgu: bool = True` (other flags at default), (ii) verify locally that `from configs.llm_config import Tiny1M3MGMLPSGUConfig` succeeds in the smoke stub's import path, (iii) verify the on-box `/root/universe-lm/configs/llm_config.py` is in sync (the daemon's auto-sync may not have pulled local edits; the path in the build-smoke error is the box mirror), (iv) re-run build-smoke locally *before* queuing the box. **If the next code-impl pass also fails the build-smoke, the recode cap in `flip.sh` will close the idea on the 4th `needs-recode` flip at round ≥ 3 — let that path run, don't route to `rejected` out of band.**
- **No new findings on the definition.** No `revise` — the idea is sound, falsifiable, identity-init-able, fair-by-param, ready to re-spec. Re-approving gives the code gate a fresh attempt at resolving the build-smoke.

---

## r2 — 2026-06-16 — verdict: approve
- **r1 findings are settled — the idea definition is unchanged and remains sound.** Per the prompt's "do not re-litigate settled findings" rule, the r1 approval stands. Mechanism (gMLP-style per-channel gate broadcast, +1.74% via 4/12 stochastic blocks, α = sigmoid(α_raw) init −10 ⇒ bit-identical baseline at step 0), transfer-risk (med, parameter-count-independent broadcast shape), source citations (Liu et al. 2021 arXiv:2105.08050; 163 as the local-conv sibling), and conditional-on-163 schedule are all unchanged from r1.
- **Why the idea came back to `needs-review` at round 2 — context for this verdict.** The log.jsonl shows r1 was approved → code-impl attempted build-smoke, got `ImportError: cannot import name 'Tiny1M3MGMLPSGUConfig'`, recoded (r2 in code-impl's local counter, bumping global `round` to 2), attempted build-smoke again, *same* `ImportError`. The `auto-implement` agent then logged "auto-fix gave up after 3 failed runs — needs a human" and bounced the idea back to `needs-review` at round 2. This is a **code-gate failure**, not a definition-gate failure — the `auto-implement` re-routing to `needs-review` (rather than letting code-impl try once more, or auto-closing via the `MAX_RECODE_ROUNDS=3` cap) is a process exception; the r1 definition approval is not under dispute.
- **What I checked at the definition gate for r2.** (a) `idea.md` frontmatter and body are unchanged from r1's approved state. (b) `plan.md` matches r1's approved intent: same flag naming (`use_gmlp_sgu` / `gmlp_sgu_block_stride` / `gmlp_sgu_alpha_init` / `block_idx`), same construction (raw `nn.Parameter` for RNG alignment, not `nn.Linear`), same forward branch (mean + gelu + matmul + broadcast + `α·z` add) inserted in the correct `post-v_mix_conv / pre-W_O` slot, same 4-of-12 stochastic-block pattern, same `Tiny1M3MGMLPSGUConfig` derived config, same seed-42 tiny1m3m scope. (c) `closed.md` — no new closes since r1 that would duplicate 201's slot. (d) `git diff` shows `configs/llm_config.py` is *not* in the parallel-AI's dirty list (it's clean); `models/layers.py` and `models/llm.py` are dirty, presumably from the prior 201 attempt — out of scope for this review.
- **Verdict: approve, reset round to 1.** The definition stands. The code-side `ImportError` (build-smoke can't import the new config class) is a code-impl problem the next code-impl pass needs to resolve — most likely the new `Tiny1M3MGMLPSGUConfig` class was added in a local edit that the daemon's auto-sync did not pull, or it was added in a code path that the build-smoke stub does not import. The next code-impl pass should: (i) re-verify `Tiny1M3MGMLPSGUConfig` is exported from `configs/llm_config.py` (importable via `from configs.llm_config import Tiny1M3MGMLPSGUConfig` in the smoke stub's path), (ii) re-run build-smoke locally *before* queuing the box, and (iii) check the on-box path matches the local path (the `ImportError` trace points at `/root/universe-lm/configs/llm_config.py`, which is the box mirror — sync is the suspect). These are code-impl notes, not definition findings; I'm passing the idea through.
- **No new findings on the definition.** No `revise` — the idea is sound, falsifiable, identity-init-able, fair-by-param, ready to re-spec. Re-approve to give the code gate a fresh 3-round recode budget.

---

## r1 — 2026-06-15 — verdict: approve
- **Source is real and current.** gMLP (Liu et al. 2021, arXiv:2105.08050, NeurIPS 2021) and MLP-Mixer (Tolstikhin et al. 2021, arXiv:2105.01601) both resolve; gMLP is the SGU origin paper (ImageNet 79.4% top-1, up to ~73M params). 163 is in-repo `needs-implement` (confirmed in `autoresearch/ideas/163-v-mix-conv/idea.md`); 143-shortconv (closed borderline) and 157-conv-ffn (closed null) are different placements (pre-attn residual, post-FFN-act), not duplicates of 201's post-attn-pre-W_O axis. No fabricated citation.
- **Mechanism is a mechanism, not HP.** Spatial Gating Unit is a published architectural primitive. r2's commitment is one shape (mean-pool over T → per-channel linear → broadcast back, with α-gated add to attn output pre-W_O). The structural note from taste: the committed shape is *degenerate* — the cross-token reduction is a parameter-free `mean(dim=T)`, the per-channel `W_g: [d_model, d_model]` operates on a `[1, d_model]` pooled vector, so the lever is effectively a per-channel scalar broadcast with channel-mixing of the global summary, not a T×T spatial mix. Honest framing, but **the plan must rename** to "per-channel gate broadcast" or "degenerate gMLP SGU" — keep the "gMLP SGU" label only with a "degenerate" prefix. Future readers expecting a T×T spatial mix (which is what the gMLP SGU actually is per Liu et al. §3.1) will be misled otherwise. This is a plan-side note, not a blocker for approve.
- **Step-0 identity is clean, but RNG-state discipline is mandatory.** α = sigmoid(α_raw), α_raw init −10 ⇒ α ≈ 4.5e-5 ⇒ contribution is zero in fp32 at step 0 ⇒ bit-identical baseline. The W_g matrix must be constructed via raw `nn.Parameter(torch.empty(d_model, d_model))` with explicit `.data.normal_(...)` — **not** via `nn.Linear(d_model, d_model)`, which would consume RNG state and break the no-flag path RNG alignment. Same discipline as 175-alibi-slopes and 154-rebased-attn. Plan must say this; implementer must enforce it.
- **tiny1m3m only ✓.** Pitch references no other tier; no scope drift. Seed 42 only — never multi-seed (taste-side guard). Noise band ±0.04 vs expected |Δval| ≤ 0.02 ⇒ bar is borderline-resolvable; the α-trajectory is the load-bearing diagnostic when val-loss sits inside the band, which is the right design (3-axis readout: α, val_loss, conditional-on-163 — informative in *all* outcomes).
- **Not already closed.** Checked `autoresearch/closed.md` axes lines + the in-loop closes through 2026-06-15: no entry for a post-attn pre-W_O global per-channel gate broadcast. Closest neighbors are 143 (pre-attn depthwise conv, null borderline), 157 (post-FFN-act depthwise conv, null), 163 (post-attn pre-W_O depthwise conv at k=3, in-queue), 175 (alibi, WIN at post-attn pre-O bias slot, but positional not channel-mixing). 201 is structurally distinct.
- **Implementable in <200 LoC against real files.** Taste says <100 LoC in `models/layers.py`, mirroring the established pattern (`use_v_mix_conv`, `use_qk_norm_depth`, `use_qk_rms_scaling` — all on `MultiHeadAttention.__init__` as `use_<feature>: bool = False` kwargs with conditional register, conditional forward branch, default-off bit-identity). Fits.
- **Transfer-risk: med is justified and the citation matches the tag.** gMLP has no in-LM validation (ImageNet-only in Liu et al. 2021) — that is the *reason* `med`, not the *reason for low*. The pitch's "Scale evidence" section explicitly cites the largest scale gMLP demonstrated gains at (ImageNet, up to ~73M, 79.4% top-1) and tags the lever `med`. The tag is consistent with the rest of the pipeline's convention (e.g., 175-alibi is `med` for the same reason — large-scale positional-prior validation, no in-LM at tiny1m3m scale). Plan should add one sentence stating that the lever is shape-preserving across scales (a per-channel broadcast is the same shape at 0.94M and 135M), so if it binds here, it should bind at 135M — i.e., the `med` tag reflects "no in-LM validation at any scale," not "doesn't transfer across tier."

**Notes for the plan (not blocking, should land in plan.md):**
- **Naming**: use "per-channel gate broadcast on attn output" or "degenerate gMLP SGU" in the plan title and mechanism sketch. Keep the Liu et al. 2021 citation for lineage, but add a one-line caveat that the committed shape is a global mean-pool (not a T×T spatial mix as in published gMLP).
- **Block-stride wiring**: `gmlp_sgu_block_stride: int = 3` means only block_idx ∈ {0, 3, 6, 9} get the SGU (4 of 12). The implementer must plumb `block_idx` into the MHA constructor — the existing pattern in `models/layers.py` already has `block_idx` in the build path (e.g., for `rebase_stride`-gated variants and per-layer scalars), so this is a known wiring detail, not a new constraint. If plumbing is awkward, a per-block config map at the LLM-build level is an acceptable alternative. Plan should state the wiring choice.
- **α-trajectory logging**: 4 α scalars (one per SGU-enabled block); each is a separate `nn.Parameter(shape=[])`. Implementer must log α per SGU-enabled block per checkpoint to evidence.md as a 4-row table (block_idx, step, α, val_loss). This is the load-bearing diagnostic when val_loss sits inside the noise band.
- **Schedule after 163**: r2's bet is conditional on 163. The plan must state 201 runs *after* 163 lands, not in parallel. If 163 ships null, 201 is the next-in-line "global vs local post-attn" follow-up. If 163 ships WIN, 201 is the conditional "wider vs narrower" follow-up. Per the protocol's `one-seed-only` rule, 201 must not sweep seeds even on a 163-WIN branch.
- **LoC budget check**: W_g construction + α scalar + forward branch (mean-pool, gelu, matmul, broadcast, add) is well under 100 LoC if the implementer follows the `use_v_mix_conv` template. Plan should sanity-check against the actual existing file (the parallel agent has been adding flags; verify no name collision on `use_gmlp_sgu` / `gmlp_sgu_block_stride` / `sgu_W` / `sgu_alpha`).

**No findings block approval.** Ready to spec.
