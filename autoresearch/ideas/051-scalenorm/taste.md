# Taste log — 051 ScaleNorm

## r2 — 2026-06-11 — verdict: accept
- **r1 findings all closed cleanly.** Repitch (a) reframes baseline as RMSNorm and cites `models/layers.py:246`; (b) moves the *axis* off param count onto residual-stream gain expressivity (vector→scalar), making the bet a *diagnostic* not a savings pitch; (c) disambiguates from PNorm(p=2) (closed `norm zoo` in `closed.md:24`, same L₂ denominator, different gain shape), N1 reparam_gain, N6 gain_init, N7 softplus_gain (all keep `g ∈ ℝᵈ` — orthogonal axis); (d) owns the wrong-signed direction vs 016-qk-norm WIN by stating the prior: "per-channel residual gain is mildly load-bearing in d=192 / 12-layer, so we expect a *narrow* loss — and that loss is informative, not a blowout." The pitch is no longer symmetric.
- **The bet is the right shape for a clean null.** Where 016-qk-norm WIN (`closed.md:36`) said *add* targeted norm structure on QK, ScaleNorm bets the *opposite* on the residual stream: per-channel gain `g ∈ ℝᵈ` is doing little or no work, so collapsing it to `g ∈ ℝ` costs ≤noise. That's not "either it works or we learn something" — it's a named direction with a named prior, and *both* outcomes carry a downstream decision:
  - Lose (narrow): per-channel residual gain is load-bearing → **don't** try cheaper-gain residual norms, **do** keep investing in per-channel structure.
  - Win (narrow): residual gain is vestigial → freeze the gain to 1 entirely (zero params on residual norms) and migrate the budget to attention sites where 016 already showed it pays.
  The A/B is *defined by* its diagnostic value, which is exactly the "clean null is still worth logging" criterion from the taste prompt.
- **Leverage framing is honest.** ~0.24% param save is rounding-error and the repitch says so; the edge is the information. transfer-risk: med is correctly tagged (mechanism is task-agnostic, magnitude at d=192 is genuinely unknown) and the "Why it's worth a slot" sentence ends on the 135M-relevance point, not a vibes-claim.
- **Niche mechanics check.** New `norm_type` string `"rmsnorm_scalar"` registered in `make_norm`; `g` is a single nn.Parameter init at 1.0 so step-0 == RMSNorm up to a constant absorbed by the existing baseline — clean identity-init, no control gymnastics. The closed norm zoo doesn't include this axis (gain *shape*, not gain *init/param*), so it's a fresh slot, not a duplicate. <50 LoC is in budget.
- **Portfolio fit is fine.** Norm zoo (closed.md:24) is retired on the denominator/init axes; ScaleNorm is the one remaining lever on the *gain-shape* axis. Crowd of 15+ `needs-taste` ideas is mostly optimizer variants (032-040) and attention mechanisms (041-048) — the residual-stream-norm bet is uncrowded.
- **Sent to the definition gate** as `needs-review`, round reset to 1. The definition critic should re-verify the 135M transfer argument (the repitch says per-channel gain is task-agnostic, but d=192 → d=768 gain-load could shift) and the identity-init constant absorbability against the `RMSNorm` `models/layers.py:246` reference. Those are the only places r2 leans on the reader's trust.

## r1 — 2026-06-11 — verdict: revise
- **Wrong baseline framing.** The pitch sells ScaleNorm vs *LayerNorm* ("avoids per-feature mean/variance statistics"), but this codebase already uses **RMSNorm** as default (`models/layers.py:246` `RMSNorm`, `make_norm` default `"rmsnorm"`). RMSNorm already drops the mean/variance piece — `y = g · x / √(mean(x²)+eps)`. So the "skip mean/var" leverage the idea claims is already paid. The relevant A/B is **ScaleNorm vs RMSNorm**, not vs LN, and the pitch must say so.
- **Mechanism collapses to "RMSNorm with scalar weight."** ScaleNorm `g·x/‖x‖₂` and RMSNorm `g·x/√(mean(x²))` differ by a fixed `√d` factor absorbable into init — the only real delta is `g ∈ ℝ` (ScaleNorm) vs `g ∈ ℝᵈ` (RMSNorm default). That is a parameter-count restriction (d_model→1 per norm site; at d=192·12 sites ≈ 2.3k params, ~0.24% of 0.94M — rounding-error param savings, no separate leverage). Re-pitch must name what *else* the bet buys beyond "fewer params on the gain."
- **Adjacent territory is closed.** `closed.md:24` already retires the norm zoo (pnorm, manhattan, center, squash, clip, channelscale), and the in-code `RMSNorm` ships 17 levers (N1 reparam_gain, N7 softplus_gain, N15 dyntanh, N17 centermix RMS↔LN knob). None of those is *exactly* scalar-g, but the family is well-trodden — the re-pitch must explicitly disambiguate ScaleNorm from PNorm(p=2) (same L₂ shape, different gain shape) and from the N6/N7 scalar-init experiments already in `RMSNorm`.
- **Portfolio direction is wrong-signed.** 016-qk-norm WIN (`closed.md:34`) says *adding* targeted norm structure helps at tiny1m3m; ScaleNorm bets the opposite — that *less* gain expressivity is fine. That's a legitimate adversarial bet, but the pitch must own it: "we expect RMSNorm's per-channel gain is doing work — clean null on ScaleNorm confirms it; otherwise ~0.24% param win." Right now the "Why it's worth a slot" sentence is symmetric ("either it works or we learn something") with no edge.
- **Sharp re-pitch shape, to close the taste gap in r2:**
  - Reframe baseline as RMSNorm (cite `models/layers.py:246`).
  - One sentence on what scalar-g would mechanistically buy beyond param savings (e.g. "stronger isotropy of residual stream because no per-channel scaling can drift").
  - State the null direction: which is the prior — is per-channel gain doing work, or vestigial?
  - Acknowledge the closed `pnorm/channelscale` neighbors and name the orthogonal axis.
