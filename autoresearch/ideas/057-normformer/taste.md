## r2 — 2026-06-11 — verdict: accept
- **r1 taste gap closed.** r1 sent it back for bundle-granularity ("pick one of the three"); r2 isolates exactly the piece r1 named as the fresh-bet candidate — post-FFN-W1 (mid-MLP) LN — and drops the other two NormFormer ops. Single mechanism, single LN insertion, ~10 LoC at one site.
- **Site is genuinely fresh in the norm portfolio.** 016 (QK-norm, WIN) and 029 (V-norm, running) own the attention path; 026/027 stack QK-norm with FIRE/Moonlight; 051/052/055/056 normalize the residual stream. *No* active or closed lever targets the FFN's hidden activation between W1 and the non-linearity. Mid-MLP norm is the one major activation site without a magnitude-bounding lever — distinctness claim is correct.
- **Informative-null shape is real.** With the bet narrowed to "post-W1 magnitudes drift faster than the entry pre-norm can correct", a null means "post-projection magnitudes are fine, bottleneck is elsewhere" — that's a genuine read on the *site*, not the technique. Under the r1 bundle framing a null meant nothing; under r2 it bounds where to look next.
- **Identity-init keeps the A/B clean.** LN with default gain=1, bias=0 starts ≈ baseline modulo the normalization itself — isolates the magnitude-bounding effect rather than tangling with init perturbation. Standard niche-fit shape.
- **Transfer is honestly accounted.** transfer-risk: med (not low) — NormFormer's 1.3B headline is the bundle, not L_m alone; that's the right tag. Mechanistic argument (outlier hidden activations after `W1·SiLU` accumulate across depth, a pre-norm at block entry can't reach them) is sound and scale-monotone. Plausible carry to 135M if it wins.
- **Leverage.** Activation-magnitude levers at this tier have a track record (016 WIN, 021 WIN, 023 WIN, 024 WIN, 025 WIN) — a fresh site in the same family is a reasonable big-if-true bet. Crisp one-sentence "we expect X because Y" is present. No portfolio crowding *at this site*.

## r1 — 2026-06-11 — verdict: revise
- **Bundle = bad info value.** Idea ships *three* mechanisms at once (post-attn LN, per-head attn-output scale, post-FFN-W1 LN). At tiny1m3m we get one val-loss number per A/B; a win can't be attributed to which of the three, and a null can't acquit any one piece. The pipeline runs single-mechanism A/Bs for a reason — pick **one** of the three and propose it cleanly.
- **Crowded norm portfolio — bundle is the wrong granularity.** Active norm-family at needs-taste/running: 016-qk-norm (WIN, in stack), 029-v-norm (running, symmetric per-head LN on V), 026-fire-x-qknorm + 027-moonlight-x-qknorm (stacks), 051-scalenorm, 052-fixnorm, 055-deepnorm, 056-branchnorm. The taste call here is to *isolate* a piece NormFormer adds that the rest of the queue does NOT — the head-wise attention-output scale, or post-attn LN, or post-FFN-W1 LN — and run that as one crisp mechanism. The bundle test is what you'd do *after* the singletons settle, not before.
- **Mechanism overlap with what's already winning.** Per-head learned scale on attention output is close in spirit to value-residual / channel-scale levers (021 WIN w/ caveat, channelscale closed); a post-attn LN sits next to QK-Norm's stabilization story (016 already paid that bill). The fresh-bet candidate inside the trio is the **post-FFN-W1 LN** — a *mid-MLP* norm, distinct from anything in 016/029/051-056 — and that's the single-mechanism re-pitch with the cleanest mechanistic story ("bound the hidden activation magnitude before the down-projection, separate from the pre-norm at the FFN entry").
- **Sharper bet shape.** Re-pitch as one ~10 LoC LN insertion at one site, identity-init, with one sentence: "we expect mid-FFN LN to move loss because [outlier hidden activations after W1·SiLU are the bottleneck / not already controlled by the entry pre-norm]". A null then *means* something ("mid-FFN magnitudes are fine"); under the current bundle framing a null means nothing.
- Not a `reject`: NormFormer has direct LM-pretraining evidence in the right band and transfer-risk: low is fair. The taste gap is *granularity*, not legitimacy.
