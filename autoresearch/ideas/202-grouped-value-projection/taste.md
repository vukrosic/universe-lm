## r1 — 2026-06-15 — verdict: revise

- **The novelty is real, but the bet is handwavy and the family is already closed twice.** `closed.md:23` lists "MHA vs GQA, MLA, Tied QK" as a closed arch-sweep axis at tiny1m3m. `closed.md:39` records `178-mqa-gated — null: Δ=0.1647 at tiny1m3m (inside variance)` — the **probe** that should have mechanistically closed the family. 178's evidence.md only records val loss (6.4050 vs baseline 6.2403); it does NOT record whether the per-head β_k, β_v gates moved. Without that read-out, 202's bet ("V-redundancy is real where K-redundancy isn't") is unanchored — we cannot tell whether 178's null came from (a) both β_k and β_v collapsing to 0 → family is dead, OR (b) one or both gates moved but the loss landscape didn't reward sharing → there is a per-head gradient but no quality lever. The whole 202 framing hinges on this distinction, and the idea does not engage with it.
- **Mechanism argument is a vibe, not a bet.** The idea's one-line rationale is "V is what to read from (content-based, can be shared), K is what to attend to (more head-specific)." This is the kind of asymmetric-handwaving that papers do *after* showing the result, not before. No published work validates V-only GQA — the idea concedes "novel (no published paper I'm aware of)." At 0.94M with H=4, d_k=16, each per-head V projection is 1024 params and the in-group V (mean of 2 heads) is the same 1024 params. The savings are 24,576 params (-2.6% of 0.94M), real but small. The question is whether the V-redundancy mechanism is real at this scale, and "content-based" doesn't answer it — both K and V receive the same input projection (`x @ W_Q` vs `x @ W_K` etc.) and operate on the same residual stream; the difference is what attention does with the projection (scores vs values), not whether the projection itself is shareable.
- **Crisp-bet problem.** The idea has no directional prediction in val-loss terms. "V redundancy is real" is an architectural claim, not a "we expect val to drop by X because Y" sentence. If the bet is "V-only GQA is quality-neutral (parity with MHA, since V-sharing is a no-op)", that's a fine null hypothesis but it doesn't earn a slot — parity on a closed family is what 178 already implied. If the bet is "V-only GQA WINS because V carries content redundancy that K does not", then name the magnitude, what 178's gates did, and what would falsify it.
- **Queue crowding.** Active queue holds ~15 ideas at needs-taste. The attention-axis portion (188 qk-rms-scaling, 189 cos-attn, 191 relu-attn, 192 pre-rope-qk-rotation, 195 mid-attn-rmsnorm, 198 pre-ffn-attnmix, 199 spectral-attn-output, 200 rope-phase-offset-per-layer, 203 pre-wo-se-channel-attn, 204 cross-block-attn-score-share) is a wave of recent nulls (152/155/160/162/165/166/177/179/181/182/185/186) — the bar to add another attention-axis idea is "the family hasn't been closed" or "the bet is sharper than what closed." 202 does not meet that bar.
- **Niche fit.** Mechanism ✓ (soft-blend V with per-head α). Identity-zero-init-able ✓ (sigmoid(α=-10) ≈ 0 ⇒ W_V_h_eff = W_V_h at step 0). tiny1m3m-runnable ✓ (no compute budget issue). Transfer-risk tag is `low` but the asymmetric V-only variant has zero published validation — `low` should be `med` until the mechanism has *some* scale evidence.
- **What the miner must produce in r2.** (a) **Engage with 178's outcome** — check whether the runner recorded β_v trajectory; if β_v collapsed to 0 alongside β_k, the family is dead and 202 should be `reject`-pitched; if β_v moved while β_k stayed at 0, that's the empirical grounding the bet needs. (b) **Replace the "content-based" handwave with a mechanism** — e.g., "V's per-head redundancy dominates because post-softmax V mixing is closer to a linear projection (no QK nonlinearity), so V is the dominant driver of head specialization loss surface" or similar — *cite or derive*, don't restate. (c) **State one sharp "we expect X because Y" sentence** with a falsifiable direction and magnitude (Δ on val, expected gate trajectory on β_v). (d) **Re-tag `transfer-risk` to `med`** until the asymmetric V-only form has any published scale validation.
- **Information value, the saving grace.** If 178's β_v DID move (β_k didn't), 202 is the natural follow-up probe and has unique info value — it isolates the V-axis from the K-axis. If 178's β_v also collapsed, 202 should be `reject`-pitched (family is closed mechanistically). Either way the r2 repitch should re-frame this as a probe (à la 178) rather than a lever: the success criterion is the β_v trajectory and the val-loss sign, not the val-loss magnitude.
- **Verdict: revise.** Asymmetric V-only GQA is a genuinely novel framing with unique information value IF 178's β_v trajectory was non-zero; without that anchor the bet is handwavy, the family is closed twice, and the attention-axis queue is already saturated.