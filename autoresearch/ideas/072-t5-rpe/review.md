# Review log — 072 t5-rpe

## r1 — 2026-06-11 — verdict: revise

**Mechanism is sound, dedup is clean, source is real, scope is right — but the doc has no falsifiable bar with numbers tied to a real control. One finding, fillable in <10 min.**

**5-check sweep:**
- **Source real & current:** Raffel et al., "Exploring the Limits of Transfer Learning with a Unified Text-to-Text Transformer" (JMLR 2020, arXiv:1910.10683). Foundational paper, all authors real, widely-cited. ⚠️ 2019/2020, not 2025-2026 — but T5 is *the* canonical reference for the learned-bucketed distance bias; not a reject. The prompt §2 says "prefer 2025-2026," not "require," and the 2019/2020 source is mainstream-at-scale (T5 11B), which is exactly what the `transfer-risk: low` tag requires. ✓
- **Mechanism is structural, not HP:** drop-in replacement for the current positional prior — a learned `[n_buckets]` table indexed by `clamp(|i − j|, max=n_buckets/2−1)` added directly to attention logits. Zero-init the table ⇒ step-0 bias = 0 ⇒ bit-identical to the no-bias baseline. Categorically distinct from FIRE's smooth γ-decay kernel (`models/fire_pe.py:67-70`) — that is the *whole point* of the bet per `idea.md` §Why. Not an LR/schedule/init-constant lever. ✓
- **🔴 tiny1m3m only:** doc says "the lever fires at any context length so tiny1m3m (1024 ctx) is fine." No reference to screen20m or larger tiers. ✓
- **Not already closed:** `closed.md` lines 21-26 cover RoPE base, NoPE, post-norm, layer tying, MHA/GQA/MLA, Tied QK, dilated attention, logit softcap, the norm zoo, NSA/diff/hybrid, multiscale/parallel/sink. The T5 *learned bucketed distance bias* is the canonical form every modern PE is benchmarked against and is not in this list. 013-cope (closed drift: +0.069/+0.077 ≫ gap 0.0078) is content-conditional — different mechanism, different bucketing. ✓
- **< 200 LoC:** new `T5RPEBias` module in `models/layers.py` (bucket table + log-bucket index + additive logit bias) ≈ 60-80 LoC; config flag + plumbing in `configs/llm_config.py` and `models/llm.py` ≈ 15 LoC; total ≈ 75-95 LoC, well under budget. ✓
- **Falsifiable pass/fail bar with real control at tiny1m3m:** ⚠️ doc asserts "either outcome is informative" with no control number, no pass threshold, no fail threshold, no noise band. Per the new prompt §2, "a wide expected-Δ range that can't be resolved at tiny1m3m (box noise ~±0.01 val loss) is a finding."

**Findings (must be addressed before `needs-plan`):**

- **No pass/fail bar with named control.** Doc says "if the bucketed form wins, the useful part is quantized relative distance rather than a smooth kernel" but never cites a baseline. The natural control here is the **V+q+SWA+HighRoPE+FIRE tiny1m3m** baseline (current `009-fire-pe` winner) at val **6.3234** per `closed.md:47`, since the whole bet is "is the bucket form doing what the smooth γ did, or is there a real difference?" T5-RPE is *additive on RoPE* in the natural setup (T5 uses relative bias *on top of* relative position in some variants; pure-bias-only with NoPE is the cleaner counterfactual). **Fix:** name the control(s) and the expected-Δ range. Two clean A/Bs to choose between:
  1. **FIRE-only baseline (closed 009 ctrl = 6.3234) vs FIRE+T5-bias** (stacked): the bias is purely additive on top of the current WIN. Expected Δ in [−0.005, −0.02] (T5-bias strictly *adds* a learned bucketed prior on top of FIRE's smooth prior; a win is plausible if the bucket table picks up periodic content structure the smooth γ cannot).
  2. **NoPE-only baseline (closed 053, no RoPE at all) vs NoPE+T5-bias** (clean counterfactual): tests "does T5's bias *substitute for* RoPE or only *augment* it?" Expected Δ wider in sign — could be null or modest win.
  Pick one as the primary A/B and state it; optionally stage the second. Thresholds: `pass ≤ ctrl − 0.005`, `fail ≥ ctrl + 0.005`, `noise |Δ| ≤ 0.005`. Cite the box-noise ±0.01 (per the new prompt) so the bar is meaningful.
- **Bucket count not pinned.** T5's default is 32 buckets (log-spaced). At tiny1m3m (T=1024), 32 log-buckets gives 4–8 distances per bucket near the far end — coarse enough to test the "quantized relative distance" hypothesis. Pin `n_buckets=32` in the plan (matches the paper default, removes a free knob). The "shared or per-head" knob is the *one* worth flagging — T5 paper uses shared across heads; per-head is a tiny extension. Pick shared, mention per-head as a follow-up, don't ship a knob.
- **Source-date note for the code-gate critic.** Doc is honest about 2019/2020. No fix needed — but worth one sentence in the plan mirroring the 009 pattern: "T5 has been the canonical learned-bucketed distance bias since 2019/2020; no successor has dethroned it as a mainstream pretrained form, and it is the reference point every modern PE (ALiBi, RoPE, FIRE, CoPE) is benchmarked against."

**Hand-off to reviser:** 1 blocking finding (falsifiable bar with numbers) + 1 minor (pin `n_buckets=32`) + 1 cosmetic (source-staleness sentence for the code gate). Mechanism paragraph, scale-evidence paragraph, and "Why it's worth a slot" are all solid — keep them. The Bar and Tier sections are the only rewrites. After the bar is in, this is a clean `approve` — the bet is the canonical reference for an entire lever family and tiny1m3m is the right tier to test it.
