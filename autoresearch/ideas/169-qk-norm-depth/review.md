# Review log — 169 qk-norm-depth

## r2 — 2026-06-14 — verdict: approve
- **r1 finding #1 (numeric pass/fail bar) — resolved.** `## Pass / fail bar` now locks control = **016-qk-norm WIN config** (`Tiny1M3MQKNormConfig` with `use_qk_norm` enabled; closed entry `trt=6.3906 vs ctrls 6.4044/6.4091`), pass = Δtrt < −0.005 vs that 016 control, null = |Δ| < 0.005, drift = Δtrt ≥ +0.005, crash → `needs-recode`. Numbers tied to the real control, clears the ±0.04 box noise band by ≥2×, code-implementer can write the runner contract without re-deriving.
- **r1 finding #2 (missing `## Plan`) — resolved.** `## Plan` enumerates the three files (`configs/llm_config.py` field + `Tiny1M3MQKNormDepthConfig` subclass mirroring the 162/165 dataclass pattern at ~5315-5353, `models/layers.py` kwarg + register + forward sites including the MoA `extra_K` branch at ~2437-2450, `models/llm.py` read + thread sites at ~440/607/685/850/941), the flag name `use_qk_norm_depth` (off by default), step-0 identity (byte-identical to 016), the standard tiny1m3m seed-42 run command, and the LoC budget (~40 lines, well under the 200 cap).
- **r1 finding #3 (mechanism ambiguous) — resolved.** `## Mechanism (Option A — locked)` is explicit: keep per-head `self.q_norm = RMSNorm(d_k)` and `self.k_norm = RMSNorm(d_k)` (016's existing setup at lines 1039-1040), add `self.qk_norm_scale = nn.Parameter(torch.ones(1))`, apply `q = q_norm(q) * qk_norm_scale; k = k_norm(k) * qk_norm_scale` pre-QK-matmul. The "Why Option A, not Option B" paragraph now explicitly rejects Option B (per-block RMSNorm replacing per-head) as a restructuring of 016's parameterization, +192 params instead of +12, and losing the per-head normalization shape that 016 won with.
- **r1 finding #4 (step-0 identity wording) — resolved.** `## Plan` now correctly states: "Flag ON at step 0 ⇒ `qk_norm_scale = 1.0` exactly ⇒ **byte-identical to 016's step-0 forward (max-abs-diff = 0.0)**" for the 169-vs-016 reference, and quotes `fp32 max-abs-diff < 1e-3` only for the 016-vs-unmodded RMSNorm rescaling (already accepted by 016's spec). The two comparisons are no longer conflated.
- **r1 finding #5 (mutual-exclusion asserts) — resolved.** `## Design sketch` lists three explicit `assert not (self.use_qk_norm_depth and self.X)` lines for `use_q_only_norm`, `use_k_only_norm`, and `use_qk_norm_post_rope`, mirroring the existing `assert not (self.use_cope and self.use_qk_norm_post_rope)` at line 1948.
- **Cross-check vs closed list — distinct.** 169 is not a duplicate of any closed entry: distinct from 016 (per-head-shared scale), 152 (per-head additive logit bias), 155 (per-head multiplicative temperature), 160 (post-AV per-head gain), 161 (per-layer temperature, NULL — scale-prior fight), 162 (Q-only RMSNorm, NULL). 169 starts scale-aligned at α_l=1.0 from a 016-WIN position; the per-block lever is a small adjustment on top of an already-good scale, not a fight against the canonical QK-scale prior. Clean axis.
- **Transfer-risk tag (`low`) verified vs citation.** `## Scale evidence` cites RMSNorm family well-validated at 1B+ (LLaMA 3, Qwen 2.5, Mistral) and NormFormer's per-layer gains (Shleifer et al. 2021) validated at 100M+ on long-document tasks — both back the `low` tag. Per-block QK-norm gain is a narrow extension of a well-tested primitive.
- **Tiny1m3m + seed 42 only.** Run command is `--steps 3000 --batch_size 32 --seed 42` against the standard `Tiny1M3MConfig` tier; no `screen20m`, no full ladder, no multi-seed — protocol-compliant.
- **Verdict: approve.** All five r1 findings are concretely resolved (no hand-waving, no skipped items). Sound, falsifiable, well-specified, distinct from the closed list, ≤200 LoC, identity-init-able, mechanism-not-HP. Resets `round` to 1 so the code gate gets a fresh budget.

## r1 — 2026-06-14 — verdict: revise
- **Missing numeric pass/fail bar.** `## Mechanism` and `## Why it's worth a slot` describe the bet qualitatively ("if 169-WIN > 016-WIN") but give no concrete Δ threshold or noise-band budget. The control identity is also unstated — the taste reviewer's last line specifically called this out: *"spec should lock step-0 identity to 016's forward... the control must be 016's WIN config, not unmodded-nanogpt, so the Δ is depth-conditional-on-016 not 'QK-norm yes/no'"*. Lock: **control = 016-qk-norm WIN config** (the same config that produced `trt=6.3906 vs ctrls 6.4044/6.4091`), **pass bar Δtrt < -0.005 vs that 016 control** (mirrors 016's own bar and clears the ~±0.04 box noise band at tiny1m3m by ≥2×). Add a sentence naming the null band explicitly so a code-implementer can write the runner contract without re-deriving the gate.
- **No `## Plan` section.** Compare with 162/165 (sibling ideas already in the queue): they have a `## Plan` block enumerating the files (`configs/llm_config.py` field + `Tiny1M3MQKNormDepthConfig` subclass, `models/layers.py` kwarg + register + forward sites, `models/llm.py` config read + thread). Add a matching `## Plan` that names the exact field name (`use_qk_norm_depth: bool = False` on `LLMConfig`), the MHA kwarg plumbing through `TransformerBlock.__init__` (the existing site for `use_qk_norm_post_rope` at line ~1357 in `models/layers.py`), the MoA `extra_K` branch (lines ~2437-2450), and the config-read sites in `models/llm.py` (the two MHA construction sites currently reading `use_q_only_norm`/`use_qk_norm_post_rope`). Without this, code-implementer has to re-derive file locations.
- **Mechanism ambiguous — must lock Option A.** `## Design sketch` offers two alternatives: *"replaces the per-head-shared `nn.RMSNorm(d_head)` with a per-block scalar parameter `self.qk_norm_scale = nn.Parameter(torch.ones(1))` **or** a per-block `nn.RMSNorm(d_head)` (one per block rather than per head)"*. These are not equivalent. **Option A** (per-block scalar *post*-RMSNorm, applied to 016's per-head RMSNorm output) preserves 016's per-head normalization structure and adds a multiplicative gain; this matches the lever's framing ("per-block learnable scale on top of 016's WIN"). **Option B** (per-block RMSNorm instead of per-head) restructures 016's parameterization — different mechanism, different math, different parameter count. Lock **Option A**: keep `self.q_norm = RMSNorm(d_k)` and `self.k_norm = RMSNorm(d_k)` per-head (existing 016 setup, lines 1039-1040), then add `self.qk_norm_scale = nn.Parameter(torch.ones(1))` and apply `q = q_norm(q) * qk_norm_scale; k = k_norm(k) * qk_norm_scale` pre-QK-matmul.
- **Step-0 identity comparison reference is misstated.** `## Design sketch` says *"logits are bit-identical to 016's step-0 forward... fp32 max-abs-diff < 1e-3 tolerance for the RMSNorm rescaling (same as 016)"*. The reference comparison should be **169 step-0 vs 016 step-0** (the chosen control), not vs unmodded. At α_l=1.0 the per-block multiplier is exactly 1.0 ⇒ the forward graph is **byte-identical** to 016's step-0, no tolerance needed for the *169→016* check. The `fp32 max-abs-diff < 1e-3` tolerance is for the *016→unmodded* RMSNorm rescaling (already accepted by 016's spec) and should be quoted as such — don't conflate the two comparisons. Update the wording.
- **Mutual-exclusion assertions not yet specified.** If a future config accidentally combines `use_qk_norm_depth` with `use_q_only_norm`/`use_k_only_norm`/`use_qk_norm_post_rope`, the forward path becomes ambiguous. Add an explicit `assert not (self.use_qk_norm_depth and self.use_q_only_norm)` (and the symmetric for K-only, and for 016's symmetric) at the top of `MultiHeadAttention.forward`, mirroring the existing `assert not (self.use_cope and self.use_qk_norm_post_rope)` at line 1926. Same scope as the reviser-note precedent.
