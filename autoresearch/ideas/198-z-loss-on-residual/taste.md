## r1 — 2026-06-15 — verdict: revise

**Taste gap (one sentence):** the bet is *theoretically* sharp ("residual grows O(√L) while logits are bounded by 1/√d_model init, so residual z-loss binds where logit z-loss nulled") but lacks an empirical falsification signature, and the closed-loop pattern this week is heavy against residual-side magnitude levers at 0.94M (167 null, 196 reject, plus 017/116/150/164 in the wider family) — the miner needs to commit to a specific residual-norm trajectory that would *falsify* the bet, and explicitly differentiate from the two active siblings (197 init-time α-scale, 195 mid-attn RMSNorm).

**Findings:**

- **Empirical prior in the magnitude-stability family is heavy, and 167 is the direct sibling.** 167-logit-zloss (null Δ=−0.0018 inside band) is the most informative prior: a small L2-style penalty on a magnitude-related quantity *did not bind* at 0.94M, and 167's z_coef=1e-4 is the same order of magnitude as 198's proposed 1e-4. The miner's argument for 198 is that 167 targeted the *logit-side* magnitude (bounded by 1/√d_model init) while 198 targets the *residual-side* magnitude (unbounded by init, grows O(√L)). That argument is fine *in principle*, but the closed 167 doesn't tell us which axis is binding — it only tells us the *logit-side* axis isn't. The miner needs to commit to an empirical signature: "we expect to see the per-token residual L2 norm grow from O(√d_model) at step 0 to ≥5·√d_model by step 1000, in which case the penalty's contribution to total loss reaches O(0.01) and the gradient is non-trivial; if the residual norm stays bounded at ≤3·√d_model (i.e., the existing RMSNorm + QK-norm + final_proj scaling already keep it well-conditioned), the lever cannot bind and 167's null is the symmetric story on a different tensor." Without that signature, the bet is unfalsifiable in the NULL direction.
- **Leverage at L=12 is modest, and the baseline architecture is well-conditioned.** modded-nanogpt already applies (a) per-block RMSNorm pre-attn/pre-FFN, (b) QK-norm pre-softmax, (c) a learnable per-channel final_proj scaling. The residual stream at L=12 grows by O(√12)≈3.5× from input to output, but each block's contribution is *already* well-conditioned by the surrounding normalizations. A penalty `log(1+‖r‖²)·1e-4` adds a small gradient component pointing the optimizer away from high-magnitude regions — but the optimizer is *already* finding a well-conditioned solution (champion val ≈6.24). The leverage argument ("at 0.94M, residual grows by O(√L)") understates how much conditioning is already applied by the existing scaffolding. The miner should quantify: at this tier, what is the *expected* residual L2 norm at step 2k, and how does that compare to the penalty's saturation point `log(1+x²)≈2`?
- **Portfolio fit is the most concrete concern — three stability/residual variants in the active queue, and 196 was *just* rejected on this family today.** 195-mid-attn-rmsnorm (mid-attn normalization, attention-side), 197-output-residual-sqrt-2l (init-time α-scale, residual-side), 198-z-loss-on-residual (training-time L2 penalty, residual-side). 196-block-residual-ema was taste-rejected earlier today with the explicit note "cross-block residual-mixing axis closed at 0.94M." 196's mechanism (cross-block EMA with detach + learned scalar gate) is technically distinct from 198 (loss-side L2 penalty, no learnable params), but the *broader* closed-loop story is "residual-side levers don't bind at this tier." The miner should differentiate 198 from 196 explicitly — the 196 reject was driven by the detach + learned-scalar-gate structure, which 198 doesn't have. But the residual-side L2 penalty is a *different* lever on a *related* (not identical) axis. The active queue is crowded; one of {195, 197, 198} should be de-prioritized unless the bet can be sharpened.
- **Information value is real and goes in the accept column.** A clean NULL is informative: "paired with 167's logit-side null, *both* magnitude axes (logit-side and residual-side) are well-conditioned at 0.94M" — that's a load-bearing closure for the magnitude-stability thesis at this tier. A clean WIN is also informative: it opens a stability lever for the 135M recipe where residual growth is more dramatic. The two-sided information value is high; the issue is whether the *leverage* is high enough to make a clean NULL the *expected* outcome, in which case the slot is being spent on a predicted null.
- **Lever design is clean.** z_coef=0 at step 0 (or even z_coef=1e-4 with the residual small at step 0) gives step-0 byte-identity. 0 new params. One forward hook, one config flag, one loss term. Identity-init-able. No early-step gradient interaction with the optimizer. No detach concerns (no learnable params to compete with the main path). This is the cleanest possible implementation of the regularizer.
- **Transfer-risk: low is correct.** A residual L2 penalty is a generic regularizer; it would scale to 135M (deeper models have more dramatic residual growth, so the lever's leverage grows with depth). The mechanism is not tied to tiny-vocab/3M-token regime/embedding dominance. Not a scale-bound gimmick.
- **What's needed on re-pitch to make this `accept`.** Three concrete asks:
  1. **Pre-register a residual-norm trajectory that would falsify the bet.** E.g., "if the per-token residual L2 norm stays within [1, 3]·√d_model throughout the 92-step run, the penalty contribution is ≤ 1e-4·log(1+9) ≈ 1e-3, which is < 0.02% of ce_loss — the lever is too small to bind. We commit to this falsification: residual norm trace plotted at steps {0, 100, 500, 1000, 2000}." This makes the NULL informative.
  2. **Explicitly address 167's null with a *testable* prediction, not just a theoretical one.** "If 167's null was driven by the LM head's output being well-conditioned (init-bounded), then 198 *should* bind — but only if the residual stream is in a *different* regime. We predict the residual L2 norm at step 2k will be ≥ 5·√d_model (= 5·8 = 40 in float-space units) — that's the signature that makes 198 different from 167."
  3. **Differentiate from 197 and 195 in one paragraph.** 197 is init-time α-scale (changes the *forward graph* at step 0 if α ≠ 1, otherwise bit-identical with a frozen scalar); 195 is mid-attn RMSNorm (modifies attention flow); 198 is training-time residual L2 penalty (modifies loss only). The three are mechanistically distinct, but they all push on the same broader axis (residual/attention magnitude stability). The miner should state: "if 197 wins, the binding lever is init-time scaling; if 198 wins, the binding lever is training-time regularization; a NULL on both is the cleanest possible closure of the residual-magnitude axis at this tier." The active queue cannot hold three of these in parallel without a stated priority.

**Verdict routing:** `needs-repitch` — re-pitch with the three asks above (residual-norm trace signature, testable 167-comparison prediction, explicit 197/195 differentiation). Round resets to 1 for the miner.
