# Taste log — 097 ASEntmax

## r1 — 2026-06-11 — verdict: accept
- Strong long-context generalization claim and a clear sparse-attention mechanism.
- Distinct from the fixed-pattern sparse ideas already in the queue because it adapts sparsity instead of hard-coding it.
- Worth a slot because it targets a real failure mode of dense softmax at long context.

