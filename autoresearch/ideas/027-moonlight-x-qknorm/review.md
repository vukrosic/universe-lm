# 027 — review log

## r1 — 2026-06-10 — verdict: approve

- **Source & parents verified.** `015-moonlight-muon-rms/evidence.md` and `016-qk-norm/evidence.md` are real, same-session A/B: ctrl 6.4044/6.4091, both 6.3906 (Δ −0.0138/−0.0185, both ≫ 0.0047 ctrl-to-ctrl gap, both pass their own plan bars −0.01 / −0.005). Citations (arXiv:2502.16982 Moonlight, arXiv:2302.05442 Dehghani) are real and current. Composition is sound.
- **Mechanism is a mechanism.** Moonlight = per-tensor RMS rescale in the optimizer step (`optimizers/muon.py`); QK-LayerNorm = per-head LayerNorm on Q,K in MHA forward (`models/layers.py`). No shared state, sequential sites, step-0 identity for both. Not a hyperparameter lever.
- **Flag wiring confirmed in repo.** `configs/llm_config.py:546` has `use_moonlight_muon: bool = False` (default-off, recipe-flipped at 718). `configs/llm_config.py:559` has `use_qk_layernorm: bool = False` (default-off, recipe-flipped at 733). Both are plumbed through `models/llm.py:207/274` and `models/layers.py` to the call sites. Composition is a two-flag enable — well under the 200 LoC budget.
- **Not closed / not duplicated.** 015/016 are closed as WINs (closed.md:32-33) and explicitly available for stacking (closed lever list treats them as shipped, not as banned). 026 (009×016, FIRE×QK-Norm) is parallel in design but distinct — it tests positional×MHA, not optimizer×MHA. No mathematical duplicate. Complementary, not redundant.
- **Finding (low): flag-name shorthand in idea.md.** Idea.md refers to "use_qk_norm" twice in the body; the actual config flag is `use_qk_layernorm` (and the runtime MHA attribute `use_qk_layernorm`). The reviser/planner must use `use_qk_layernorm` when writing the config recipe — not "use_qk_norm" (which does not exist). The existing 016 plan.md already uses the correct name; the 027 plan should follow.
- **Finding (low): pass/fail bar not in idea.md.** The idea's "Why it's worth a slot" sketches the hypothesis range (~−0.028 additive, subadditive, or null) but never states a single number for the pass bar. Recommend the planner write: **PASS** `trt ≤ ctrl − 0.01` (matches 015's bar, stricter than 016's −0.005, well above the in-session ctrl-to-ctrl gap 0.0047). **NULL** `|Δ| < 0.01` (sub-noise, log and move on). **DRIFT** `trt > ctrl + 0.01`. A clean null is informative (orthogonality test), not a failure mode — language to mirror from taste log.
- **Finding (low): control selection.** The natural control is `Tiny1M3MConfig` (plain Muon, RMSNorm Q/K), treatment is a derived config that flips BOTH `use_moonlight_muon=True` AND `use_qk_layernorm=True` on top of `Tiny1M3MConfig`. Inside the same A/B session, include a `Tiny1M3MConfig` and a `Tiny1M3MMoonlightMuonConfig` and a `Tiny1M3MQKNormConfig` (the two parents individually) to disentangle single-lever vs joint Δ, just like the 015/016/017 batch did. This turns the run into a 4-arm A/B and resolves the orthogonality question directly.
- **Transfer-risk: low, justified.** Both parents demonstrated at billion-param (Moonlight) and 22B+ (QK-Norm); composition does not introduce a new scale risk. Frontmatter tag matches the evidence.
- **Verdict: approve.** Two proven WINs, both flags already wired, no LoC work, falsifiable orthogonality hypothesis, well above the noise floor. Move to `needs-plan`. Round reset to 1 for the code gate's fresh budget.
